# The fallback build, for when the full one does not fit the free tier.
#
# Retrieval is simply absent: `correction.check_with_retrieval` catches the import
# error and corrects without grounding, and GET /health reports
# retrieval.available = false with the reason. Nothing silently degrades.
#
# WHAT IS LOST is the citation shown beside a correction, and nothing else. Every
# published measurement is produced locally by evals/, so the numbers in the README
# are unaffected by which of these two files the demo was built from — but the demo
# then shows a weaker product than the one the README describes, and that has to be
# said on the page rather than left for a visitor to notice.
streamlit==1.60.0
langchain==1.3.14
langchain-google-genai==4.3.2
langchain-anthropic==1.5.3
python-dotenv==1.2.2
sudachipy==0.6.11
sudachidict-core==20260723
langgraph==1.2.10
streamlit-webrtc==0.77.0
