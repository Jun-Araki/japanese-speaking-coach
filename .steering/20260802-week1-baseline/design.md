# 第1週 設計

対応する要求: [requirements.md](requirements.md)

---

## 実装アプローチ

**テキストのみの縦スライスを1本通す。**抽象化は入れない。第3週に FastAPI と LangGraph へ
切り出すことは分かっているが、**今週その形に寄せない**——使われ方が確定する前の抽象化は
作り直しになる。関数を素直に呼び、モジュール境界だけ将来の分割に合わせておく。

```
Streamlit (app/)
   │  st.session_state に会話履歴
   ├──▶ dialogue.reply(scene, level, history) ──▶ Claude API ──▶ 1〜2文
   └──▶ correction.check(sentence, scene, level) ──▶ Claude API（構造化出力）
                                                        │
                                            会話中は表示せず session_state に溜める
                                                        ▼
                                                  終了時に review 表示
```

**訂正は毎ターン走らせるが、表示は終了時。** 今週は同期呼び出しでよい（並列化は第4週の
最適化枠。ここで先に非同期にすると数字の前後比較が取れなくなる）。

---

## 変更・追加するコンポーネント

| モジュール | 今週作るもの |
|---|---|
| `app/` | Streamlit 1画面。場面・レベル選択 → 会話 → 振り返りの状態遷移 |
| `dialogue/` | `reply()`。場面とレベルに応じた相手役プロンプト。1〜2文に制限 |
| `correction/` | `check()`。構造化出力。**ベースライン版もここに `baseline_check()` として置く** |
| `evals/` | 採点スクリプト、実行記録の書き出し |
| `data/evaluation/` | 120件の JSON |
| `data/grammar/` | 8本（今週は RAG に載せない。文章を書くだけ） |

`retrieval/` `nlp/` `api/` は今週作らない。

---

## データ構造

[docs/ja/functional-design.md](../../docs/ja/functional-design.md) の定義に従う。今週の範囲では：

**評価データ1件**（`data/evaluation/items.json` の配列要素）

```json
{
  "id": "eval-001",
  "scene": "greeting",
  "learner_sentence": "おはようです",
  "needs_correction": true,
  "corrected_sentence": "おはようございます",
  "reason_en": "The polite morning greeting is a fixed phrase.",
  "split": "dev"
}
```

**訂正の構造化出力**（`correction.check()` の戻り値）

```json
{
  "needs_correction": true,
  "corrected_sentence": "おはようございます",
  "reason_en": "The polite morning greeting is a fixed phrase.",
  "grounding_ids": []
}
```

`grounding_ids` は今週は必ず空配列。第2週に RAG を入れたときに埋まる。
**キーは最初から最終形にしておく**（後から増やすとデータの作り直しが発生する）。

**実行記録**（`evals/runs/YYYYMMDD-HHMM.json`）

```json
{
  "run_id": "20260806-0730",
  "implementation": "baseline",
  "model": "gemini/gemini-2.5-flash",
  "prompt_version": "baseline-v1",
  "n": 40,
  "split": "test",
  "date": "2026-08-06",
  "detection_accuracy": 0.0,
  "over_correction_rate": 0.0,
  "format_compliance_rate": 0.0
}
```

---

## 訂正の設計（2026-08-04 実装時に確定）

### JSON を自前で解析する（制約デコードを使わない）

LangChain の `with_structured_output` や Gemini の `responseSchema` を使えば JSON は
まず壊れない。**それでも使わない。**

`format_compliance_rate` は「壊れていない JSON であり、かつ理由が英語で返った割合」
（[glossary](../../docs/ja/glossary.md) §5）で、**今週出す3つの数字のうちの1つ**である。
制約デコードにすると JSON 側は構造上つねに適合になり、**測っていない数字を測ったことにする。**
残る言語の取り違えも測れないままで、これは llm-jp-eval の批判記事が挙げた罠そのもの。

副次的な利点として、ベースラインと本実装が同じ解析経路を通る（design.md 冒頭の方針どおり
採点コードが1本で済む）。

### 再試行は「何が壊れていたか」を添える

`temperature = 0` なので、同じプロンプトを投げ直せば同じ壊れた出力が返る。
**再試行が形だけになる。** そこで1回目の出力と「どこが駄目だったか」を会話に足して投げ直す。

### `format_compliance_rate` は1回目の出力で判定する

再試行で直った場合も**不適合のまま記録する**。再試行が書式の失敗を消せるなら、
壊れた件を分母から捨てるのと同じ嘘になる。訂正そのものは再試行の結果を使う
（判定の精度と書式の適合は別々に記録する、という §5 の原則どおり）。

### 理由が英語かの判定 — 引用の中は見ない

助詞を説明するには「は」と書くのが自然で、**引用の日本語まで不適合にすると
良い理由ほど落ちる。** 「」および引用符の中を除いてから日本語が残るかを見る。

| 理由 | 判定 |
|---|---|
| `「は」 marks the topic, so 「が」 sounds odd here.` | 適合 |
| `「ありがとう」は友達に使う言い方です。` | 不適合 |
| `This is 丁寧な言い方ではありません。` | 不適合 |

### モジュール名は `correction/engine.py`

`check.py` にすると `correction/__init__.py` が再輸出する関数 `check` と衝突し、
`correction.check` が関数かサブモジュールか決まらない（実際にテストが壊れて気づいた）。
公開する呼び出しは設計どおり `correction.check()` のまま。

### 振り返りに出せなかった文

2回とも JSON が壊れた文は、[functional-design](../../docs/ja/functional-design.md) の
とおり訂正としては出さない。ただし**件数だけは画面に出す**。黙って消すと
「この文は問題なかった」と読めてしまい、それはこの文について唯一分かっていないことである。

---

## ベースラインの設計（今週の主役）

**素朴版 = 大規模モデルに1回投げるだけ。** 検証も RAG も形態素解析も無い。

- プロンプトは「この日本語を直して、理由を英語で教えて」相当の1回呼び出し
- **意図的に良くしようとしない。** 素朴版を丁寧に作り込むと比較表の差が縮み、
  第2週以降の実装の価値が見えなくなる。ただし**出力形式だけは同じ JSON に揃える**
  （形式が違うと採点コードが2本になり、そこが不具合の温床になる）
- 測るのは `detection_accuracy` / `over_correction_rate` / `format_compliance_rate` の3つ

**測るのは2回。それ以降は第4週まで触らない。**

| いつ | 対象 | 位置づけ |
|---|---|---|
| Day 4（8/6） | その時点の評価用 **20件** | **暫定。** 配管が通り数字が出ることの確認。README には載せない |
| 8/10（月） | 評価用 **40件**（確定後） | **正式。** README とベースライン比較表に載るのはこちら |

**2回に割れるのは、評価用が 8/8 まで40件に揃わないため。** 60件時点で振った `split` は
**最後まで動かさない**ので、Day 4 で測った20件は最終の評価用40件の部分集合であり、
**評価用に dev の件が混ざることはない。**

**正式測定は検証ノードを1行でも書く前に行う。** 本実装ができてから測ると
「本実装を知ったうえで作った素朴版」になり、比較表そのものが成立しない。

**実行記録には `n` と `split` を必ず書く。** これが無いと、あとから
「README のこの数字は20件のほうか40件のほうか」を復元できない。

---

## 採点の設計

- **機械採点するのは `needs_correction` の一致だけ。**
  `corrected_sentence` は完全一致で採点しない（[docs/ja/glossary.md](../../docs/ja/glossary.md) §5）
- `format_compliance_rate` は**精度と分けて記録する**。JSON が壊れた／理由が日本語で
  返ったケースを精度の低下と混同しない
- **採点スクリプト自体に pytest を書く。** 採点の不具合はモデルの改善と区別がつかない
- 実行のたびに**評価用から5件を手で照合**する

---

## 影響範囲

- 既存コードなし（初回実装）
- [docs/ja/glossary.md](../../docs/ja/glossary.md) の閾値2つ（編集距離・レベル超過語彙）は
  今週の範囲外。第2週に確定させて glossary へ書き戻す
- `docs/ja/repository-structure.md` の *(planned)* 表記は、作ったディレクトリから外していく

---

## 実機確認の結果（2026-08-03・iPhone Safari）

### ① 録音 = 成功（決着）

**テキスト方式には戻さない。音声は予定どおり第3週で載せる。**

**操作感（実機で観察）**

- **押して開始 → もう一度押して停止。** 押しっぱなしではない
  → 画面ではボタンの文言を状態で変える（「話す」／「話し終える」）
- **マイクの許可は、ページを開き直すたびに1回聞かれる**（録音のたびではない）。
  会話の開始時に1回だけなので実用上は問題ない。ただし**テスターへの説明文には必ず書く**

### ② 自動再生 = 成功（決着）

**当初「失敗」と記録したが誤りだったので取り消した。** 音の高さを毎回変える版で再確認した結果：

- **②-A（即時）・②-B（3秒待ち）とも、押すたびに毎回鳴った**
- **②-C（毎回まったく同じ音）だけ、1回鳴ったあとは鳴らなかった**

→ **原因は Safari の制限ではなく、音声の同一性。** Streamlit は音声を内容のハッシュで参照するため、
**同じバイト列を再度描画しても新しい再生が起きない。** 中身が違えば毎回鳴る。

**第3週は `st.audio(autoplay=True)` で組んでよい。** 3秒待った ②-B も毎回鳴ったので、
モデルの応答待ちを挟んでも「ユーザー操作の直後」の扱いは切れない。

**ただし本番で1つ罠がある（第3週で必ず対処する）**

**AI の返事は繰り返す。** 初級者向けの相手役は「はい、そうです。」「いいですね。」のような短い相槌を
返しやすく、**同じ文が2回出れば音声合成の結果も同じバイト列になり、2回目は鳴らない。**
これは②-C で実際に再現した現象そのもので、**学習者からは「たまに音が出ない」と見える。**

対処は、**音声のバイト列をターンごとに必ず変えること。** 末尾に長さの違う無音を数十ミリ秒足すだけでよい。
カスタム JS は不要で、「UI フレームワークを入れない」方針とも衝突しない。

### ③ この時点で確定していること

- テスターへの説明文（第3週4日目）に**「マイクの許可を求められたら Allow を押す」**を必ず入れる。
  **書き忘れると「マイクが動かない」という感想が返ってきて、本当に測りたい訂正の質の話にならない**
  （返事の再生は自動なので、こちらの説明は不要になった）
- `app/audio_check.py` は役目を終えた。**会話画面ができた時点で削除し、
  Community Cloud の Main file path をそちらへ差し替える。** 枠ごと消さないこと —
  サブドメインはアプリに紐づくため、削除すると確保した URL を手放すことになる
- **画面に「発音は見ていない」と1行書く**（第3週、音声を載せるとき）。文面は
  `This checks the words you choose, not how you pronounce them.` 程度でよい。
  **声で話すアプリを渡されたら、学習者は発音も見てもらえると思う。** そして音声認識が
  言い間違いを黙って直した場合、**正しい文字が出るので「発音は通じた」と誤解して持ち帰る。**
  書かなければ感想フォームに「発音が違うのに何も言われない」と返り、
  **本当に測りたい訂正の質の話が埋もれる。** コストはほぼゼロで、
  「測ったことしか主張しない」という方針とも一致する

---

## 評価データの検証で分かったこと（2026-08-05・60件）

候補生成は [evals/generate_items.py](../../evals/generate_items.py) で自動化し、**ラベルは付けさせない。**
モデルに文もラベルも決めさせると、`false` の件が「そのモデルが自然だと思う文」になり、
**`over_correction_rate` を最も自分に甘い題材で測ることになる。** 候補には
`intended_label`（そう作れと指示した内容）だけを持たせ、判定はネイティブ検証で確定させた。

### ネイティブ検証で判定が変わった件

**60件中8件（13%）** で `intended_label` が覆った。**この割合自体が報告に値する数字**であり、
「候補生成は自動化し、全件を検品した」という方法論が形だけでないことの証拠になる。

| 変わった向き | 件 |
|---|---|
| `true` → `false` | ごきげんよう。／良い朝ですね。／あなたはこの本を持っていますか。／あなた、どこですか。 |
| `false` → `true` | 寒いですね、今日。／名前は？／すみません、トイレはどこ？／今日、残業できます。 |

### ラベルより、直した文のほうが誤りが多かった

判定が合っていても `corrected_sentence` が使えない件があった。**評価データとしてはこちらのほうが厄介で、
機械採点に出ないので気づかれない**（§5 のとおり直した文は完全一致で採点しない）。

- `スーパーに買い物します` の直しに「を」が落ちていた
- `良い朝ですね` を「いい天気ですね」に直そうとしたが、**直した文のほうが不自然だった**
- `部長、話したいことあります` に「に」を足そうとしたが、**呼びかけの「部長、」は正しく、直しのほうが誤り**

### 「あなた」を理由にした `true` は成立しなかった

生成された45件のうち8件が「あなたは不自然」を理由にしていた。**1つの型が判定の正しさを支配する**うえ、
検証の結果その理由自体が通らなかった（`あなたはこの本を持っていますか` は `false`）。
不自然に見えるのは語順か直訳のほうなので、**理由はそちらを名指しする**。glossary §2 に書き戻した。

### 丁寧さの下限は場面ではなく相手で決まる

候補の段階で、同じくだけ方が `true` と `false` に分かれていた（`これ、いくら？` は `true`、
`これ、ください。` は `false` など）。**放置すると2つの主要指標がどちらに倒したかの数字になる。**
検証で「トイレはどこ？は同僚には良いが店員には直す」と判明し、**相手による3段階**に確定した。
glossary §2 の表がそれで、Day 5 の追加60件はこの規則で作る。

---

## 追加60件で分かったこと（2026-08-07・Day 5）

### 生成プロンプトを v2 に上げた

**v1 の生成プロンプトは glossary §2 の判定基準を1つも渡していなかった。** 丁寧さの3段階も
「あなた」の規則も **Day 3 の検証で決めて glossary に書き戻したのに、生成側は更新していなかった。**

結果、v1 の候補60件は **14件（23%）で判定が覆り、うち12件がその2つの規則によるもの**だった
（Day 3 の覆りは 8件＝13%）。**同じ検証を2度やっていた**ことになる。

| | v1 | v2 |
|---|---|---|
| 判定が覆った件 | **14 / 60（23%）** | **7 / 60（12%）** |
| うち丁寧さの下限・「あなた」由来 | 12 | 1 |

v2 で渡すようにしたのは3つ。**①場面ごとの丁寧さの下限**（`dialogue/scenes.py` の
`POLITENESS_FLOORS`。glossary §2 の表をコードに置いた）**②「あなた」は欠陥ではない**
**③文は書かれたとおりに判定する**（「本当はこう言いたかったはず」で `true` にしない）。

**ラベル付けの権限は動いていない。** 生成側に**基準**を渡しただけで、**ラベル**は相変わらず
付けさせていない（`intended_label` のまま）。訂正のプロンプトにも触っていないので、
**ベースライン比較は影響を受けない。**

### 監査2周で分かったこと

サブエージェント2体（言語の正しさ／データセットの設計）に独立して監査させ、2周回した。

| | 差し替え | 修正 | 判定 |
|---|---|---|---|
| 1周目 | 14件 | 6件 | — |
| 2周目 | 3件 | 11件 | **条件つきで可** |

**1周目で最も効いたのは重複の指摘。** 完全一致の重複はゼロだったが、
**「一方を直せるエンジンはもう一方も必ず直せる」件が20件**あった。とくに
`corrected_sentence` が既存件の直しと**文字列一致**する件が5件（`手伝ってくれてありがとう。`
`感謝しています。` など）。**学習者の文が違っても、直しが同じなら測っているものは同じ。**

**2周目で最も効いたのは「直した文が不自然」の指摘。** Day 3 と同じ失敗が再発していた
（`大変、荷物でした` → `大変な荷物でした` は文法的には正しいが、荷物を運んでもらった礼としては
言わない）。**直した文は機械採点に出ないので、読まなければ永久に残る。**

**やり過ぎた修正も指摘された**：`それ、手伝って。` の直しを `〜ていただけますか` にしていた。
**glossary §2 は段階Cに です・ます までしか求めていない**のに、直しだけ敬語に上げると
**訂正エンジンの目標値を黙って引き上げる**ことになる（既存の eval-055 とも食い違う）。`〜てください` に戻した。

### 誤りの型を数えたら、偏りが見えた

120件を型で数えて初めて分かったこと。**助詞が26件（29%）で、うち「に ⇔ で」だけで6件**あった。
`detection_accuracy` が「助詞を検出できるか」の数字に寄る。差し替え後は助詞21件・に/で 3件。

**逆に1件も無い型もあった**：助数詞・否定形・自他の対・授受の向き・敬語の向きの5つ。
差し替えで **敬語の向き2件・自他の対1件・授受1件・否定形1件**が入ったが、
**助数詞は最後まで入らなかった**（`--focus` で名指しで頼んでも、モデルは慣れた誤りに戻る）。
**測っていない型があることは、隠さず先に書くほうが強い。**

### `--focus` を足した（当初の設計には無い）

差し替え用の候補を「この型で」と指定して生成する引数。**9月に250件へ増やすときにも要る**——
件数を増やしても型が偏ったままなら、増えるのは測っていることの量ではなく同じことの繰り返しになる。
ただし上記のとおり**指示どおりの型が返る保証は無い**ので、生成後に数え直す工程は消せない。

### ネイティブ検証の結果（2026-08-08）

**機械の事前検品が提案した60件のラベルのうち、3件が覆った。3件とも `false` → `true`。**
**向きが揃っているのが重要で、機械は一貫して「直さない」側に倒していた。**
そのまま採用していたら `over_correction_rate` が実力より良く出ていた。

| 学習者の文 | 機械 | ネイティブ | 直した文 |
|---|---|---|---|
| `私は待ちます。` | そのまま | **直す** | `私は待っています。` |
| `この資料、確認しますか。` | そのまま | **直す** | `この資料をご確認いただけますか。` |
| `お仕事、何？` | そのまま | **直す** | `お仕事は何でしょうか？` |

**先の2件は、機械の提案を伏せて答えてもらった。** アンカリングを避けるためで、
2件とも外れたことが**「基準が人に伝わる形になっていない」ことの証拠**になった。
これは 8/9 の第二採点者との `rater_agreement` と同じ性質の数字である。

### 3件のうち2件は、ラベルではなく基準のほうが誤っていた

**個別の判断ミスではなく、glossary §2 の定義が足りていなかった。** ここが今日いちばん重い発見。

| 覆った件 | §2 のどこが足りなかったか | 書き戻した内容 |
|---|---|---|
| `この資料、確認しますか。` | 段階Cを「です・ます があればよい」とだけ書いていた | **です・ます で足りるのは自分の動作。上司に相手の動作を求める文は尊敬語・依頼形が要る**（`少し考えます` が `false` のままなのは自分の動作だから） |
| `お仕事、何？` | 段階Bの下限を「お／ください／です／ます のどれか1つ」と書いていた | **「お」だけでは足りない。** 「お」は語を丁寧にするだけで文の丁寧さを決めていない。**例外は言いさし**（`お名前は。`） |

**ラベルが動いた既存件は無い。** 動いたのは既存2件の**直した文**だけで、機械採点は
`needs_correction` しか見ないので**測定への影響はゼロ**。

- `eval-055` `資料を見せて。` の直しを `資料を見せてください。` → **`資料を見せていただけますか。`**
- `それ、手伝って。` の直しを敬語へ戻した（8/7 に監査の指摘で `〜てください` に下げていたが、**その向きが逆だった**）

**監査を2周回してもこの2つは出てこなかった。** サブエージェントは §2 の**字面には忠実**で、
だからこそ**字面が間違っている場所は指摘できない**。基準そのものの誤りはネイティブにしか見つけられない
——**この工程を飛ばせない理由が、実例として1つ増えた。**

### 内訳を戻した

3件の反転で **48:12** に崩れたので、監査が落とすべきと指摘していた `true` を3件落とし、
`false` を3件入れて **45:15** に戻した（場面ごとの配分も `BATCH_PLAN` どおり）。

| 落とした件 | 理由 | 入れた件 |
|---|---|---|
| `お名前は何を言いますか？` | 直した文がバッチ内の別件と完全一致（最後の衝突） | `名前は何ですか？` |
| `今どこがいますか？` | `今、あなたはどこにありますか？` と近い重複 | `電車、遅れていましたか？` |
| `私は会議を持っています。` | `持つ` の直訳が120件で4件目 | `資料はコピーしました。` |

`電車、遅れていましたか？` は**助詞を落とした `false`** ＝ 助詞の欠落を直す `true` が多数ある中で、
**訂正エンジンが助詞の補完を過剰般化していないか**を測る件になる。

### 120件に統合した

`eval-061`〜`eval-120` を付与し、`split` は **`start_index=60` から続きで割った**
（Day 3 の60件の `split` は1件も動かない）。結果は **dev 80 / test 40、場面ごとに 2:1**。
機械点検（`tests/test_evaluation_items.py`）は全件通過。

**`test` 側の内訳は `true` 27 / `false` 13。** `over_correction_rate` を `test` だけで見ると
**分母13件＝1件が7.7ポイント動く。** README には `n` を必ず併記する。

> **ここに「`test` 単独の値だけを載せない（120件全体の値と並べる）」と書いていたのは誤り**（2026-08-09 に訂正）。
> **分母が薄いことへの対処は、分母を混ぜることではない**——120件のうち80件は `dev` で、
> 第2週にプロンプトを合わせれば合わせた分だけ数字が上がる。**公開するのは `test` の値だけ**で、
> 薄い分母は**幅（±19〜27pt）をそのまま書くことで扱う**。詳細は「誤差の但し書きは、直した先も間違っていた」。

---

## ベースライン測定で分かったこと（2026-08-06・test 20件）

実行記録：[`evals/runs/20260806-0724-baseline-test.json`](../../evals/runs/20260806-0724-baseline-test.json)

| 指標 | 値 | n |
|---|---|---|
| `detection_accuracy` | **100%** | 13 |
| `over_correction_rate` | **57.1%** | 7 |
| `format_compliance_rate` | **100%** | 20 |
| 判定不能（JSON が2回とも壊れた） | 0 | 20 |

**n=20 の暫定値。README に載るのは 8/10 に test 40件で測る正式値。**
誤差の目安は n=20 で ±11pt 程度あり、とくに `over_correction_rate` は分母が7件しかない。

### `over_correction_rate` が 57.1%（7件中4件）

**素朴版は、そのままで自然な文の半分以上を直しに行った。** これは PLAN.md §2-4 が
「ここが一番差が出るはず」と予想した箇所そのもので、**比較表の主役が実際に効きそうだと分かった。**

**どの4件がどう外れたかはここに書かない。** 4件は `test` の件であり、
中身を読んで対策を考えた時点で「評価用を見て調整した」ことになる（[glossary](../../docs/ja/glossary.md) §7）。
**誤り分析は第2週に dev 80件で行う。**

### `detection_accuracy` が 100% は「良い」ではない

直すべき13件を全部直すのは、**全部直しに行くモデルなら自動的に達成できる。**
実際 `over_correction_rate` が 57.1% なので、この 100% は精度ではなく
**直す方向に倒れている**ことの裏返しとして読むのが正しい。**2つの指標は必ず並べて出す。**

### 英語判定の規則を Day 4 に直した（数字が動いた）

最初の実装は「引用符の外に日本語が1文字でもあれば不適合」だった。**20件中9件が不適合になり、
9件すべてが英語の説明文だった**（`the masu-stem of the verb (遅れ)`、`add です` の類）。
`format_compliance_rate` は 55% と出たが、**日本語で返った件は1件も無かった。**

- 指標の名前は「理由が英語で返った割合」なので、**引用の作法を測っていたのは定義違反**
- **llm-jp-eval の批判記事が挙げた「言語の取り違え」の罠を、逆向きに踏んでいた**
- 割合による判定に変更（閾値は `config/thresholds.toml` の `reason_language`）。
  引用符なしで日本語が残った件は `japanese_left_unquoted` として実行記録に残すが**数えない**
- glossary §5・§7（ja / en）に書き戻した

**この修正の前に取った実行記録は削除した。** 採点規則が変わっており、
**同じスクリプトで再現できない記録を残すほうが害になる**（残せば、あとで
比較表に混ぜてしまう）。同じ理由で実行記録に **`scorer_version`** を足した——
採点側の規則で動いた数字は、プロンプトの版だけ見ていると「モデルが変わった」と読める。

### ベースラインにも場面とレベルを渡す

素朴版は**作り込まない**が、**与える情報は本実装と同じにする。** ラベル自体が
「誰に言うか」で決まる（[glossary](../../docs/ja/glossary.md) §2）ので、場面を伏せた素朴版は
「素朴だから」ではなく「情報が足りないから」外し、**比較表が「場面を教えたかどうか」の表になる。**
差は**プロンプトの中身と、検証・RAG・形態素解析の有無**に閉じる。

---

## 採点キットと文法リファレンスで分かったこと（2026-08-08・Day 6）

### 一致率の20件は `dev` から採る

[glossary](../../docs/ja/glossary.md) §5 の `rater_agreement` は「両方が採点した20件の一致率」としか
書いておらず、**どの分割から採るかが決まっていなかった。** `test` から採ると、採点のために自分が
評価用の中身を読むことになり、第2週以降のプロンプト調整がその記憶に汚染される（§7）。
一致率は**2人が尺度を同じに読んでいるか**を見る数字なので `dev` で成立する。§5 に書き戻した。

**副作用が1つあった。** §6 は「40件を採点し、うち20件を第二採点者が」と書いていたが、
20件を `dev` から採ると**40件の内数ではなくなる。** 人手の採点は合計60件になる。§6 も直した。

### 場面は「丁寧さの段階」ではなく、モデルに渡した文言そのものを刷る

最初は採点キットに丁寧さの段階（§2 の A/B/C）だけを刷っていた。これは**相手を特定しない**——
段階Bの文言は「初対面の相手**または**店員」であり、2択のまま採点者に渡ることになる。

実物で問題が出た。キット #13 は「良い一日。→ **いってらっしゃい！**」で、この直しは
**相手が出かけるところ**でなければ成立しない。その情報は `scene_brief`（モデルに渡した文言）に
あり、丁寧さの段階には無い。**情報不足で割れた採点は、尺度のずれではないので §6 を直しても直らない。**

### 文法リファレンスの例文を評価データからずらした

初稿は、45件の `true` のうち**30件の学習者文をそのまま例文に使っていた。**
これだと第3週の `retrieval_hit_rate` が**文字列の一致で当たる**——高い数字が出たうえで、
実際の学習者の発話には移らない。llm-jp-eval への批判記事が挙げた罠と同じ形である。
規則と ✗/✓ の対比は残したまま、例文を別の文に差し替えた。

### 監査で見つかった、リファレンス側の誤り

**規則を厚く書くと、規則そのものが嘘になる。** 実際に出たもの：

- **「スーパーで買い物します」に 「を」 が要る、と書いていた。** これは正しい文であり、
  そのまま RAG の根拠になれば**正しい文を直しに行かせる**（`over_correction_rate` が悪化する）
- **「です は名詞にしか付かない」と書いていた。** 「寒いです」があるので嘘で、同じ8本の中の
  grammar-004 と矛盾していた
- **「私を助けて、ありがとう」の誤りは 「私を」 だ、と書いていた。** 「私を助けてくれてありがとう」は
  自然な文で、欠けているのは 「くれて」 のほう。**理由が間違っていると §6 では「誤り」に採点される**
- **助詞の省略を「です・ます と混ぜると不自然」と一般化していた。** 「コーヒー、飲みますか」は普通なので、
  `が`（あります・要ります・必要です の前）に限定した
- **「隣人さん」を「日本語は関係で人を呼ばない」と説明していた。** 部長・先生・お客さん があるので嘘。
  eval-052 の理由（「部長、」は問題ない）と正面から矛盾していた

### 「このままでよい」の根拠を足した（2周目で塞いだ）

1周目の8本は**ほぼすべてが「これは誤り」の説明**で、**「この文はこのままでよい」の根拠になる
記述が1つも無かった。** `over_correction_rate` を15%以下に抑える設計なのに、
**根拠の側が直す方向にしか効かない。** 30件の `false` はそこを測るためだけに存在するのに、
検索して引ける裏付けが無ければ、モデルは黙る判断を根拠づけられない。

2周目で6箇所足した。「あなたはこの本を持っていますか。」は正しい文である（grammar-003）／
「待ちます」と「待っています」はどちらも誤りではない（grammar-006）／
段階Aで普通形は誤りではない（grammar-008）／「を」の省略は丁寧な話し方でも自然（grammar-002）／
「掃除します」も「掃除をします」も普通（grammar-005）／「少し考えます」は上司にもこのままでよい
（grammar-007）。

### まだ埋まっていない穴（第2週以降）

- **例文の流用が完全には消えていない。** 90件の `true` のうち、学習者の文が11件・
  正解の直しが14件、**文字列としてリファレンスに残っている**。残っているのはほぼ定型句
  （おはようございます／はじめまして／お願いします／お先に失礼します）で、
  **定型句を載せない定型句の説明は書けない**ため、ここは許容した。
  ただし **`retrieval_hit_rate` を測るときは、この17件を別に集計する**——
  そうしないと「文字列が一致しただけ」と「文法で当てた」が混ざる
- **1話題に複数本が当たっているのは「を／が」「ています」「丁寧さ」「定型句」の4系統。**
  残りは実質1話題1本で、相互参照はポインタであって2本目の説明ではない。
  第3週に Chroma へ載せるときに再点検する

---

## 完成した120件を監査して分かったこと（2026-08-09・Day 7）

**統合が終わってから、120件全体にもう一度独立監査を当てた**（言語の正しさ／測定手段としての妥当性の2体）。
Day 5 の監査2周は**追加60件にしか当たっておらず**、その事実自体が最大の指摘になった。
**「完成したので点検を終える」ではなく、完成した形に対してもう一度当てる。**

### 最大の指摘は、データではなく実行記録だった

[`evals/runs/20260806-0724-baseline-test.json`](../../evals/runs/20260806-0724-baseline-test.json) が
**`test` 20件の `learner_sentence` と `expected` と `predicted` を全部持っていた。**
`expected != predicted` を引くだけで、**外した4件が名指しで出る。**
design.md は「どの4件がどう外れたかはここに書かない」と書いた3行下でこのファイルにリンクしている。

**監査エージェント自身がその4件を読んでしまった**ので、穴は理屈ではなく実物である。

- **漏れているのは「文」ではなく「結果」。** 評価データは公開物で、`split` も全件に書いてある。
  §7 が守っているのは**調整が結果を見ないこと**であって、文を世間から隠すことではない
- したがって伏せるのは `expected` / `predicted` / システムの直しと理由の4つ。
  **書式の列（`format_compliant` ほか）と集計値は残す**——答えの形の話であって、当たり外れの話ではない
- [`evals/score.py`](../../evals/score.py) に `results_redacted` を足し、
  `split == "test"` のときだけ per-item の判定を落とす。pytest 2件で固定した
- **既存の記録も同じ規則で伏せ字化した。集計値は再計算していない**（採点規則は変えていないため）。
  **伏せる前の版はコミット `a27ad4a` に残っている。** 公開済みの履歴を書き換えるほうが害が大きいので、
  消さずに `redaction_note` へ明記した。**隠したことを隠さない**

### データ自身が矛盾していた3件

**同じ場面の中で、ある件の「正解の直し」が別の件の「誤り」になっていた。**

| | 内容 |
|---|---|
| eval-044 / eval-109 | eval-044 は「私、ここで待つ。」を**「待ちます」に直す**のが正解。eval-109（8/8 のネイティブ判定）は**「私は待ちます。」を誤り**として「待っています」に直す |
| eval-059 / eval-107 | eval-059 は「今日、残業できます。」に **「は」が要る**とする。eval-107 の理由は **「到着、」のあとの「は」の省略は問題ではない**と明記 |
| eval-012 | 「寒いですね、今日。」を `true` としていたが、**後置された句は助詞を落とすほうが普通**（「食べた？昼ごはん。」）。理由が日本語に無い規則を述べていた |

**ネイティブ判定は動かさない。** eval-044 の学習者の文を「私、ここで待っている。」に変え、
**誤りを、理由が名指ししている1つ（普通形）だけにした。**
eval-059 と eval-012 は文ごと差し替えた。

### 重複は5組あり、うち2組は句読点しか違わなかった

`corrected_sentence` の**完全一致3組**（`おはようございます。` `ありがとうございます。` `手伝ってくれてありがとう。`）は
**すべて day-3 バッチの内側**にあった。Day 5 の監査は eval-061 以降しか見ていない。

さらに**包含関係を機械で当てたら2組が出た**——「どちらから来ましたか**。**」と「どちらから来ましたか**？**」、
「感謝しています。」と「本当に感謝しています。」。**文字列の等号では見えない重複がある。**

**片方が `test` の組は、必ず `dev` の側を差し替えた**（eval-013・eval-086）。
`test` は 8/10 の正式測定まで1件も触らない。

### 差し替えで、測っていなかった型が6つ埋まった

Day 5 に `--focus` で名指しして頼んでも入らなかった型が、**差し替え先を自分で書いたら入った。**
（生成は2周目でも試したが、返ってきた18件のうち使えたのは1件で、残りは既存の型の焼き直しだった。
**モデルは「慣れた誤り」に戻る**という Day 5 の観察がそのまま再現した。）

| 型 | 8/8 まで | いま |
|---|---|---|
| 助数詞 | 0件 | 1件（eval-095「コーヒーを二枚」→「二杯」） |
| 条件（〜たら） | 0件 | 1件（eval-012「降るたら」→「降ったら」） |
| 複文の接続 | 0件 | 2件（eval-012・eval-029「忙しいから」→「のに」） |
| 数量・時間の語法 | 0件 | 1件（eval-013「二年間前」→「二年前」） |
| 受身 | 0件 | 1件（eval-059「部長に呼びました」→「呼ばれました」） |
| 使役 | 0件 | 1件（eval-048「お待たせさせて」→「お待たせして」） |

**受身と使役は、1周目の集計では「埋まった」ことになっていた。** 2周目に形態素で数え直したら
**120件すべてで0件**で、1周目が拾っていたのは 「〜てくれて」「見せて」 のようなテ形の誤検出だった。
**型の集計は、正規表現で数えたら必ず中身を見る。**

**ただし `test` 側の型分布は変わっていない**（差し替えは全件 `dev`）。
`detection_accuracy` を `test` 27件で読むと、助詞3件・直訳5件で、**6つの型は1件（＝3.7pt）でしか見ていない。**
**8/10 の数字は「助詞と直訳を検出できるか」の数字であって、助数詞・条件・複文・受身・使役については何も言っていない。**
README に載せるときは、この但し書きを数字の隣に置く。

### 2周目で分かったこと ——「対処が新しい誤りを作る」

**1周目の指摘に答えた12件のうち、3件で対処そのものが誤りを持ち込んでいた。**

- **eval-044 の理由で、待つ側と待たせる側を逆に書いた。** `SCENE_BRIEFS["delay_notice"]` は
  「**学習者が遅れていて、同僚が待っている**」。差し替え前の理由は正しかったのに、書き直して壊した。
  **さらに、この場面の件なのに学習者の文が「私、ここで待っている。」だった**——役割が場面と逆で、
  これは差し替え前から入っていた誤り
- **eval-013 の理由が過剰一般化だった。**「過去の一点は 二年前 で、間 は付かない」と書いたが、
  **「一週間前」「一時間前」は標準**（週間・時間 は助数詞そのものに 間 が入っている）。
  **規則を広く書くと、規則のほうが嘘になる**——Day 6 に文法リファレンスで踏んだのと同じ穴
- **eval-095 の差し替え先が、2つ隣の件と同じ構文だった。**「カード、使える？」→「使えますか。」は
  eval-100「このリンゴ、甘い？」・eval-042「トイレはどこ？」と同型で、同じ場面に3件並んだ

**重複を消す作業が、別の重複を作る。** 差し替えは1件ずつ見ても足りず、**入れたあとに全体でもう一度数える。**

### 伏せ字化は1回目では破られていた

**最も効いた指摘。** `test` の実行記録から `expected` と `predicted` を落としたが、
**`japanese_left_unquoted` を残していた。**

- この列は**理由が返ったときにしか立たない**。理由が返るのは**訂正が返ったとき**だけ。
  つまり `japanese_left_unquoted: true` は **`predicted: true` の別名**である
- 評価データは `needs_correction` を全件公開している。突き合わせると、
  **`false` の7件のうちこの列が立っている4件＝外した4件**が確定する。
  `over_correction_rate` が 4/7 なので、**曖昧さも残らない**
- 実際に再現した。**復元できた4件は、伏せる前の記録の4件と完全に一致した**

**「答えの形の列」と「当たり外れの列」を分けたつもりだったが、
答えが返ったときにしか存在しない列は、当たり外れの列である。**
`format_problems` の `reason_not_english` も同じ経路で、いまは全件空なので気づけなかった。

**対処：`test` は per-item の行を1つも書かない。** 集計値だけを残す。
第2週の誤り分析は `dev` の記録で行うので、失うものは無い。

**テストも書き直した。** 「このフィールド名が無いこと」を確かめるテストは、
**綴りの検査であって性質の検査ではない**（現に、漏洩したままそのテストは通っていた）。
**全問正解の実行と全問不正解の実行を作り、per-item の行が一致することを確かめる**形に変えた。

### 誤差の但し書きは、直した先も間違っていた

1周目の対処として「**120件全体の値を主に書く**」と §5 に書いたが、これも誤り。
**120件のうち80件は `dev` ＝ §7 が「調整に使ってよい」と決めた側**であり、
第2週にプロンプトを合わせれば**合わせた分だけ数字が上がる。**
**分母が薄いことへの対処は、分母を混ぜることではない。**
公開するのは `test` の値（`n=13`、±19〜27pt）で、幅もそのまま書く。

**同じ誤りが README にも残っていた。** ドキュメントは ja / en の2面だと思っていたが、
**README は3面目**で、しかも**面接官が最初に読む面**である。§5 を直したら README も見る。

### 誤差の但し書きが、そもそも当たっていなかった

glossary §5 は「評価用40件では誤差はおよそ ±8ポイント」と書いていたが、
**40件を分母にする指標は1つも無い。** 2つの指標は別のラベルの上で測るので分母は割れる。

| 指標 | `test` の分母 | 1件 | 目安 |
|---|---|---|---|
| `detection_accuracy` | 27 | 3.7pt | ±13pt |
| `over_correction_rate` | **13** | **7.7pt** | **±19〜27pt** |

**`over_correction_rate` の目標15%は、実質「2件まで」という意味だった。**
§5 を ja → en の順で直した。

> **この節を最初に書いたとき「120件全体の値を主に、`test` の値を併記する」と結んでいた**（2026-08-09 に訂正）。
> **すぐ上の節（「誤差の但し書きは、直した先も間違っていた」）で否定した当の方針**を、
> 数ページ下でもう一度結論として書いていた。**確定している方針は「公開するのは `test` の値」**である。
> **同じ日に2回書いた文章は、後ろのほうが古いことがある**——直した順と、文書に並んでいる順は一致しない。

### 残した指摘（対処せず、理由を書いて残す）

- **eval-090（`test`）の直し「助かりました、ありがとう。」が丁寧さの混在**という指摘 —— `test` なので触らない。
  8/10 の正式測定のあとに判断する
- **`data/grammar/` との近似一致が約30件**（「弟は20歳を持っています」↔ eval-018「私は25歳を持っています」など、
  1語だけ入れ替えたもの）。design.md の既知17件は**完全一致だけを数えていた。**
  第3週に `retrieval_hit_rate` を測るとき、**別集計の対象を完全一致から類似度へ広げる**
- **丁寧さの下限が訂正エンジンにもベースラインにも渡っていない**（`politeness_floor()` は採点キットからしか呼ばれない）。
  **いまは両実装に対して対称なので 8/10 の比較表は成立する。**
  第2週にこれをプロンプトへ足すのは1行だが、それだけで `detection_accuracy` は最大20pt 動きうる。
  **足すなら、それ自体を独立した前後比較として測る**——RAG や検証ノードの効果に混ぜない
- **全120件が単一の短い節**（平均10.3文字、2文からなる件は2件）。
  第3週にテスターから集まるのは自由会話の発話なので、**複数の誤りを含む文にどう振る舞うかは1件も測っていない。**
  スキーマ（2値ラベル＋直し1本）自体が複数誤りを表現できない。9月の250件で入れる

### 手で書いた10件は、生成 → 検証の鎖から外れている

差し替え先の文は**生成では埋まらなかったので手で書いた**。結果として、
**「生成した候補をネイティブが検証して item にする」という鎖が、この10件だけ切れている。**
正しさの問題ではなく**出所（provenance）の問題**で、リポジトリだけを読んだ人には
「このデータはどう作られたか」が均一に答えられなくなる。

[batch3-hand-authored-2026-08-09.json](../../data/evaluation/candidates/batch3-hand-authored-2026-08-09.json)
に **`verified: false`** で記録した。**未検証であることを散文ではなく機械に見える形で残す。**

### 12件のネイティブ再検証 —— 覆りゼロ（2026-08-09）

**eval-001, 005, 012, 013, 029, 044, 048, 059, 085, 086, 091, 095 の12件とも、書いたとおりで可。**
ラベル・直し・理由のいずれも覆らなかった。用紙は [day7-native-recheck.md](day7-native-recheck.md)（0.5h）で、
**差し替えの理由（`native_check`）を伏せて判断した**——先に読むと「そう言われればそう見える」になる。
[batch3-hand-authored-2026-08-09.json](../../data/evaluation/candidates/batch3-hand-authored-2026-08-09.json)
を `verified: true` に更新した。**これで120件すべてがネイティブ検証済みになった。**

**覆り率は回を追って下がっている。**

| | 対象 | 覆り |
|---|---|---|
| Day 3（8/5） | 生成60件 | 8 / 60（13%） |
| Day 5（8/7・v1） | 生成60件 | 14 / 60（23%） |
| Day 5（8/7・v2 後） | 事前検品60件 | 3 / 60（5%） |
| Day 7（8/9） | 監査後に書き換えた12件 | **0 / 12** |

**0件を「もう間違いが無い」と読まない。** この12件は**独立監査2周を通った直後**の文であり、
**書いた本人が同じ日に読んでいる。** 覆りが出るとすれば、**第2週の誤り分析（dev 80件）で
訂正エンジンの出力と並べたとき**——自分の書いた正解を、他人の出力の隣で読むのが次の機会になる。

### 採点キットの20件のうち4件が、書き換えた12件と重なった

kit_no **1（eval-001）・2（eval-013）・11（eval-044）・19（eval-005）**。
うち3件は**学習者の文ごと差し替わっている。**

**キットは作り直さない。** `rater_agreement` は「**2人が §6 の尺度を同じに読んでいるか**」の数字で、
**2人が同じ紙を見ていれば成立する。** キットの JSON は学習者の文も直しも自分の中に持っており、
items.json を参照していないので、**キット単体では矛盾していない。**

**危ないのは、あとから `id` で items.json と突き合わせる作業のほうである。**
kit_no 1・2・11・19 は **2026-08-08 時点のデータセット**を指す。突き合わせるときは
キットの `learner_sentence` を正とすること。**作り直せば、印刷済みの紙と第二採点者の手元が食い違う。**

#### この判断の前提が当日に消えた（2026-08-09 追記）

**「作り直さない」の根拠は「印刷済みの紙が相手の手元にある」だった。その紙は渡せなかった。**
会場でその場の採点まで行かず、配布は**後日 WhatsApp** に変わったので、
**食い違う相手の手元が存在しない。** 自分の採点（`...-jun.json`）も**全件 `null` のまま**なので、
いま作り直しても**捨てる作業がゼロ**である。

判断そのものは前提の下では正しかった。**変わったのは前提のほうで、判断を撤回する理由になる。**

**作り直した場合に得られるもの**：kit_no 1・2・11・19 に付く
「これは 2026-08-08 時点のデータを指す」という但し書きが**恒久的に消える**。
`rater_agreement` は尺度の一致を見る数字なので**どちらでも成立する**が、
**第4週の `correction_validity` は `id` で items.json をたどる**ので、
存在しない3件を指したキットが残ると、そのときに同じ確認をやり直すことになる。

**作り直す場合のコスト**：差し替わった3件にベースラインを走らせ直す（`dev` のみ。API 1回ぶん）。
**`test` は触らないので 8/10 の正式測定には一切影響しない。**

### 採点をしたら、データセットのラベルが1件覆った（2026-08-09・夕）

**キットの20件を採点する過程で、`true` の1件が「元の文で合っている」と判定された。**
採点は訂正エンジンの出力を見る作業だが、**そこに刷られている学習者の文も同時に読むことになる**ので、
**評価データの検証としても効く**——これは設計時に想定していなかった副産物である。

- **eval-052「部長、話したいことあります。」→ `false` に倒した。**
  話し言葉では **`が` は落ちる。** 直した文「話したいことがあります」も自然なので、
  **この件は捨てずに `false` として残した**——**ベースラインが実際に直しに行った件**であり、
  「直したくなる自然な文」は `over_correction_rate` を測れる唯一の材料で、いちばん足りていない
- **`に` の省略は誤りのまま**（eval-002「毎朝六時起きます」／eval-046「何時来ますか」を確認）。
  **`が` だけが例外**という線が引けた。eval-054「この件、確認必要です。」（`test`）は
  **「確認が必要です」が要る**という判定で、**現状のまま。`test` は1件も触っていない**
- 内訳の帳尻は **eval-118「資料はコピーしました。」を `true` に差し替えて**取った
  （同じ場面の平叙文の `false` が3件あり、いちばん誘惑が薄かった）。
  差し替え先は**生成した候補から採った**ので、出所の鎖は切れていない

### 尺度に「訂正が必要だった」以外の箱が無かった

**採点20件のうち3件（kit_no 4・6・17）が `insufficient` になり、3件とも §6 に箱が無かった。**
§6 は「訂正が必要だった」ことを前提に書かれている。放置すると
**`insufficient` と `wrong` に採点者ごとに割れる**——直した文じたいは自然なので「妥当」に見え、
触らなくてよいところを触っているので「誤り」にも見える。**割れた原因が尺度にあると、一致率は何も語らない。**

**当初は3件を1つの型として書いたが、これは誤りだった**（2026-08-09・監査で判明）。**型は2つある。**

| kit_no | id | `needs_correction` | 型 |
|---|---|---|---|
| 4 | eval-034 | `false` | ①そもそも直す必要がなかった |
| 6 | eval-052 | `false` | ①同上 |
| 17 | eval-046 | **`true`** | **②直す必要はあったが、AI がそれ以上に変えた** |

**kit_no 17 は「元の文で合っていた」件ではない。** `何時来ますか。` は 「に」 が抜けており実際に誤りで、
**必要な直しは 「に」 の補いだけ**だった。AI はそこに `来ます → 行きます` を足している。
**①の規約では 17 を救えない**——①は「元の文が正しかったとき」にしか掛からない。

**放置していたら、ここは高い確率で割れた。** 印刷された `wrong` の定義は
「直した文が不自然、**または意味が変わっている**」で、`来る → 行く` は**視点が入れ替わっている**。
**尺度どおりに読んだ第二採点者は `wrong` を付ける。** 自分は `insufficient` を付けて封をしてある。
**1件割れれば5ポイント**で、しかもそれは判断の違いではなく**尺度の穴**である。

**箱を増やさず、規約を2つに増やして揃えた**（4段階にすると印刷済みの様式と食い違う）。
②は「**必要な直しが正しく入っているなら `insufficient`。加えた変更で不自然になった場合だけ `wrong`**」。
§6（ja / en）に書き戻し、**同じ文言を採点キットにも刷った**（`scale_note()`）。
**キットはまだ送っていないので、送る前に揃えられた。**

> **②は `over_correction_rate` では拾えない。** あの指標が数えるのは
> `needs_correction: false` の件を直しに行ったケースだけで、②は元の文が実際に誤っている。
> §6 の但し書きに「過剰訂正は別の指標が見ている」と書いていたが、**それは①にしか当たらない。**

> **一致率は「尺度を直したあとの尺度」で測ることになる。** 割れてから直すのが tasklist の当初の手順で、
> 今回は**割れる前に穴が見えた**ので先に直した。**先に直したこと自体を記録しておく**——
> そうしないと、あとで高い一致率を「尺度がよくできていた」と読んでしまう。

### 下限規則が、自分の正解と矛盾していた（「はじめまして」）

キットを刷り直したときに、**段階Bの文言も 8/8 の改訂ぶんで新しくなった**（印刷済みの紙は古い文言だった）。
新旧どちらで送るかを決めるために、**その文言に依存する採点が何件あるかを数えた**——
`self_introduction` の4件はいずれも助詞・語法・定型句の誤りで、**丁寧さでは割れない**。
**0件なので、この2択は結果が変わらない。**（判断の型：**印刷物と基準がずれたら、
刷り直すか戻すかで悩む前に、ずれた箇所に依存する採点を数える。**）

**数える途中で本当の穴が出た。**「はじめまして」は です・ます・ください のどれでも終わらず、
言いさしでもないので、**下限規則を字義どおり読むと「直すべき文」になる。**
**そしてそれは eval-017 の直した文そのものだった**——監査で何度も出た「規則が自分の正解と矛盾する」型。

**例外は必要な幅だけ広げた。** 段階B・C の全120件を数えたところ、丁寧形で終わらない文は
**「はじめまして」と「お名前は」の2件だけ**で、**段階Cには1件も無い。**
そこで **「その語に丁寧形が存在しない場合に限る」** と条件を付けた（「ごめん」には「すみません」があるので通らない）。
§2（ja / en）と `POLITENESS_FLOORS` に書き戻し、キットを刷り直した。

### 採点キットが、採点対象の答えを刷っていた（2026-08-09・監査3周目）

**段階Bの丁寧さの文言が、そのまま評価データの答えだった。**
8/8〜8/9 に `POLITENESS_FLOORS` の段階Bへ**判定の実例**を書き足していた——
「`お` だけでは足りない（`お仕事、何？` は要訂正）」「`お名前は。` のように言い差すならよい」
「`はじめまして` のような定型句はよい」。**この3つはどれも評価データの項目そのものである。**

| 文言の中の例 | 実体 | 何が起きるか |
|---|---|---|
| `お仕事、何？` | **eval-081（`test`・`true`）** | **`test` の文と正解が、外へ送るファイルに載っていた** |
| `はじめまして` | eval-017（`dev`・`true`）の**正解** | **その eval-017 はキットの kit_no 20。自分の答えが4行上に刷ってある** |
| `お名前は。` | eval-022（`dev`・`true`）の**直した文** | **正解そのものが判定つきで露出** |

**採点キットは紙とチャットの両方でこの行を刷っていた**（`kit_markdown` と `whatsapp_message`）。
kit_no 20 は `はじめました。 → はじめまして。` を採点させる件で、**その直上に「はじめまして は正しい」と
断定して書いてある。** これは §5 の「**相手にはラベルも正解の直しも見せない**」の正面からの違反で、
`rater_agreement` は「尺度が同じに読めるか」ではなく「**先に見せた答えを写せるか**」の数字になる。

**さらに、この文言はベースラインには渡っていない。** `correction/baseline.py` が使うのは
`scene_brief` だけで、`politeness_floor` は**データを作る側と採点キットしか読んでいない**。
つまりキットは「**AI が受け取っていない、データのラベリング規則**」を採点者に見せていた。
「**モデルに渡した文言そのものを刷る**」（8/8 の決定）を満たしてすらいなかった。

**対処：規則を2つに割った。**

- `politeness_floor()` ＝ **誰が聞いているか・どこに線があるか**だけ。項目を1つも引用しない。**人に見せてよい**
- `politeness_rule()` ＝ 上に**判定の実例**を足したもの。`generate_items.py` だけが使う。**人には見せない**

**pytest で閉じた**（`test_no_form_quotes_an_evaluation_item_in_its_own_scaffolding`）。
**項目の中身をダミーに差し替えて両方の様式を描き、`items.json` の文が1つでも残ったら落とす。**
残ったものは「採点対象の項目」からではなく「**様式そのもの**」から来たことになる。
規則の文言をあとから足しても、同じ穴が開いた瞬間に落ちる。

> **この穴は 8/8 の監査を通り抜けている。** 当時の文言には実例がまだ入っておらず、
> **8/9 に丁寧さの規則を精密にした作業が、そのまま漏洩を作った。**
> **規則を厳しくする作業と、規則を人に見せる作業が、同じ文字列を共有していたのが原因**である。

### 監査2周目 —— 1周目の対処が残した穴（2026-08-09）

**キットの漏洩を塞いだ1周目の対処そのものに、3つ穴が残っていた。**

- **穴を塞いだはずの pytest が、穴を作った当の語では落ちなかった。**
  `はじめまして`（規則の文中）と `はじめまして。`（eval-017 の正解）は**句点1つ違い**で、
  完全一致で照合していたテストは素通りさせた。**規則は文を「引用」せず「言及」する**ので、
  句読点は落ちるほうが普通である。**照合の前に `。、？！「」` を落とす**ようにした。
  `reason_en` も禁止対象に足した（1周目は学習者文と直した文しか見ていなかった）
- **`refuse_if_graded()` は関数だけがテストされ、事故の経路がテストされていなかった。**
  採点を消したのは `--run` であって関数ではない。**`main()` から `build_kit()` を切り出し**、
  「採点済みの様式があると1ファイルも書かずに止まる」を経路ごと縛った。
  実際に守衛の2行を消すと落ちることを確認した
- **`politeness_rule()` を使っているのが `generate_items.py` だけなのに、それを縛るものが無かった。**
  1文字（`politeness_rule` → `politeness_floor`）戻すだけで**生成プロンプトから判定の実例が消え、
  誰も気づかない**。候補の質は静かに落ち、落ちたことは次のネイティブ検証まで分からない。
  `scene_prompt()` を切り出してプロンプト本文を assert できるようにした

**さらに、1周目の対処が新しい誤りを2つ書き込んでいた。**

- **`お名前は。` を「eval-090（`dev`・`false`）の学習者文」と書いたが、全部違った。**
  実体は **eval-022（`dev`・`true`）の直した文**で、eval-090 は `test`・`true` の別の文である。
  **漏洩を記録する表で、id・分割・ラベル・フィールドの4つとも取り違えた**
- **§6（ja / en）に `kit_no 4・6・17` と eval-046 を書いた。** これは
  **一致率を測る20件の番号と、自分がそこに付けた評価**であり、**公開リポジトリに置けば
  第二採点者が読んだ時点で一致する**。§5 が「相手にはラベルも正解も見せない」と書いている当のものを、
  それを守るための説明の中で公開していた。**件数だけ残して落とした**（`rater_kit.py` のコメントも同じ）

> **「漏洩を塞ぐ作業」が、漏洩を2つ作った。** 2周目の教訓は 8/8 と同じ形である——
> **対処は新しい誤りを作る。** 違うのは、今回それが**対処を説明する文章のほうに出た**ことで、
> コードは pytest が止めたが、**文章を止めるものは監査しかない。**

### `test` を見て見せ方を決める手順が残っていた（2周目）

**§3-2 の「見せる2文」の2番目が `早いですね。`＝ eval-011（`test`）だった。**
しかも「**直されてしまったら別の `false` 件に差し替える**」と書いてあり、
これは **`test` の1件についてシステムの判定を見てから行動を変える**手順である。
**8/10 の正式測定の前日**に、規模が小さいだけで§7 の禁じている自己採点そのものになる。

**同じ場面（Greeting）の `dev` の `false` 件 `今日は暑いね。`（eval-071）に差し替えた。**
予備の2件はどちらも `dev` だったので、**取り違えであって方針の誤りではない。**

### 同じ `id` の中身を差し替えたので、8/8 の `dev` 実行記録が古くなった（2026-08-09）

**8/9 に items.json を14件書き換えたが、`id` は据え置いた。** その結果、
[20260808-2041-baseline-dev.json](../../evals/runs/20260808-2041-baseline-dev.json) の
**40行中8行が、いまその `id` を持つ項目を記述していない**（eval-005・012・013・029・044・048・059 は
学習者の文ごと差し替え、eval-052 は `true → false` の反転）。

**数字も動く。** `over_correction_rate` は記録では **0.625（5/8）**だが、
現在のラベルで数え直すと **0.667（6/9）**になる（`detection_accuracy` は 1.0 のまま）。

**記録の数字は書き換えない。** 8/8 のデータセットに対しては正しい値であり、
書き換えると「あとから辻褄を合わせた記録」になる。**代わりに `dataset_note` を1行足した。**

> **`scorer_version` と同じ問題が、採点規則ではなくデータの側で起きた。**
> §7 が `scorer_version` を要求したのは「規則が変わって動いた数字を、モデルが変わったと読ませない」ためで、
> **データセットが変わって動いた数字にも同じ理屈が要る。**
> **第2週の誤り分析はこの記録を読む**ので、行を1つ引くたびに items.json と突き合わせるか、
> `dev` を測り直すこと。
>
> **`tests/test_evaluation_items.py` が固定しているのは `split` だけ**で、
> `learner_sentence` と `needs_correction` は固定していない。`select()` も `id` が `dev` かしか見ない。
> **「同じ id で中身が入れ替わる」を機械で捕まえるものは、いまのところ無い。**
> 第2週に実行記録へ items.json の内容ハッシュを刻むところから始める。

### 第3週の前に閉じるもの（2周目で出た）

**`data/grammar/` が `test` の項目を正解ごと引用している。** grammar-008 が7件
（eval-011・eval-072・eval-060（「このままでよい」の根拠として 8/8 に足したもの）、
eval-081・eval-117・eval-057・eval-007）。**8/10 には影響しない**（今週は Chroma に載せず、
ベースラインはリファレンスを見ない）が、**第3週に載せた瞬間に `retrieval_hit_rate` が
文字列の一致で当たり、第4週の `test` 再測定も汚れる。**

**grammar-008 だけではない**（2026-08-09・確認パスで判明）。**grammar-004 が eval-040 の直した文
`お会計、お願いします。`、grammar-007 が eval-060 の学習者文 `少し考えます。` を引用している。**
**8本中3本**である。

> **したがって第3週の pytest は grammar-008 を見るのではなく、`data/grammar/` 全体 × `test` 40件を走査する。**
> 1本だけを想定して書くと、**残り2本を通したまま緑になる**。
> 照合は**句読点を落として**行う——8/9 に、完全一致の照合が
> `はじめまして` と `はじめまして。` を別物として通した実例がある（「監査2周目」の節）。

---

## 「降ろす順番」の現在地（2026-08-09・週の終わり）

**第1週で降ろしたものは無い**（PLAN.md §3 の 0〜6 は1つも発動していない）。**8/16 の点検はここから始める。**

ただし現在地は「全部そのまま残っている」ではない。**0 と 3 は目減りしている。**

| # | 降ろす候補 | 現在地 |
|---|---|---|
| 0 | 音声合成（2h）・会話セッション（1h）・見た目（1h）・訂正結果の保存（0.5h） | **4.5h のうち 1h は済んでいる。** 会話セッション（開始／終了／振り返り）は [app/main.py](../../app/main.py) にあり実機確認済み。**降ろし代は残り 3.5h** |
| 1 | 音声認識（5h） | 未着手（第3週）。8/3 に iPhone Safari で録音が動くことは確認済みなので、**降ろす理由は工数だけ** |
| 2 | 文法リファレンスを10本→8本 | **まだ生きている。** Day 6 が8本で着地したのは第1週の予定どおりで、降ろした結果ではない。**PLAN.md は第2週の土日（8/15・8/16）に「リファレンス10本へ」を置いており、降ろせば 2.5h の一部が浮く** |
| 3 | 評価データを120件→90件 | **作る工数は使い切った**（120件で確定・検証済み）。**ただし採点の工数は残っている**——`test` が40→30件になれば、第4週の `correction_validity` の手採点が10件ぶん減る |
| 4 | レベル遵守率と検索の当たり率をやめる | 未着手（第3週に測り始める）。**着手前に降ろせば満額浮く** |
| 5 | LangGraph をやめる | 未着手（第2週の検証ノードから）。**第2週の設計時点が最後の分岐** |
| 6 | テスターを5人→3人 | **確保は済んだが、選択肢は残っている。** 降ろすのは第3〜4週の運用工数（アクセスコード配布・感想の回収と反映）であって、確保そのものではない |

**7つとも1つも消えていない。** 8/16 の点検では全部が選択肢である。
番号の若い順に降ろす規則のままだと**次に手が伸びるのは 0 と 1**で、
**0 の 4.5h のうち音声合成は 2h**、1 は音声認識 5h ——つまり**手が伸びる先はほぼ音声**である。
だが **2・3・4・5 が生きている以上、音声より先に落とせるものはある。**
「上から順」を機械的に適用すると、**まだ工数の浮くリファレンス・採点・未着手の指標2つを残したまま
テスターの食いつきを先に捨てる**ことになる——8/16 に迷わないための順番が、迷わないまま損をさせる形になる。
**8/16 は順番どおりに降ろす前に、2・3・4・5 で足りるかを先に見る。**

**第1週は降ろさずに終えたが、遅れは出ている。** `rater_agreement`・ベースラインの正式測定（test 40）・
`dev` 80件の誤り分析の3つを第2週へ送った。**これらは「降ろした」のではなく「先送りした」**もので、
第2週の枠を先に食っている。**8/16 の点検では、まずこの3つが片付いているかを見る**——
片付いていなければ、**2・3・4・5 を先に発動する**（音声＝0・1 より後ろにあるものは、この4つ）。

---

## 判断が必要になったら

- **実機確認（8/3）で①録音が動かない場合** → 音声を諦めてテキスト方式に切り替える。
  PLAN.md §3「降ろす順番」の1番を第1週の時点で適用する。この判断は先送りしない
- **②自動再生だけ動かない場合** → **テキスト方式には戻さない。**「再生ボタンを押す」方式に落として第3週へ進む。
  iOS Safari の自動再生ブロックは**ブラウザ側の方針**であり、実装で解決できるものではない。
  録音・文字起こし・読み上げは残るため、降ろす対象は「自動再生」だけに限定する
- **確認は必ず HTTPS の URL で行う（誤判定の防止）** — マイク取得（`getUserMedia`）は
  セキュアな文脈でしか動かない。PC で `streamlit run` して iPhone から `http://192.168.x.x:8501` を開くと、
  **コードが正しくても録音は失敗する**。これを①の失敗と誤読すると、不要なテキスト方式への撤退を招く。
  第3週予定の Streamlit Community Cloud へのデプロイを**1つ前倒しして**、その URL で確認する
- **評価データ作成が3時間を大きく超える場合** → 90件に減らし、誤差の但し書きを厚くする
  （降ろす順番の3番）。**分割の比率は変えない**
- **テスター候補の前向きな返事が3人未満** → 第2週の頭に計画を見直す
