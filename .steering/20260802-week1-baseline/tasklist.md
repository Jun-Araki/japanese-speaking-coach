# 第1週 タスクリスト

対応する設計: [design.md](design.md) ／ 期間: **2026-08-02（日）〜 08-09（日）**

**週は月曜始まりで数える（2026-08-04 変更）。** これにより **8/9（日）の Minna Shuugou は第1週に入り**、
第二採点者の確保と `rater_agreement` は第2週へ引き継ぐのではなく**今週の完了条件**になった。
**8/2（日）だけは例外で第1週に含める**（プロジェクトの初日であり、次の週へ送る先がないため）。
第2週以降は月曜始まりのとおり（第2週＝8/10 月〜8/16 日）。

**Day 番号は月曜を Day 1 とする**（2026-08-04 に振り直した）。
**8/2（日）は Day 0** ＝ 週の始まる前に着手した分なので、**工数には数えない**。

### 稼働の前提（今後も守る）

**平日は2時間、休日は8時間。** 金曜も平日なので2時間。**この枠を超える計画を書かない。**
枠に入らない作業は、翌日へ送るか PLAN.md §3「降ろす順番」に従って降ろす。
**枠を広げて辻褄を合わせると、遅れが「見積り違い」ではなく「無理な計画」として積み上がる。**

| 日 | 見積 |
|---|---|
| Day 1（8/3 月） | 2h |
| Day 2（8/4 火） | 2h |
| Day 3（8/5 水） | 2h |
| Day 4（8/6 木） | 2h |
| Day 5（8/7 金） | 2h |
| Day 6（8/8 土） | 8h |
| Day 7（8/9 日） | 8h |
| **合計** | **26h** |

Day 0（8/2 日）は数えない。**Day 7（8/9）だけは朝2h ＋ 会場3h ＋ 帰宅後3h に割れる**
（Minna Shuugou が 10:00 開始で、家を 9:30 に出るため）。

進捗記号: `[ ]` 未着手 ／ `[~]` 着手中 ／ `[x]` 完了 ／ `[-]` 降ろした

---

## Day 0（8/2 日）— 土台と人集め

- [x] 環境セットアップ（**uv ＋ Python 3.12.13**、`.venv`、`ruff` / `mypy` / `pytest`）
- [x] **LLM の疎通確認**（LangChain 経由で Gemini。日本語の応答を確認）
      — キーは `~/.zshenv` の `GEMINI_API_KEY` を**環境変数から読む**（`.env` に複製しない）
- [-] `ANTHROPIC_API_KEY` — **決済フォームが通らず取得できていない。** Claude API は見送り、
      **Gemini で進める**（LangChain 経由なので1行で差し替え可能）。詳細は design.md「プロバイダの選定」
- [x] **Minna Shuugou の開催日を確認** → **毎月第2日曜。直近は 8/9**（対応は末尾の節へ）
- [x] **japanesecompass.com の裏取り完了**（08-02 トップ・料金、08-03 `/practice/speaking/`）
      → **当初の差分①②はどちらも成立せず**（向こうも訂正する・向こうにも音声入力と発音採点がある）。
      差は「音声で自分の言葉を話す」という**組み合わせ**と、**精度を公開していること**。README・PLAN.md・docs に反映済み

> **テスター募集は 8/9 の Minna Shuugou で行う。** 個別の声かけはしない。

## Day 1（8/3 月）— 疎通確認 ← 計画の前提

- [x] **最初の30分**：`st.audio_input` と `st.audio(autoplay=True)` だけの画面を Streamlit Community Cloud へ公開
      — **HTTPS の URL でないとマイクが動かない。** LAN の `http://192.168.x.x:8501` で試すと誤判定する
- [x] **iPhone Safari の実機で ①録音 と ②自動再生 を別々に確認** → **①成功**
- [x] **①が成功したのでテキスト方式へは戻さない。** 音声は予定どおり第3週で載せる
- [x] 押して開始→もう一度押して停止、マイク許可はページを開き直すたび1回 — [design.md](design.md) に記録
- [x] **②自動再生は成功。** A・B は毎回鳴り、同じ音の C だけ鳴らなかった
      → 原因は Safari の制限ではなく**音声の同一性**。**第3週は自動再生で組んでよい**
- [x] 結果と**本番の罠**（同じ返事＝同じ音声は2回目が鳴らない）を [design.md](design.md) に記録
- [x] `app/audio_check.py` を削除
- [x] **Community Cloud へデプロイし直した** → https://nihongo-coach.streamlit.app/
      **起動ファイルは Settings から変更できない**（変えられるのは App URL と Python version だけ）。
      アプリを削除して作り直し、同じサブドメインを取り直した。Python は 3.14
- [x] **本番で会話を確認。** 誤った文（「チェンナイに一宮予定です」）を入れても相手役は直さず、
      返事は全ターン1〜2文、場面からも外れなかった
- [x] Streamlit で会話画面（録音なし・テキスト直結）— [app/main.py](../../app/main.py)
      場面・レベル選択 → 複数ターン会話。**会話中に訂正は出さない**。
      生成失敗は**定型文でごまかさず**エラーと再試行ボタンを出す
- [x] `dialogue.reply()` — 場面とレベルに応じた相手役、**1〜2文に制限**
      プロンプトだけでは3文が3割ほど出たため、**コード側でも文数を切る**（`limit_sentences`）
- [x] `llm.py` — 提供元を選ぶ唯一の場所。Gemini ⇄ Anthropic を1行で差し替え
- [x] pytest 18件（文数の制限、場面・レベルが glossary と一致すること）。`ruff` / `mypy` 通過
- [x] 実機の会話で**訂正が混ざらないこと**を確認（誤った文を3つ入れても直さなかった）

## Day 2（8/4 火）— 訂正

- [x] `correction.check()` を**会話とは別呼び出し**で実装 — [correction/engine.py](../../correction/engine.py)
      モジュール名は `engine`。`check.py` にすると `correction.check` が関数か
      サブモジュールか曖昧になり、実際にテストが壊れた
- [x] 構造化出力の JSON スキーマを固定（`grounding_ids` は空配列で確保）
      **プロバイダの制約デコードは使わず、自前で JSON を解析する。**
      制約デコードにすると `format_compliance_rate` の JSON 側が構造上つねに 100% になり、
      **測っていない数字を測ったことにしてしまう**（design.md「訂正の設計」参照）
- [x] **JSON が壊れて返ったときの処理**（[docs/ja/functional-design.md](../../docs/ja/functional-design.md) エラーハンドリング）
      — 再試行1回、失敗しても `format_compliance_rate` の分母には必ず数える。**捨てて隠さない**
      **再試行は「何が壊れていたか」を添えて投げ直す。** temperature 0 で同じプロンプトを
      投げ直しても同じ壊れた出力が返るだけで、再試行が形だけになる
- [x] **`format_compliance_rate` は1回目の出力で判定する。** 再試行で直っても不適合のまま
      — 再試行が書式の失敗を消せるなら、分母から捨てるのと同じ嘘になる
- [x] **理由が英語かの判定**：「」や引用符の中の日本語は除いてから判定する。
      「「は」 marks the topic」は適合、「これは丁寧ではありません」は不適合
- [x] 訂正結果を `st.session_state` に溜め、**会話中は表示しない**
      訂正の失敗で会話を止めない（[app/main.py](../../app/main.py) `record_correction`）
- [x] 「会話を終える」→ 振り返り表示。画面は1つのまま、状態を1つ増やしただけ
- [x] pytest 41件（18 → 41）。`ruff` / `mypy` 通過
- [x] **実機の会話で確認**（thanks・2ターン）：会話中に訂正は出ず、振り返りに2件とも出た。
      `ありがとう → ありがとうございます` も含め、理由は英語で返った

- [x] **`docs/ja` / `docs/en` の `functional-design.md` を増補**（CLAUDE.md の「厚くする条件」を満たした）
      画面遷移を3状態に訂正／訂正結果の各キーの意味／呼び出し結果（`attempts`・`format_problems`）／
      エラーハンドリングに5行追加。**ja を先に直し、en を同じコミットに含める**

## Day 3（8/5 水）— 評価データ 60件

- [x] 候補を大規模モデルに生成させる（場面ごと、初級者が言いそうな文）
      — [evals/generate_items.py](../../evals/generate_items.py)。**ラベルは付けさせず `intended_label` だけ持たせる。**
      文もラベルも同じモデルに決めさせると、`false` の件が「そのモデルが自然だと思う文」になり、
      **`over_correction_rate` を最も自分に甘い題材で測ることになる**（design.md）
- [x] **全件を自分で検証・修正**（[docs/ja/glossary.md](../../docs/ja/glossary.md) §2 の基準に従う）
      — **60件中8件で判定が覆った。** 直した文の誤りはそれより多く、`corrected_sentence` は
      機械採点に出ないので**検証しなければ永久に気づかない**（§5）
- [x] 迷った件は `false` に倒し、判断に迷った旨をメモに残す
      — [design.md](design.md)「評価データの検証で分かったこと」に記録。
      **「あなたは不自然」を理由にした `true` は成立しなかった**ので、glossary §2 に禁止として書き戻した
- [x] **丁寧さの下限を確定し glossary §2 に書き戻した**（当初のタスクには無い。検証中に必要になった）
      — 候補の段階で**同じくだけ方が `true` と `false` に分かれていた**。放置すると2つの主要指標が
      「どちらに倒したか」の数字になる。**場面ではなく相手で決まる3段階**に確定。ja → en を同じコミットに
- [x] **dev 40 / test 20 に分割**（場面ごとに 2:1）
      — **この割り当ては最終形（dev 80 / test 40）の一部であり、Day 6 では動かさない。**
        Day 6 は**追加60件だけを割って足す**。ここで振った `split` を後から振り直すと、
        Day 4 で測ったベースラインの test 件が dev に流れ、**「評価用は最初と最後にしか触らない」が
        気づかないうちに壊れる**（[glossary](../../docs/ja/glossary.md) §7）

## Day 4（8/6 木）— ベースラインの配管を通す（`n=20` の暫定値）

- [x] 採点スクリプトの骨組み（`needs_correction` の一致のみ機械採点）— [evals/score.py](../../evals/score.py)
      **判定不能（JSON が2回とも壊れた）件の扱いを決めた**：`detection_accuracy` では
      「検出できなかった」に数え、`over_correction_rate` では「余計に直していない」に数える。
      後者は自分に甘い側なので、**`unusable_verdicts` を実行記録と画面に必ず並べて出す**
- [x] `format_compliance_rate` を精度と**別に**記録
- [x] 採点スクリプトの pytest（41 → 60件）
- [x] `correction.baseline_check()`（1回呼び出し、作り込まない）— [correction/baseline.py](../../correction/baseline.py)
      **場面とレベルは本実装と同じものを渡す**（design.md 末尾）。出力の JSON も同じ形にして
      採点経路を1本に保つ
- [x] **この時点の評価用20件で測定 → 数字が1つ出る**
      — `detection_accuracy` **100%**（n=13）／`over_correction_rate` **57.1%**（n=7）／
        `format_compliance_rate` **100%**（n=20）。**素朴版は自然な文の半分以上を直しに行った**
- [x] 実行記録を `evals/runs/` に保存。**`n` と `split` を必ず書く**
      — [20260806-0724-baseline-test.json](../../evals/runs/20260806-0724-baseline-test.json)。
        **`scorer_version` を追加した**（Day 4 に採点規則を1つ変えたため。glossary §7 に書き戻し済み）
- [x] **評価用から5件を手で照合** — 5件とも採点結果とラベルが一致。
      照合対象は実行記録の `manual_check_ids` に残す（等間隔で採るので再現できる）
- [x] **英語判定の規則を割合方式に変えた**（当初のタスクには無い。測定して初めて分かった）
      — 「日本語が1文字でもあれば不適合」だと20件中9件が不適合になり、**9件すべてが英語の説明文**だった。
        `config/thresholds.toml` に閾値を出し、glossary §5・§7 を ja → en の順で更新

> **ここで出るのは `n=20` の暫定値。** 評価用が最終形（40件）になるのは Day 6 なので、
> **README に載せる数字はこれではない**（正式値は第2週初日に test 40 で測る。末尾の節）。
> 誤差の目安は n=20 だと ±11pt 程度あり、n=40 の ±8pt より広い。
> **暫定と正式を実行記録の `n` で区別できるようにしておく**——区別できないと、
> あとで「どっちの数字を README に書いたのか」が分からなくなる。

## Day 5（8/7 金）— 評価データ120件の中身を確定する

**金曜も平日なので2時間。** 分割・機械点検・コミットは土曜へ送った（合計2h）。
**8/9 に間に合えばよい**ので、8/7 に必要なのは「**120件の中身が確定していること**」まで。
文法リファレンスも土曜へ回した（8/9 に要るのは評価データであって文法リファレンスではない）。

- [x] 追加60件の候補生成（0.5h）— **既存60件を渡して重複を避ける**
      （[generate_items.py](../../evals/generate_items.py) が items.json と過去の候補ファイルを読んで自動で渡す）。
      **内訳は Day 3 と同じ 45（`true`）／15（`false`）**。Day 3 が 45:15 ちょうどに着地したので、
      同じ配分をもう一度で合計 90:30 になる。場面ごとの配分も `BATCH_PLAN` のまま変えない
- [x] **生成プロンプトを v2 に上げた**（当初のタスクには無い。1回目の検証で必要になった）
      — v1 は **glossary §2 の判定基準を1つも渡していなかった**。丁寧さの3段階も「あなた」の規則も
      Day 3 に決めたのに、生成側は知らないまま。**14件が覆り、うち12件がその2規則**だった。
      [design.md](design.md)「生成プロンプトを v2 に上げた」参照
- [x] **機械による事前検品を全件（1.5h）** — [docs/ja/glossary.md](../../docs/ja/glossary.md) §2 の基準。
      迷った件は `false` に倒し、迷った旨を `native_check` に残した
      — [batch2-verified-2026-08-07.json](../../data/evaluation/candidates/batch2-verified-2026-08-07.json)
      **45（`true`）／15（`false`）、場面ごとの配分も `BATCH_PLAN` どおりに着地。**
      **`verified: false` のまま**＝ここまでは機械の事前検品であって、**ネイティブ検証は未了**
- [x] **サブエージェントによる独立監査を2周**（当初のタスクには無い）
      — 1周目で **14件差し替え・6件修正**、2周目で **3件差し替え・11件修正**。2周目の判定は
      「条件つきで可」。[design.md](design.md)「監査2周で分かったこと」に結果を記録

> **残っているのは Jun 本人のネイティブ検証**（`native_check` 21件が「人が決めるべき」と名指ししてある）。
> **ここを飛ばすと、この評価データは「モデルが書いてモデルが検品したデータ」になり、
> §0 の武器（ネイティブが正解データを作れる）そのものが消える。** 所要は21件ぶんで足りる設計にしてある。

> **2時間で120件の検証が終わらない場合、その場で90件に落とす**（「降ろす順番」の3番。
> [design.md](design.md)「判断が必要になったら」に同じ判断が書いてある）。
> **土曜に食い込ませない。** 土曜は 8/9 の準備で埋まっており、押し出される先は
> 「第二採点者に渡す20件」＝ **8月に一度しかない機会**になる。**分割の比率は変えない。**

## Day 6（8/8 土）— 評価データの仕上げ・文法リファレンス・8/9 の準備（見積 8h）

### 金曜からの引き継ぎ（2h）

- [x] **ネイティブ検証**（当初のタスクには無い。Day 5 の事前検品を裁定した）
      — **3件が覆り、3件とも `false` → `true`。** 向きが揃っており、機械は一貫して
      「直さない」側に倒していた。うち2件は個別の判断ではなく **glossary §2 の定義のほうが誤っていた**
      （段階Cの「自分の動作／相手の動作」、段階Bの「お」だけでは足りない）。**ja → en を同じコミットに。**
      詳細は [design.md](design.md)「ネイティブ検証の結果」
- [x] 内訳を 90（`true`）／30（`false`）に揃える（0.25h）
      — 反転3件で 48:12 に崩れたので、監査が落とすべきとした `true` を3件落とし `false` を3件入れた
- [x] **追加60件だけを dev 40 / test 20 に割って足す**（0.5h）— **場面ごとに 2:1 で割る。**
      全体で 2:1 でも場面が偏ると、`detection_accuracy` が「どの場面を引いたか」で動く。
      **Day 3 で振った60件の `split` は1件も変更しない**（変更すると Day 4 の測定が
      何に対する数字か分からなくなる）。結果として dev 80 / test 40 になる
      — `assign_splits(..., start_index=60)` で続きから割った。**場面ごとに 2:1 で着地。**
      **`test` の内訳は `true` 27 / `false` 13** ＝ `over_correction_rate` を `test` だけで見ると
      1件が 7.7pt 動く。**README には `n` を必ず併記する**
      — **当初ここに「`test` 単独の値だけを載せない」と書いたのは誤り**（2026-08-09 訂正）。
        **公開するのは `test` の値だけ**で、薄い分母は幅（±19〜27pt）を書いて扱う。
        120件全体と並べると、`dev` 80件＝調整に使ってよい側が分母に入る（[design.md](design.md)）
- [x] **評価データの機械点検を pytest で書く**（1h）— [tests/test_evaluation_items.py](../../tests/test_evaluation_items.py)
      件数120／内訳 90:30／dev 80・test 40／`id` の重複なし／必須キーの欠落なし／
      `needs_correction: false` の件に `corrected_sentence` が入っていないこと／場面が §3 の6つに収まること／
      **Day 3 の60件の `split` が保存されていること**（`id` と `split` の対応を固定値で持つ）
      — **120件を目で数えると必ず間違える。** 第二採点者に渡す前に固める
      — **データより先に書いた。** いまは件数系の3件が**意図的に赤**で、追加60件を足すと緑になる。
        データを作ってから書くと、出来上がったものに合わせてテストを書くことになり点検にならない
- [x] `data/evaluation/items.json` をコミット（0.25h）。`ruff` / `mypy` / `pytest` 通過
      — **2026-08-10 07:19 のコミット `3f8cfb1` に含めた。**

### 文法リファレンス（2.5h）

- [x] **文法リファレンス 8本** — [data/grammar/](../../data/grammar/)。1本200〜400語。
      **1話題に複数本が当たるように書く**（1話題1本だと検索が対応表になり、RAG と呼べなくなる）。
      今週は Chroma に載せない。**文章を書くだけ**
- [x] **例文を評価データからずらした**（当初のタスクには無い。監査で必要になった）
      — 初稿は45件の `true` のうち**30件の学習者文をそのまま載せていた**。それだと第3週の
      `retrieval_hit_rate` が**文字列の一致で当たり**、高く出たうえで実際の学習者の発話には移らない。
      規則と対比は残したまま、例文を別の文に差し替えた（[design.md](design.md)「監査で分かったこと」）

### 8/9 の準備（3.5h）

- [x] **第二採点者に渡す20件を確定させる**（1h）— [evals/rater_kit.py](../../evals/rater_kit.py)、
      キットは [20260808-2041-rater-kit.md](../../evals/rater/20260808-2041-rater-kit.md)（dev・6場面・20件）
      — **20件は `dev` から選ぶ。** [docs/ja/glossary.md](../../docs/ja/glossary.md) §5 の `rater_agreement` は
        「両方が採点した20件の一致率」であり、**どの分割から採るかは書かれていない**。
        `test` から採ると、採点のために自分が評価用の中身を読むことになり、
        **第2週のプロンプト調整がその記憶に汚染される**（§7「評価用を見て調整すると自己採点になる」）。
        一致率は尺度の解釈がそろっているかを見る数字なので、dev でも成立する
      — **この決定を glossary §5 に書き戻す**（`rater_agreement` は dev の20件で測る、と1行）。ja → en を同じコミットに
      — 候補の dev 件にベースラインを1回走らせ、**`needs_correction: true` を返した件から20件を選ぶ**。
        **直しが出ていない件は §6 で採点できない**（尺度が「直した文と理由」を見るものだから）。
        実行記録には**分割が `dev` であること**を必ず書く（test の測定と混ざると比較表が壊れる）
- [x] **採点キットを作る**（1h）— 8/9 に紙かタブレットで渡せる形にする。
      英語の手順（3〜4行）＋ §6 の3段階の定義＋20件の「学習者の文 / ベースラインの直し / 英語の理由 / 評価欄」。
      **アプリは見せなくても成立する**ので、当日の通信状況に依存させない
      — **場面はモデルに渡した文言そのものを刷る**（当初のタスクには無い。監査で必要になった）。
        丁寧さの段階だけだと「初対面の相手**または**店員」と2択のままで、
        たとえば「良い一日。→ いってらっしゃい！」は**相手が出かけるところ**でないと成立しない。
        場面が欠けたまま割れた採点は、尺度のずれではなく情報不足であり、§6 を直しても直らない
- [x] 8/9 の持ち物と段取りを1箇所にまとめてコミット（0.5h）— [day7-minna-shuugou.md](day7-minna-shuugou.md)。
      採点キット／連絡先を控えるもの／テスター向けの説明1分版（**まだ配れない。第3週に個別リンクを配る**と伝える）
- [x] **自分が先に同じ20件を採点し、封をする**（1h）← **Jun 本人の作業。**
      採点は 8/9 の夜、封（コミット `3f8cfb1`）は 8/10 07:19（Day 7 の該当行へ）
      — §6 の3段階（妥当／不十分／誤り）。**相手の採点を見てから自分が採点すると一致率が無意味になる。**
        様式は生成済み（[20260808-2041-rater-kit-jun.json](../../evals/rater/20260808-2041-rater-kit-jun.json)）で、
        `rating` に `valid` / `insufficient` / `wrong` を入れて 8/9 まで開かない
      — **機械が埋めたら `rater_agreement` は「ネイティブとモデルの一致率」になり、測りたいものではなくなる**

## Day 7（8/9 日）— Minna Shuugou と週の締め（見積 8h）

**毎月第2日曜。8月の開催はこの回だけ**（次は 9/13）。**取り返しがきかない日**なので、
アプリの作業より 8/9 でしかできないことを優先する。

**Minna Shuugou は 10:00 開始で、家を 9:30 に出る。** したがって
**朝2h ＋ 会場3h ＋ 帰宅後3h** の3ブロックに割る。**朝はどう頑張っても2時間**で、
他の休日のように朝からまとめて8時間は取れない。

### 朝（9:30 の出発まで・2h）

> **この3つは Jun 本人が記入する。** 会場の3件が達成できている以上、実際には通っているはずだが、
> **「結果から逆算して checkbox を埋める」のはやらない**——朝の枠が実際に何に消えたかは
> 実績工数の唯一の手がかりで、埋めてしまうと**見積り 2h に合わせた記録**になる。

- [ ] 採点キットと持ち物の最終確認（0.25h）— 印刷したもの／連絡先を控えるもの／充電
- [ ] **会場で聞くことを3つに絞り、1分の説明を声に出して1回練習する**（0.5h）
      — 対面で話せる時間は短い。**その場で考えると、第二採点者の依頼が後回しになって終わる**
- [ ] 土曜の積み残しの吸収（1.25h）— **なければ早く切り上げて出発する。**
      新しい作業を始めない（中断される前提の枠で始めた作業は、中断されたまま夜まで戻れない）

### 完成した120件への監査（当初のタスクには無い・帰宅後の枠から 1.5h）

**実装したら独立監査を可能な限り回す**、を運用に入れた（2026-08-09 決定）。
統合が終わった120件に対して、観点を分けたサブエージェント2体で1周目を回し、
**12件を修正したうえで2周目を回した**。結果は [design.md](design.md)「完成した120件を監査して分かったこと」。

- [x] **`test` 実行記録の漏洩を塞いだ** ← **最優先だった。8/10 の正式測定より前に閉じる必要があった**
      — [20260806-0724-baseline-test.json](../../evals/runs/20260806-0724-baseline-test.json) が
      `test` 20件の文・正解・システムの判定を全部持っており、**外した4件が引き算で出る状態**だった。
      [score.py](../../evals/score.py) に `results_redacted` を足し、既存の記録も伏せ字化。
      **伏せる前の版はコミット `a27ad4a` に残る**ので、消さずに `redaction_note` に明記した
- [x] **データ自身の矛盾3件を裁定**（eval-044/109・eval-059/107・eval-012）
      — **ネイティブ判定は動かさず**、eval-044 の学習者の文を最小化して整合させた
- [x] **重複5組の `dev` 側を差し替え**（eval-005・013・029・085・086）
      — **うち2組は句読点しか違わず、文字列の等号では見えなかった。**
      片方が `test` の組は必ず `dev` を動かした
- [x] **直しが「その場面で言わない」件を2件修正**（eval-048・eval-095）
- [x] **理由が自分の直しを説明していない件を2件修正**（eval-001・eval-091）
- [x] **glossary §5 の誤差の但し書きを直した**（ja → en を同じコミットに）
      — 「評価用40件で ±8pt」は**分母の取り方が誤っていた**。`over_correction_rate` の
      実分母は `test` で13件＝**1件 7.7pt**。目標15%は実質「2件まで」の意味だった
- [x] **2周目：1周目の対処が作った誤りを3件直した**
      — eval-044（理由で待つ側と待たせる側を逆に書いた）／eval-013（「一週間前」まで誤りに見える一般化）／
      eval-095（2つ隣の件と同じ構文になった）。**重複を消す作業が別の重複を作る**
- [x] **2周目：伏せ字化が破られていたのを塞ぎ直した** ← **最も効いた指摘**
      — `japanese_left_unquoted` は**理由が返ったときにしか立たない＝ `predicted: true` の別名**で、
      公開済みラベルと突き合わせると**外した4件が完全に復元できた**（実際に再現した）。
      **`test` は per-item の行を1つも書かない**方式に変更。
      **テストも「フィールド名が無いこと」から「全問正解と全問不正解で per-item の行が一致すること」へ**書き直した
- [x] **README の ±8pt も直した**（ドキュメントは ja/en の2面だと思っていたが**README が3面目**）
- [x] **手で書いた10件を候補ファイルに残した**（`verified: false`）—
      [batch3-hand-authored-2026-08-09.json](../../data/evaluation/candidates/batch3-hand-authored-2026-08-09.json)。
      **生成 → 検証の鎖がこの10件だけ切れている**ので、未検証であることを機械に見える形で残す
- [x] **今日書き換えた12件のネイティブ再検証**（eval-001, 005, 012, 013, 029, 044, 048, 059, 085, 086, 091, 095）
      — **覆りゼロ。12件とも書いたとおりで可。** 用紙は [day7-native-recheck.md](day7-native-recheck.md)（0.5h）で、
        **差し替えの理由を伏せて判断した**。[batch3-hand-authored-2026-08-09.json]
        (../../data/evaluation/candidates/batch3-hand-authored-2026-08-09.json) を `verified: true` に更新。
        **これで120件すべてがネイティブ検証済み**（design.md「12件のネイティブ再検証」）
      — **キットの20件のうち4件（kit_no 1・2・11・19）がこの12件と重なっている。**
        キットは作り直さない（理由は design.md）。突き合わせるときは**キットの文を正とする**
- [x] **採点キットへの監査を2周（当初のタスクには無い・2026-08-09）**
      — **1周目：採点キットが採点対象の答えを刷っていた。** 段階Bの丁寧さの文言に 8/9 に足した判定の実例
        （`お仕事、何？`＝ **eval-081・`test`**／`はじめまして`＝ eval-017 の正解で、**その eval-017 はキットの kit_no 20**）
        が、紙とチャットの両方に載っていた。`politeness_floor()`（人に見せてよい）と
        `politeness_rule()`（データ生成側だけ）に規則を割って塞ぎ、**様式をダミーで描いて
        `items.json` の文が残らないことを見る pytest** を足した
      — **1周目：§6 の「3件とも元の文で合っていた」が誤りだった**（上の採点の行へ）
      — **2周目：1周目の対処が残した穴を3つ**——照合が句読点1つで素通り／`refuse_if_graded()` の
        経路が未テスト／`politeness_rule()` の使用が未テスト。**いずれも塞いだうえで変異を入れて落ちることを確認**
      — **2周目：1周目が新しい誤りを2つ書き込んでいた**——漏洩の表で id・分割・ラベル・フィールドを取り違え／
        **§6 に kit_no と自分の評価を書いて公開していた**（＝一致率そのもの）。どちらも修正
      — **2周目：`test` の1件を見て見せ方を決める手順が §3-2 に残っていた**（`早いですね。`＝ eval-011）。
        `dev` の件へ差し替え。詳細は [design.md](design.md)
- [ ] **第3週の頭：`data/grammar/` が `test` を引用していないことの pytest**
      — grammar-008 が **`test` 7件を引用**（eval-007, 011, 057, 060, 072, 081, 117。うち
        eval-057・eval-081 は**正解ごと**）。8/10 には影響しないが、
        **Chroma に載せた瞬間に `retrieval_hit_rate` が文字列一致で当たる**
      — **grammar-008 だけではない。** grammar-004 が eval-040 の直した文、
        grammar-007 が eval-060 の学習者文を引用しており、**8本中3本**である。
        **pytest は `data/grammar/` 全体 × `test` 40件を走査する**（1本を想定して書くと残り2本を通す）
      — 照合は**句読点を落として**行う。8/9 に、完全一致の照合が
        `はじめまして` と `はじめまして。` を別物として通した実例がある（[design.md](design.md)「監査2周目」）
      — **上の件数は句読点を落とした照合での値。** 完全一致だと grammar-008 は4件、
        grammar-004 と grammar-007 は0件に見える。**「引用していない」と誤って結論できるのはこの差**である

### 会場（移動を含めて 3h）

- [x] **第二採点者を確保し、その場で20件を採点してもらう** ← 最優先。相手の所要30分
      — **承諾は取れた。その場での採点までは行かなかった**ので、想定どおり**後日 WhatsApp で回収**する。
        **その場で30分を頼むのは通らない**というのが今日の実測。会場は立ち話が連続する場で、
        「いま座って20件を読んでください」が入る隙が無い。**次回（9/13）も同じ前提で組む**
      — **採点者ゼロで帰る**は回避した（8月に次の回は無く、第4週の `correction_validity` が
        独りの採点だけになるところだった）
- [x] テスター候補に対面で声をかける（目標5人以上）— **インド人5人以上を確保。目標達成**
- [x] **連絡先（WhatsApp）を控える** — 第3週は**個別のアクセスコード付きリンクを配る**形なので、
      ここで連絡先を取り損ねると**配る相手がいなくなる**
      — **日本語ネイティブ3名の連絡先も取れた。** これは当初の想定に無い収穫で、
        **第二採点者が1人に依存しない**（後日回収が1人から返らなくても代わりに頼める）
- [ ] テキスト版を見せて、**訂正の質への反応**を聞く（「動くもの」としては見せない）
      — 実施したかどうか未記録。**次回の反省に使うので、やったかどうかだけでも埋める**

### 帰宅後（3h）

**その場で採点まで行かなかったので、この枠の中身が変わった。**
`rater_agreement` は今日は出ない（後日 WhatsApp で回収）。空いた分は**送る準備**に充てる。

- [-] 採点結果を JSON にして `evals/` に保存し、`rater_agreement` を計算（0.5h）
      — **今日は回収できないので降ろす。** 相手の返信が届いた日にそのまま実行する
        （手順は [day7-minna-shuugou.md](day7-minna-shuugou.md) §5 のまま変わらない）
- [-] **一致しなかった件を読み、尺度のどこで解釈がずれたかを glossary §6 に書き戻す**（1h）
      — 同上。**採点が返ってからでないと着手できない**
- [~] **採点キットを WhatsApp で送れる形にして送る**（当初のタスクには無い。0.5h）
      — **紙を渡せなかったので、配布経路が「印刷物」から「WhatsApp」に変わった。**
        送るのは `dev` の20件だけで `test` を1件も含まないので、**外に出しても測定は汚れない**
      — **返信の様式を先に決めて一緒に送る。** 自由記述で返ってくると、写すときに
        `valid` / `insufficient` / `wrong` への割り当てを**自分が解釈することになり、
        一致率が「自分が相手をどう読んだか」の数字になる**
      — **本文は生成した**（[20260808-2041-rater-kit-whatsapp.txt](../../evals/rater/20260808-2041-rater-kit-whatsapp.txt)、
        `rater_kit.py --whatsapp`）。末尾に「1:」〜「20:」の20行、期限は `Sunday 16 August`。
        **紙の様式を手で書き写していない**——同じキット JSON から刷るので20件が食い違わない
      — **3択は小文字で刷った。** 紙は `Valid` と大文字で、`_graded` はそれを弾く。
        チャットなら**写すときの変換を1手まるごと消せる**
      — **`test` のキットは `whatsapp_message()` が送信前に拒否する**（pytest 済み）。
        `select()` は「test の実行記録からキットを作らない」を見ているが、**外へ出る瞬間の関所は無かった**
      — **送信そのものは Jun 本人。** 貼って送る（相手は 8/9 に承諾した1人目）
- [x] **自分の20件の採点を先に終えた**（Day 6 からの持ち越し・1h）← **Jun 本人**
      — **`valid` 17 ／ `insufficient` 3 ／ `wrong` 0**。`insufficient` は kit_no 4・6・17
        （[20260808-2041-rater-kit-jun.json](../../evals/rater/20260808-2041-rater-kit-jun.json)）
      — **3件を「元の文で合っていた」1つの型として書いていたが、誤りだった**（監査で判明）。
        **4・6 はその型だが、17（eval-046）は `needs_correction: true`** ＝「直す必要はあったが
        AI が余計に変えた」型。**規約①では 17 を救えず、印刷済みの `wrong` の定義（意味が変わっている）に
        当てはまってしまう**ので、規約を2つに増やした
      — **この採点を一度消してしまった**（キット再生成で空の様式に上書き）。復元済み。
        `refuse_if_graded()` で塞いだ。詳細は [day7-minna-shuugou.md](day7-minna-shuugou.md) §6
- [x] **採点の副産物：データセットのラベルが1件覆った**（当初のタスクには無い）
      — **eval-052「部長、話したいことあります。」を `false` に倒した**（話し言葉で `が` は落ちる）。
        **`に` の省略は誤りのまま**で、`が` だけが例外。**`test` の eval-054 は現状のまま**（「確認が必要です」が要る）。
        帳尻は eval-118 を `true` に差し替えて取った。詳細は [design.md](design.md)
- [x] **§6 に規約を2つ足した**（当初のタスクには無い）
      — ①**そもそも直す必要がなかった**（kit_no 4・6）／②**直す必要はあったが AI がそれ以上に変えた**（kit_no 17）。
        どちらも尺度に箱が無く、放置すると `insufficient` と `wrong` に割れる。
        ja → en → 採点キット（`scale_note()`）に同じ文言。**キットを送る前に間に合った**
      — **②は `over_correction_rate` では拾えない**（あの指標は `false` の件しか数えない）。
        §6 の但し書きに「過剰訂正は別の指標が見ている」と書いていたのは**①にしか当たらなかった**
      — **紙とチャットで文言が二重管理になっていたのを1箇所にまとめた**（`scale_note()`）。
        **3段階の表記だけが違う**（紙は大文字、チャットは小文字）
- [x] **封をする＝ `-jun.json` をコミットする** — **2026-08-10 07:19 のコミット `3f8cfb1` で完了。**
      git のタイムスタンプが「第二採点者の返信より前」であることの唯一の証拠になる。**返信が届いたあとは開かない**
      — **副作用として、20件の評価がそのまま公開された。** 封の証拠をコミット時刻に置いた以上、
        答案も一緒に出るのは避けられなかった。**次回は答案のハッシュだけ先にコミットする**
        （[docs/ja/glossary.md](../../docs/ja/glossary.md) §6 に手順を書き戻した）
- [~] 記録欄を埋める（**実績工数を含む**）（0.5h）
      — **数字の欄は埋めた。`実績工数` だけ空のまま**＝ Jun 本人しか知らない。
        **機械が推測して埋めると、第2週の見積り精度を上げるための欄が「見積りの写し」になる**
- [x] 「降ろす順番」の現在地を1行 [design.md](design.md) に書く（0.25h）— **8/16（日）の点検**で迷わないため
      — [design.md](design.md)「「降ろす順番」の現在地」。**発動はゼロ。7つとも選択肢として残っている**
        （量は減った——0 は 4.5h のうち会話セッション 1h が済み、3 は作る工数だけが済んでいる）
      — **初稿で 2・3・6 を「使い切った／不要になった」と書いたのは誤りだった**（監査で判明）。
        **2 は PLAN.md が第2週の土日に「リファレンス10本へ」を残しており生きている**。
        3 は第4週の手採点が10件ぶん浮く。6 は運用工数の話で、確保できたことと別
      — **番号順に機械適用すると、まだ浮く 2・3 を残したまま音声を先に捨てることになる。**
        8/16 は順番どおりに降ろす前に 2・3・4・5 で足りるかを見る
- [-] 予備枠（0.75h）— **会場が延びた分をここから差し引く**。**消化せずに週を閉じた**（下の棚卸しへ）

> **予備枠を作業で埋めない。** 対面の予定は延びるのが普通で、埋めると 8/9 の遅れが
> そのまま第2週の初日に食い込む。余ったら早く終える。

> **第2週のステアリング作成（1.5h）は 8/10（月）の朝に移した。** 8/9 は
> 朝2h・会場3h・帰宅後3h に割れて、まとまった1.5h が取れないため。
> **第2週の作業を始める前に書けばよい**ので、初日の朝で手順としては足りる
> （CLAUDE.md のとおり**1ファイルずつ承認を得る**）。

---

## 週を閉じる時点で開いているもの（2026-08-09 時点の棚卸し）

**第1週の作業は終わり。** 残っている `[ ]` `[~]` は3種類しかなく、**どれも「やり残した実装」ではない。**

**コミットは 8/10 07:19 の `3f8cfb1` で済んだ**（`items.json`・`-jun.json` の封・完了条件の3行）。

| 種類 | 残っているもの | 誰が |
|---|---|---|
| **Jun 本人しか書けない** | 朝の枠3件／「テキスト版を見せた反応」／記録欄の `実績工数` | **推測で埋めない**（理由は各所に記載） |
| **週明けの実行アクション** | **採点キットを WhatsApp で送る**（本文は生成済み。貼って送るだけ） | **これが第2週の3件すべての前提**。送らないと `rater_agreement` は永久に出ない |
| **第2週・第3週へ送ったもの** | `rater_agreement`／ベースライン正式測定（test 40）／`dev` 80件の誤り分析／`data/grammar/` の `test` 引用を見る pytest | 送り先は「第2週へ引き継ぐ確定事項」に記載済み |

**予備枠（0.75h）は消化しない。** 余ったら余ったまま週を閉じる——
埋めると 8/9 の遅れが第2週の初日に食い込む、というのがこの枠を作った理由そのものである。

> **`[ ]` が残ったまま週を閉じてよい。** 閉じられないのは「何が残っているか分からない」ときだけで、
> 上の3種類に全部入っているなら、**8/16 の点検で読むべきものは揃っている。**

---

## 完了条件

- [x] テキストで複数ターンの会話ができ、終了時に訂正の一覧が出る
      — [app/main.py](../../app/main.py)。**実機で確認済み**（Day 2 の thanks・2ターン）。
        会話中に訂正は出ず、`End the conversation` を押すと2件とも振り返りに出た
- [x] **ベースラインの `detection_accuracy` と `over_correction_rate` が実行記録つきで出ている**
      — **`n=20` の暫定値でよい**（Day 4）。n=40 の正式値は第2週初日に測る（末尾の節）
      — [20260806-0724-baseline-test.json](../../evals/runs/20260806-0724-baseline-test.json)
        （`detection_accuracy` 1.0 ／ `over_correction_rate` 0.571 ／ `unusable_verdicts` 0）
- [x] 評価データ120件が JSON でコミットされている — **`3f8cfb1`（2026-08-10 07:19）**
- [x] 音声の疎通確認が済み、**第3週に音声を載せられるか判断がついている**
      — 8/3 に iPhone Safari の実機で①録音・②自動再生とも成功。
        **判断：第3週は自動再生で組んでよい**（罠は「同じ返事＝同じ音声は2回目が鳴らない」。design.md）
- [x] **第二採点者を確保した**（採点まで済めば `rater_agreement` も出ている）
      — **確保は達成（承諾ベース）。`rater_agreement` は出ていない**（後日 WhatsApp で回収）。
        **完了条件は「確保」までなので満たしている**が、**数字は第2週に持ち越し**になった
- [x] テスター候補の前向きな返事が集まっている — **インド人5人以上＋日本語ネイティブ3名**

> **月曜始まりに変えたことで 8/9 が週内に入り、上の2行が完了条件に加わった**（2026-08-04）。
> 前向きな返事が**3人未満なら第2週の頭に計画を見直す**（design.md「判断が必要になったら」）。
> **5人以上なので見直しは発動しない**（2026-08-09 判定）。

---

## 第2週へ引き継ぐ確定事項

**第2週は 8/10（月）〜 8/16（日）**（月曜始まり）。ディレクトリは `.steering/20260810-week2-validation/`。

- **第2週のステアリング3点は 8/10（月）の朝2h で作る**（8/9 から移動）。
  **初日の枠がほぼ全部これに消える**ので、第2週の1日目に実装を積まないこと

- **ベースラインの正式測定（test 40件）は 8/10（月）に行う。** ← **検証ノードを1行でも書く前に。**
  評価用が40件に揃うのは 8/8 で、土曜は 8/9 の準備で埋まっているため週内に入らなかった。
  **本実装ができてから測ると「本実装を知ったうえで作った素朴版」になり、比較表が成立しない**
  （[glossary](../../docs/ja/glossary.md) §7 のベースラインの定義）。0.5h。
  スクリプトは Day 4 で出来ているので、**走らせて実行記録を残すだけ**。
  **この n=40 の数字が README に載る値**で、Day 4 の n=20 は暫定値として実行記録に残るだけ

- **ベースラインの誤り分析（dev 80件）は第2週へ送った。** 金曜が2時間になった分の玉突きで、
  土曜の8時間に入らなくなったため。**第2週の検証ノード設計の直前にやるほうが自然**でもある
  （閾値を決める作業と同じ日に読める）。**test 40件は、上の正式測定1回のほかは第4週まで触らない**
- **8月の Minna Shuugou の開催は 8/9 のみ**なので、テスター投入は**個別にアクセスコード付きリンクを配る**形にする。
  配布方法が変わるだけで作業量は増えない。PLAN.md 側は修正済み

- **`rater_agreement` は第2週へ持ち越した**（2026-08-09 確定）。会場で承諾は取れたが、
  **その場で30分を頼むのは通らなかった**ため、採点は WhatsApp で回収する。
  **第4週の `correction_validity` の期限から逆算すると、遅くとも第3週中には手元に無いと間に合わない。**
  返信が来ない場合の代替は用意できている——**日本語ネイティブ3名の連絡先を取ってある**ので、
  **1人からの返信に賭けない**。第2週の頭に、返信が無ければ2人目へ出す

- **対面で「いま30分の作業」を頼むのは通らない、が実測（2026-08-09）。** 9/13 の回に向けて、
  **依頼はその場で完結させる形ではなく「持ち帰ってもらう形」で設計する**
  （紙を渡して後日回収、または QR で様式へ誘導）。**次回の準備はこの前提で組む**

---

## 記録欄（週の終わりに埋める）

| 項目 | 結果 |
|---|---|
| ベースライン `detection_accuracy` | **100%**（n=20 暫定。うち `true` は13件） |
| ベースライン `over_correction_rate` | **57.1%**（n=20 暫定。うち `false` は7件） |
| ベースライン `format_compliance_rate` | **100%**（n=20 暫定） |
| `rater_agreement`（dev 20件） | **未測定。第2週へ持ち越し**（採点を後日 WhatsApp で回収するため） |
| 自分の採点（dev 20件） | **`valid` 17 ／ `insufficient` 3 ／ `wrong` 0**。封をするコミットが残っている |
| 評価データ | **120件（`true` 90 ／ `false` 30、dev 80 ／ test 40）。全件ネイティブ検証済み**。コミットが残っている |
| 音声疎通確認 | **成功**（8/3・iPhone Safari で録音・自動再生とも）→ 判断：**第3週は自動再生で組む** |
| 第二採点者 | **確保できた（承諾のみ）** → その場で採点：**後日**（WhatsApp で回収。本文は生成済み） |
| テスター前向き回答 | **インド人5人以上**（＋日本語ネイティブ3名の連絡先） |
| 実績工数 | 　時間（見積 26h ＝ Day 1〜7。Day 0 は数えない）← **Jun 本人が記入** |

> **`実績工数` を機械が埋めない。** この欄は「見積りとどれだけずれたか」を残すためにあり、
> 見積りから推測して書いた瞬間に、見積り精度を上げるための欄が**見積りの写し**になる。

> **実績工数を必ず記録する。** 第2週以降の見積り精度が上がり、
> 「降ろす順番」をいつ発動するかの判断材料になる。
