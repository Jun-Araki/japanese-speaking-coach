# 第1週 タスクリスト

対応する設計: [design.md](design.md) ／ 期間: 2026-08-02（日）〜 08-07（金）

進捗記号: `[ ]` 未着手 ／ `[~]` 着手中 ／ `[x]` 完了 ／ `[-]` 降ろした

---

## Day 1（8/2 日）— 土台と人集め

- [ ] 環境セットアップ（Python 3.11、`.venv`、`ruff` / `mypy` / `pytest`）
- [ ] `.env` を作り、`ANTHROPIC_API_KEY` を入れて疎通確認
- [ ] **テスター候補 5〜8 人に声をかける**（3週間後に試してほしい、と伝える）
- [x] **Minna Shuugou の開催日を確認** → **毎月第2日曜。直近は 8/9**（対応は末尾の節へ）
- [ ] japanesecompass.com を30分触り、差分メモを1段落書く

> 人に依頼する2件を初日に置いているのは、**返事に日数がかかり自分では短縮できないから**。
> コードは後から巻き返せるが、ここは巻き返せない。

## Day 2（8/3 月）— 疎通確認 ← 計画の前提

- [ ] **最初の30分**：`st.audio_input` と `st.audio(autoplay=True)` だけの画面を公開
- [ ] **iPhone Safari の実機で、録音と自動再生ができるか確認**
- [ ] 押して開始→もう一度押して停止なのか、実際の操作を記録
- [ ] **動かない場合：テキスト方式へ切り替える判断をここで下す**（先送りしない）
- [ ] Streamlit で会話画面（録音なし・テキスト直結）
- [ ] `dialogue.reply()` — 場面とレベルに応じた相手役、**1〜2文に制限**

## Day 3（8/4 火）— 訂正

- [ ] `correction.check()` を**会話とは別呼び出し**で実装
- [ ] 構造化出力の JSON スキーマを固定（`grounding_ids` は空配列で確保）
- [ ] **JSON が壊れて返ったときの処理**（[docs/ja/functional-design.md](../../docs/ja/functional-design.md) エラーハンドリング）
      — 再試行1回、失敗しても `format_compliance_rate` の分母には必ず数える。**捨てて隠さない**
- [ ] 訂正結果を `st.session_state` に溜め、**会話中は表示しない**
- [ ] 「会話を終える」→ 振り返り表示

## Day 4（8/5 水）— 評価データ 60件

- [ ] 候補を大規模モデルに生成させる（場面ごと、初級者が言いそうな文）
- [ ] **全件を自分で検証・修正**（[docs/ja/glossary.md](../../docs/ja/glossary.md) §2 の基準に従う）
- [ ] 迷った件は `false` に倒し、判断に迷った旨をメモに残す
- [ ] dev / test に分割（比率は 2:1 を維持）

## Day 5（8/6 木）— ベースライン ← 今週の成果物

- [ ] 採点スクリプトの骨組み（`needs_correction` の一致のみ機械採点）
- [ ] `format_compliance_rate` を精度と**別に**記録
- [ ] 採点スクリプトの pytest
- [ ] `correction.baseline_check()`（1回呼び出し、作り込まない）
- [ ] **評価用データで測定 → 数字が1つ出る**
- [ ] 実行記録を `evals/runs/` に保存
- [ ] **評価用から5件を手で照合**

## 8/7（金）— 評価データを仕上げる

- [ ] 評価データを **120件**へ（直した方がいい90／そのままで自然30）
- [ ] 文法リファレンス 8本（1本200〜400語、**1話題に複数本が当たるように書く**）

> **8/7 までに 120件を終える。** 翌々日 8/9 の Minna Shuugou で第二採点者に採点して
> もらうため、当初の「週末に余裕があれば」から**必達に変わった**。間に合わない場合は
> 全体が90件でもよいので、**採点してもらう20件だけは確実に用意する。**

---

## 完了条件

- [ ] テキストで複数ターンの会話ができ、終了時に訂正の一覧が出る
- [ ] **ベースラインの `detection_accuracy` と `over_correction_rate` が実行記録つきで出ている**
- [ ] 評価データ120件が JSON でコミットされている
- [ ] テスター候補の返事が集まり始めている（前向き3人以上）
- [ ] 音声の疎通確認が済み、**第3週に音声を載せられるか判断がついている**

---

## 第2週へ引き継ぐ確定事項

### 8/9（日）Minna Shuugou

**毎月第2日曜。直近は 8/9。**

この時点ではテキスト版しか無いので、目的は「触ってもらう」ではなく次の3つ。

- [ ] **第二採点者を確保する。可能ならその場で20件を採点してもらう** ← 最優先
      — [docs/ja/glossary.md](../../docs/ja/glossary.md) §6 の3段階尺度を持参。相手の所要30分。
      **アプリは要らない。評価データがあれば成立する**
- [ ] テスター候補に対面で声をかける
- [ ] テキスト版を見せて、**訂正の質への反応**を聞く（「動くもの」としては見せない）

### 第3週

**8月の開催は 8/9 のみ**なので、テスター投入は**個別にアクセスコード付きリンクを配る**形にする。
配布方法が変わるだけで作業量は増えない。PLAN.md 側は修正済み。

---

## 記録欄（週の終わりに埋める）

| 項目 | 結果 |
|---|---|
| ベースライン `detection_accuracy` | |
| ベースライン `over_correction_rate` | |
| ベースライン `format_compliance_rate` | |
| 音声疎通確認 | 成功 / 失敗 → 判断： |
| テスター前向き回答 | 　人 |
| 実績工数 | 　時間（見積 14〜16h） |

> **実績工数を必ず記録する。** 第2週以降の見積り精度が上がり、
> 「降ろす順番」をいつ発動するかの判断材料になる。
