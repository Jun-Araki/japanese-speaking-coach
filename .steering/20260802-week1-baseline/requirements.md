# 第1週 要求内容 — 会話が動く・最初の数字・テスター声かけ

**期間**: 2026-08-02（土）〜 2026-08-07（金）
**対象**: PLAN.md §3 第1週（PLAN.md はローカル限定。公開リポジトリには含めない）
**稼働見込み**: 平日朝2時間 × 5日 ＋ 週末 ≈ 14〜16時間

---

## 今回やること

テキストだけで会話と訂正を動かし、**ベースラインの数字を1つ出す**。並行して**テスター候補に声をかける**。

音声・RAG・FastAPI・LangGraph・Docker は**すべて今回の範囲外**。

## なぜこの範囲か

- 中身（訂正の質）が固まる前に音声を載せると、精度が悪いときに音声のせいか訂正のせいか切り分けられない。**縦スライスをテキストで先に通す。**
- ベースラインは**本実装を作る前に測らないと意味がない**。後から測ると「本実装を知ったうえで作った素朴版」になり、比較として成立しない。
- テスター募集は第3週に配ってからでは間に合わない。**第1週に着手する必要がある。**

---

## ユーザーストーリー

1. **学習者として**、場面とレベルを選んで日本語で書き込むと、AI が相手役として1〜2文で返してくれるので、会話の練習ができる。
2. **学習者として**、会話を終えると、自分の書いた文のうち直した方がいいものと、自然な言い方と、英語の理由が一覧で見られる。
3. **開発者（Jun）として**、評価スクリプトを実行すると、素朴版の判定の正しさと余計な直しの率が数字で出るので、以降の改善が測れる。

---

## 受け入れ条件

### 必達（これが崩れたら週として失敗）

- [ ] **8/3 中に `st.audio_input` と `st.audio(autoplay=True)` の疎通確認が終わっている**
      — iPhone Safari で録音と再生ができるかを実機で確認する。**動かない場合は第1週のうちにテキスト方式へ戻す判断をする**（第3週に発覚すると計画が崩れる）
- [ ] Streamlit の1画面で、場面とレベルを選び、**テキスト入力**で AI と複数ターン会話できる
- [ ] 訂正が**会話とは別の呼び出し**で動き、[docs/ja/glossary.md](../../docs/ja/glossary.md) §1 のキー名どおりの JSON を返す
- [ ] 評価データ **120件**が `data/evaluation/` に JSON である（直した方がいい90件／そのままで自然な30件、dev 80 / test 40）
- [ ] **素朴版（ベースライン）の `detection_accuracy` と `over_correction_rate` が測れている**
- [ ] 測定は**実行記録つき**（モデル名・プロンプト版・日付・分割）
- [ ] テスター候補 5〜8 人に声をかけ終えている
- [ ] Minna Shuugou の8月の開催日を確認し、**第3週に間に合う回を押さえている**

### 望ましい（落としてよい）

- [ ] 文法リファレンス 8本
- [ ] 既存サービス（japanesecompass.com）を30分触った差分メモ

---

## 制約事項

- **訂正は会話中に出さない。** 毎ターン裏で走らせ、終了時にまとめて表示する
- **誤りの種類分けをしない**（[docs/ja/glossary.md](../../docs/ja/glossary.md) §2）
- **評価用40件は、作った直後にベースラインを測る1回だけ触る。** それ以降は第4週まで見ない
- 評価データは**大規模モデルに候補を出させ、全件を自分で検証する**。ゼロから手書きしない
- 特定個人の発言をそのまま使わない（一般化した文にする）
- 見た目は今週は触らない（第2週以降、CSS のみ1時間）
- 秘密情報は `.env`。`.env.example` のみコミット

---

## 今週やらないこと（提案されても断る）

音声認識・音声合成・RAG・SudachiPy・検証ノード・FastAPI・LangGraph・PostgreSQL・Docker・デプロイ・和風の見た目・画面の追加。

例外は受け入れ条件1行目の**疎通確認だけ**（画面を1枚公開するが、会話には繋がない）。
