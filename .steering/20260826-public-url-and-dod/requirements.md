# 要求 — 公開 URL の復旧と、9/6 の DoD まで（2026-08-26 起票）

**実行枠**: ~~2026-08-26（水）~~ **2026-08-27（木）〜09-06（日）＝ 11日 × 2h ＝ 22h**
／ **積む見積は 11.5h（52%）**。**ディレクトリ名は起票日（8/26）のまま**にする——
起票と着手が1日ずれただけで名前を変えると、あとで日付を手がかりに探せなくなる。PLAN.md §3「枠を100%埋めると必ず壊れる」に従い、残りは緩衝として空ける。

**この作業単位の終わりは 9/6 の応募開始**である。9/6 に出す応募で面接官が最初に触るのは
**README と公開 URL の2つだけ**で、それ以外は当日そこに無い。

---

## なぜ今これをやるか

**2026-08-25 に公開 URL が落ちていることが分かった。**

```
File "/mount/src/japanese-speaking-coach/app/main.py", line 45, in <module>
    from app.theme import (  # noqa: E402
ImportError
```

**45 行目は 8/25 に足した import で、`app/main.py` は新しいのに `app/theme.py` が古い。**
Streamlit Community Cloud が**プロセスを再起動せずスクリプトだけ再実行**しており、
`sys.modules` に残った古い `app.theme` に新しい定数が無い。

**同じ日に、同じ壊れ方をローカルでも見た**——`localhost:8501` で動いていたプロセスが
8/22 より前の `app.limits` を抱えたまま `ImportError: cannot import name 'start_tts_cooldown'` を出していた。
**再起動すれば直る種類の故障だが、再起動するまでは公開 URL が死んでいる。**

**これは 9/6 の応募に直接あたる。** README に URL を載せて出す以上、
**押した先がエラー画面である状態は、コードの中身が何であっても評価を終わらせる。**

> **PLAN.md の 8/31〜9/6 の週にこの作業は載っていない。** 載っていないものを足す判断であり、
> **代わりに何かを降ろしてはいない**（枠が 48% しか埋まっていないため）。
> なお PLAN.md は §3 の週割りで「応募開始 9/6」、§4 の見出しで「応募 9/13〜9/14」と**食い違っている**。
> **8/19 夜の改訂で新しいのは §3 側**なので、本作業は **9/6 応募開始**を前提に置く。

---

## 今回やること

| # | 内容 | 見積 |
|---|---|---|
| 1 | **公開 URL の復旧**（再起動 → 開始画面・注意書き・1往復の会話を実機で確認） | 1.0h |
| 2 | **同じ壊れ方の再発防止**（デプロイ後の確認手順を runbook に。**別ファイルに定数を足したら再起動が要る**ことを明記） | 0.5h |
| 3 | **README の仕上げ**（英語注釈つきスクショ1枚・アーキ図の要否判断・**公開 URL へのリンク**） | 1.5h |
| 4 | **GitHub Bio / Pin を AI 信号に更新** | 0.5h |
| 5 | **面接準備**（英語2分デモ台本・想定質問集・音読3回・発信1本） | 4.0h |
| 6 | **携帯での実機確認のやり直し**（[20260825-mobile-audio](../20260825-mobile-audio/tasklist.md) の4項目が通らなかった場合のみ使う緩衝） | 3.0h |
| 7 | **未実装の2機能の判断**（「もう一度」ボタン・AI の返事を隠す切り替え。実装するか降ろすか） | 0.5h |
| 8 | **週の締め**（`ruff` / `mypy` / `pytest`・降ろす順番の点検・実績の記入） | 0.5h |
| | **合計** | **11.5h / 24h（48%）** |

---

## ユーザーストーリー

- **面接官として**、README のリンクを押したら**動いているデモ**に着きたい。
  エラー画面を見た時点で、その先の比較表も評価データも読まれない
- **バンガロールの学習者として**、9/13 に渡されたリンクを携帯で開いて、
  **説明を受けずに1往復**できてほしい
- **Jun として**、面接で**降ろした判断を英語で言える**状態にしたい。
  「時間が無かった」ではなく「何を残し、何を捨てたか」を話すため

---

## 受け入れ条件

1. **公開 URL が、その時点の `main` と同じコードで開始画面まで描画される。**
   ImportError も、赤いエラーボックスも出ない
2. **公開 URL で会話が1往復できる**（テキストで可。音声は端末依存なので条件に含めない）
3. **`docs/ja/` の runbook 相当箇所に、デプロイ後の確認手順が書かれている。**
   最低限「**push しただけでは古いモジュールが残る。定数や関数を別ファイルに足したら再起動する**」の1行
4. **README に、英語注釈つきのスクリーンショットが1枚**あり、**公開 URL へのリンク**がある
5. **README の Progress の `A deployed demo carrying the current code` にチェックが入る**
   （**1 と 2 が本当に済んでから**。先にチェックを入れない）
6. **GitHub の Bio と Pin が AI 信号に更新されている**
7. **英語2分デモ台本・想定質問集が書かれ、声に出して3回読んである**
8. **`ruff` / `mypy` / `pytest` が通る。** ドキュメントは **ja と en を同じコミットに含める**
9. **9/6 の時点で、9/20 DoD の残りが「テスターの感想3件」だけになっている**
   （それは 9/13 の Minna Shuugou でしか取れない）

---

## 制約

- **PLAN.md「9/20 までにやらないこと」に触れない**——発音採点・ローカルモデル・React 画面・
  復習リストの画面・評価データ150件超・DSPy / scikit-learn / AWS
- **見た目は CSS のみ。** UI フレームワークは入れない
- **無料枠のままでいく**（2026-08-22 の決定）。**有料キーに切り替えるなら、
  先に「訂正が日次トークン上限の外にある」項目を開くこと**（[前回の tasklist](../20260821-latency/tasklist.md)）
- **枠を埋め直さない。** 前倒しで浮いた時間に別の作業を積まない
- **9/13 は動かせない**（毎月第2日曜。9月はこの1回）

---

## やらないこと

- **訂正の品質・プロンプト・閾値・モデルの変更。** README の公開数字が実装を説明しなくなる
- **応答時間の再測定。** 8/22 に測った数字を使う
- **音声段の精度測定**（PLAN.md で降ろし済み）
- **Docker の作り直し**（8/21 にビルド検証済み）

---

## 未決（Jun の判断が要る）

- **未実装の2機能**を実装するか降ろすか。**降ろすなら画面遷移図の矢印も消す**（ja / en）
- **README に公開 URL を載せるか。** 載せれば面接官が触れるが、**アクセスコードをどうするかが付いてくる**
  （`ACCESS_CODE` は「金がかかるリンクを全世界に開かない」ために入れたもの。§2-5）
- **実績工数の記入**（前回の tasklist に1件残っている）
