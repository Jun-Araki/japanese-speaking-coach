# タスクリスト — 公開 URL の復旧と、9/6 の DoD まで（2026-08-26 起票）

対応する設計: [design.md](design.md) ／ 要求: [requirements.md](requirements.md)
／ 実行枠: **2026-08-27（木）〜09-06（日）＝ 11日 × 2h ＝ 22h**。**積むのは 11.5h（52%）**

進捗記号: `[ ]` 未着手 ／ `[~]` 着手中 ／ `[x]` 完了 ／ `[-]` 降ろした ／ `[→]` 先の週へ送る

**順番は A → B → C → D → E。** A が終わるまで他を始めない——
**公開 URL が落ちている状態で README を磨いても、押した先がエラー画面である。**

---

## design.md からの変更点

**着手が 9/2 になった。** 8/27〜9/1 は動いていない（コミットも編集も無い）。
**残りは 9/2〜9/6 の5日 ＝ 10h** で、積んであった 11.5h に足りない。
**F（未実装2機能の判断・0.5h）を次の作業単位へ送り**、緩衝3hは無いものとして進める。

**1. 公開 URL は「壊れて」いなかった。寝ていた（2026-09-02）。**
開いて出たのは `ImportError` ではなく「Zzzz / This app has gone to sleep due to inactivity」。
起こしたら**約4分**で開始画面まで来た。**8/25 の ImportError は、この起動で解消されている**
（開始画面に 8/25 追加の `VOICE_SOURCE_NOTE` が出た＝新しいコードが走っている）。
**B に「寝る」を足した。**再起動の話だけでは 9/13 に間に合わない。

**2. スクリーンショットの撮り方を変えた。** 設計の案 A（HTML に並べてブラウザで1枚）を、
**動いているアプリの DOM と CSS をそのまま取り出して静止 HTML にし、`qlmanage` で PNG にする**方法に変えた。
**Pillow もヘッドレス Chrome も入れない**という条件は満たしている（`qlmanage` は macOS 標準）。

**3. 注釈の3つ目を差し替えた。** 「根拠に条番号が出る」と書く予定だったが、
**ローカルでも本番でも `Reference:` の行が出なかった**（`grounding_ids` が空）。
出ないものを README の注釈で謳えないので、3つ目は
**「正しい文は正しいと言う（黙って飛ばさない）」**にした。

**原因は 9/2 に特定した（副エージェントによる調査）。**
- **検索は動いている。** Chroma も sentence-transformers も入っており、記事はプロンプトに届いている
- **モデルが `grounding_ids: []` を返している。** 実際の応答で確認
- 理由①：**上位3件に正解の条が入らない**（「を→に」の `grammar-001` は6位）
- 理由②：**`correction/engine.py:147` に「grounding_ids は常に空配列」という古い指示が残っている。**
  152行目のコメントは「最後の箇条書きを置き換える」と書いているが、コードは追記しているだけで置換していない。
  組み上がったプロンプトに**矛盾する2つの指示が同居**している
- **実測値は 21/40（52.5%、test 分割、8/20 のラン）。** 常に出るものではない
- **README 87行目は「Corrections cite a grammar reference」と無条件に書いていた** →
  **実測値と「半分は出ない」ことを明記する形に直した（9/2）**

**プロンプトは直さない（この作業単位では）。** 直すと**公開しているすべての数字が動く**。
本タスクリストの「対象外」に従い、**次の作業単位で「直す→測り直す→README を更新する」を1本にして積む。**

---

## A. 公開 URL の復旧（1.0h）

- [x] ~~**Manage app → Reboot app**（Jun のアカウント作業）~~
      → **不要だった。** アプリは寝ていたので、URL の「Yes, get this app back up!」で起きた（9/2）
- [x] 再起動後、URL を開いて**開始画面が出る**ことを確認
      — 注意書き・**音声の1行（`VOICE_SOURCE_NOTE`）**・連絡先が見えること
      — **この1行が出れば、8/25 のコードが本当に本番へ届いたことの証拠になる**
- [x] **1往復する。** 場面とレベルを選び、日本語で1文送り、返答が返る
      — 「おはよう。わたしは会社に行きますです。」→「そうなんですね。いってらっしゃい！」（9/2 本番）
- [x] **「会話を終える」まで通す。** 振り返りが出る
      — `行きますです` → `行きます` ＋ 英語の理由。**並列の訂正が本番で通ったのはこれが初めて**（9/2）
      — **ただし `Reference:`（根拠の条番号）の行は出なかった。** 本番でもローカルでも出ない → 調査中
- [ ] **携帯で開いて、[20260825-mobile-audio](../20260825-mobile-audio/tasklist.md) の4項目をここで消化する**
      — ①マイク許可が1回か ②返答が鳴るか ③鳴らないとき ▶ で鳴るか ④自分の声でターンが終わらないか
      — **⑤を足した（9/3）**：**わざと「許可しない」を押し**、赤いエラーが出ないこと・
        録音ボタンで会話が続けられること・**携帯が熱くならないこと**
      — **8/25 の修正は仕組みの側から潰しただけで、実機では1つも確認できていない**（あちらの tasklist の但し書き）
      — **③でも鳴らないときは携帯を耳に当てる。** iOS はマイクを掴んでいる間、再生を受話口へ回すことがある
      — **本番で確かめるのが正しい。** 9/13 に人が開くのは `localhost` ではない
- [x] **本番の会話画面に出た赤いエラーの原因を特定し、直した（9/2 発見 → 9/3 対応）**：
      `UnknownError: Failed to construct 'RTCPeerConnection': Cannot create so many PeerConnections`

      **原因は「毎ターン作り直される」ではなかった。** ブラウザで数えたところ、
      **ストリームが開始できないとき `RTCPeerConnection` を毎秒約5,000個作り続けていた**
      （90秒で18万個、その後42万個まで確認）。`desired_playing_state=True` を渡したままなので、
      **失敗するたびに間を置かず作り直す。**やがてブラウザが拒否し、赤字が出る。
      **踏むのは「許可しない」を押した人**で、9/13 の会場にはその人がいる。

      **直し方**：**3回続けてストリームが上がらなかったら要求をやめる**（`continuous.worth_asking`）。
      **1回では駄目**——最初の実行は必ず「まだ繋がっていない」ので、全員からマイクを取り上げる。
      やめたあとは画面の文言も **「マイクを起動しています…」→「マイクに繋がりませんでした。
      下のボタンを押すか、入力してください」** に変える（起動をやめた画面で「起動中」は嘘）。

      **確認**：ブラウザで実際に通した。**対策前は 427,704 個まで増え続け、対策後は 263 個で止まった**
      （15秒待っても増えない）。録音ボタンと入力欄はそのまま使える。

      **監査（副エージェント）で3点直した（9/3）**：
      - **回数が足りなかった。** 繋がる接続が**すでに2回を消費する**（描画→応答→やっと「再生中」）ので、
        3回では余裕が1回しかない。**交渉中に1文打った学習者から、動くはずのマイクを取り上げる。**
        → **6回**にした。理由も定数のところに書いた
      - **「一度繋がってから切れた」場合も恒久停止していた。** 切れたのは電波の都合で、端末は動くと
        分かっている → **一度でも音を運んだら二度と諦めない**（`stream_was_live`）
      - **判定を1実行に2回呼んでいて、境界の1実行だけ食い違っていた**
        （文言は「繋がりません」なのにウィジェットはまだ要求中）→ **ページの先頭で1回だけ決める**
      - **テストが「3」を固定しておらず、`GIVE_UP_AFTER=1` にしても緑のままだった。**
        さらに `app/main.py` 側の配線は**テストゼロ**だった
        → リテラルで固定し、**画面側に4本**足した（`tests/test_screens.py`）。
        **わざと壊して落ちることを確認済み**：定数を1に／`keep_trying=` を消す／
        `SESSION_KEYS` から `dead_runs` を抜く、の3つとも落ちる

      **限界も書いた（ja/en 両方）**：**この対策は嵐を自力では止めない。**
      失敗する再試行は Streamlit に何も返さないので**再実行が起きず**、
      `desired_playing_state=False` がコンポーネントに届かない。
      **止まるのは学習者が何かをしたとき**（文字を打つ・録音ボタンを押す）。**タイマーではなく人が止める。**

      **2周目の監査（9/3）でさらに4点**——**3つはテストの穴、1つはドキュメント**：
      - **`stream_was_live` を書く側と、`dead_runs` を 0 に戻す側が未テストだった。**
        使っていた偽ストリームが**原理的に live になれない**ため、
        **どちらの行を消してもテストは緑のまま**だった → **音を運ぶ偽ストリーム**を足し、
        画面側のテストを **4本 → 7本**にした
      - **「判定を1回だけにした」ことを守るテストが無かった**（境界の1実行を見ていなかった）
        → **毎実行、文言とウィジェットが食い違わないことを検査する**テストを追加
      - **「会話を終えたら忘れる」テストが `dead_runs` しか見ていなかった** →
        **`stream_was_live` も見る**（前の端末についての判定を次の会話へ持ち込まないため）
      - **ドキュメントの3点を修正**：①「2回消費する」は**ライブラリを読んで数えた下限**であって
        ブラウザで数えた値ではない、と明記 ②ja が**画面に存在しない日本語の文言**を引用していた
        （実物は英語）③「嵐は自力で止まらない」は**「許可しない」の場合の話**で、
        **ネットワーク起因なら再実行が起きるので自力で効く**——原因を区別せずに書いていた
      - **トレードオフも書いた**：一度 live になったら諦めないので、
        **会話の途中で許可を取り消されると嵐が戻る。**受け入れる（起きにくく、録音ボタンは残る）

      **わざと壊して落ちることを再確認**：`stream_was_live` の書き込み／`dead_runs` のリセット／
      `SESSION_KEYS` の2キー／判定の2回評価、**すべて落ちる。**
      `ruff` / `mypy` / `pytest`（**368件**）通過。
      **実機では未確認**——マイクを人が「許可しない」で拒否した場合の挙動は、A の実機確認で見る
- [-] 直らない場合 → **Manage app のログを読む**（赤い画面は理由を伏せている）
      → **直ったので不要**
- [-] ログが依存の解決で落ちていたら → `requirements-no-retrieval.txt` へ差し替え
      → **起動しているので不要。**ただし**根拠が出ない**件は残っている（上）

## B. 再発防止（0.5h）— **完了（9/2）**

- [x] `docs/ja/architecture.md` に節を足した（「デプロイの後に確かめること」）
      — push だけでは古いモジュールが残る。**別ファイルに定数や関数を足したら再起動する**
      — 再起動したら**URL を開いて1往復する**。「デプロイ成功」は「動いている」ではない
      — **赤い画面のメッセージは伏せられている。**理由は Manage app のログにしかない
- [x] `docs/en/architecture.md` に同じものを入れた（**コミットはまだ**）
- [x] **「テストでは防げない」ことも書いた。** `tests/test_screens.py` は `app/main.py` を丸ごと実行するので
      ローカルなら確実に落ちる。**本番で古いのはファイルではなくメモリ**である

## C. README の仕上げ（1.5h）

- [x] **スクリーンショットを撮った**（ローカル、9/2。上の「変更点2」の方法）
      — 撮ったのは**振り返りの画面**。訂正と英語の理由が1枚に入る（**条番号は出なかった**）
- [x] **英語の注釈を3つだけ**入れた
      — 訂正は会話中に出ない／理由は英語／**正しい文は正しいと言う**（3つ目を差し替え。上の「変更点3」）
- [x] `docs/images/review.png` に置き、README の「What it does」の直後から参照した
- [ ] **アーキ図は新しく描かない。** 339 行目の mermaid で DoD を満たすと判断済み（design.md §3）
- [x] **Progress の `A deployed demo carrying the current code` にチェックを入れた**
      — **A の本体（本番で1往復＋振り返り）が通ってから入れた**
- [~] **公開 URL のリンク**（**未決。ただし判断材料は 9/2 に揃えた。決めるのは Jun**）

  **事実**：
  - **アクセスコードは実装されている**（`app/limits.py:153` `code_matches`、`app/main.py:908` で門を出す）。
    **本番が素通りなのは、デプロイ側の secrets に `ACCESS_CODE` が設定されていないから**である。
    コードの問題ではなく**設定の問題**なので、Manage app の Settings → Secrets に1行入れれば効く
  - **1日の上限は効いている**：トークン 200,000／音声 50,000字（`app/limits.py:36-37`）。
    ただし**数え方はプロセスのメモリ**（`_used`）なので、**寝て起きるたびに 0 に戻る。**
    「1日 200,000 で頭打ち」ではなく「**1プロセスあたり 1日 200,000**」である
  - 数えているのは**入力の推定トークンだけ**（`estimate_tokens`）。出力は数えていない

  **推奨**：**`ACCESS_CODE` を設定したうえで、README に URL とコードの両方を書く。**
  - 面接官に「メールで問い合わせてください」と言わせた時点で、**その URL は押されない**
  - 門の実際の役目は**人を止めることではなく、クローラと通りすがりを止めること**である。
    README に書いてあるコードでも、それは果たせる
  - **応募の前と 9/13 の朝は、自分で開いて温める**（寝ていると最初の来訪者が4分待つ）

## D. GitHub Bio / Pin（0.5h）

- [x] Bio の文案を**3つ**出した（9/2。**GitHub の Bio は160字まで**。括弧内は実際の字数）

  1. **数字を先に置く案**（151字）
     `Cut over-correction from ~70% to 23% on a held-out split. I build LLM features with the evaluation written before the feature. Python / LangGraph / RAG`
  2. **プロダクトを先に置く案**（148字）
     `Japanese speaking coach for absolute beginners: 120-item evaluation set, held-out split, baseline measured first. Python, LangGraph, RAG. Bangalore.`
  3. **姿勢を先に置く案**（149字）
     `I publish what I measured and what I did not. Building AI language-practice tools in Python (LangGraph, RAG, FastAPI, Streamlit). Based in Bangalore.`

  **推奨は1。** 求人票が拾う語（LLM / LangGraph / RAG / Python）が入っていて、
  **なお最初の12語が「何を測ってどれだけ動かしたか」になっている**唯一の案である。
  2は読み手が日本語学習に興味がないと刺さらない。3は正しいが、**誰でも言える。**
- [ ] Jun が選んで更新（アカウント作業）
- [ ] Pin をこのリポジトリ先頭に並べ替え

## E. 面接準備（4.0h）

- [x] `interview-stories/demo-script-en.md` — **草稿（9/2）**
      顧客課題 → 既存サービスとの差分 → 評価の作り方 → 比較表 → **降ろした判断**
      — **9/3 に口語へ書き換えた**（`check-english-script`）。受動態とセミコロンを落とし、
        短縮形を入れ、④の対比の前で文を切った。**380語 ≒ 2分35秒**。
        2分に詰めるなら②（競合）を落とす、と本文に書いた
- [x] `interview-stories/questions-en.md` — **草稿（9/2）**。8問＋「聞かれたら困る質問」3つ
      なぜ音声か／誤りを種類分けしない理由／なぜ RAG か／評価の作り方／
      **なぜ応答時間を測っていないか**／CrewAI・ADK との対応／薄いラッパーでない理由／次にやること
      — **Q3 に「根拠が画面に出ていない」を書いた。** 直したらこの段落を書き直すこと
- [ ] **声に出して3回読む**（1.0h）← **Jun 本人。ここは縮まない**
- [-] 発信1本（LinkedIn / GDG）は §4 のまま**この作業単位に入れない**——9/6 の応募に要らない

## F. 未実装の2機能を決める（0.5h）— **次の作業単位へ送る（9/2）**

**理由**：残り枠が 10h しかなく、9/6 の応募に要らない。

**「一覧が古いかもしれない」という私の見立ては間違いだった（9/3 に確認）。**
本番にある **▶「もう一度」は、AI の返答をもう一度読み上げるボタン**であり、
設計書が言っている「もう一度」は**自分が言ったことを取り消して言い直す**道である。
**同じ名前の別機能**で、**設計書は古くなっていない**（2つとも未実装のまま）。
`docs/{ja,en}/functional-design.md` に**取り違えないよう1段落足した**——
**書いた本人が取り違えた**ので、読む人はもっと取り違える。
**作るなら名前を変えること。**

- [ ] **判断する。** 推奨は**両方降ろす**（design.md §6）
- [ ] 降ろすなら `docs/{ja,en}/functional-design.md` の記述と**画面遷移図の矢印**を消し、
      **「作らないと決めた」と理由つきで書く**——消すだけだと半年後に同じ議論をやり直す

## H. 監査（副エージェント4周、9/2〜9/3）

**3周目・4周目（マイクの対策）で計8点。**全部直した——**内容は A の項に書いた。**
**4周目で分かったのは「テストが本物か」を疑う価値**である。
**偽ストリームが原理的に live になれない**という一点で、対策の半分が無検査のまま緑だった。
回数が足りない／切断後も恒久停止／判定が1実行内でずれる／テストが数字を固定していない。
**このうち3つは、動くはずのマイクを止める向きの間違い**であり、
**副エージェントに読ませなければ 9/13 の会場で初めて分かった**種類のものだった。

**1周目（architecture.md の新節）で6点。**全部直した。
- 結論の一文が**冒頭の事実と正反対**だった（「本番は落ちなかった」→ 正しくは「テストは本番を守れない」）
- 見出しの日付が観測範囲を超えていた／`sys.modules` の推定を断定で書いていた
- `start_tts_cooldown` を「定数」と書いていた（**関数**）
- 「再起動では直らない」という未確認の断定／ja・en の強調と主語のずれ

**2周目（README・画像・ja/en・規約）で5点。**全部直した。
- [x] **README のキャプションが画像と食い違っていた。** 「注釈以外はアプリの画面」と書いたが、
      画像には**2本目の区切り線と小見出し**という、アプリが描かないものが入っていた
      → 「**アプリの画面はボタンで終わる。それより下は README のために足した**」と書き直し、
      **再現画像である**（動いているページのマークアップと CSS から描いた）ことも明記した
- [x] **`.steering/.../requirements.md` が PLAN.md へマークダウンのリンクを張っていた。**
      `.steering/` は公開リポジトリに入り、PLAN.md は入らない → **リンク切れになる。**素のテキストに戻した
- [x] **ja/en が「上に置くと3ターン残る」と書いていたが、3ターンは最小再現アプリの数字。**
      実アプリは**2ターン**（`20260825-mobile-audio` の表）→ 両方直した
- [x] `docs/ja/architecture.md` の対応の無い `**` と、句点の抜けを直した
- [x] **ドキュメントが「音声が無い端末への案内を英語にした」と書いているのに、
      `app/main.py:535` は日本語のままだった** → **英語に直した**（コード側を直した）
      — 8/25 に英語へ直したものが、同じ日の別の作業で日本語に戻っていた。
      **日本語が聞き取れない人に向けた唯一の文が日本語**では、この文の存在意義が無い

**監査で「問題無し」と確認できたもの**：画像の alt テキスト／注釈3つと実装の一致／
Progress のチェックと README 他部分の整合／ja・en の同期／README が英語であること。

## I. 当日の手順（9/6 の応募・9/13 の会場）— **Jun 本人**

**この作業単位で分かったことのうち、当日やらないと意味が無いものだけを並べる。**

### 応募を送る前（9/6）

- [ ] **公開 URL を自分で開く。** 寝ていれば起こす。**9/2 は起こすのに約4分**かかった
      （**9/5 の再確認では「Zzzz」は出ず、約40秒で開始画面まで来た**。
      **寝る間隔は一定ではない**ので、毎回開くほかない）
- [ ] **1往復して振り返りまで出す。** 押した先が動くことを、送る前に自分の目で見る
- [ ] **`ACCESS_CODE` を決める。** いま**本番は誰でも開ける**（PLAN.md の DoD と §2-5 に反している）。
      設定するなら Manage app → Settings → Secrets に1行

### 会場に着く前（9/13）

- [ ] **家を出る前に URL を開いて温める。** 会場の電波で4分待つのは、その場では長すぎる
- [ ] **携帯で1往復してから配る**（自分の携帯で、会場の電波で）
- [ ] **鳴らない端末が続いたら `BROWSER_VOICE=0`。** 会場で触る唯一のつまみ
- [ ] **WebRTC が通らなかったら `CONTINUOUS_VOICE=0`。**
      **「押さずに」より「音声で練習できる」が上位**である

## G. 締め（0.5h）

- [x] `ruff` / `mypy` / `pytest` 通過（9/2。ruff clean・mypy 61ファイル・pytest 141件 green）
- [x] **降ろす順番の点検**（9/2 実施）——**降ろすものは出なかった。**
      9/20 DoD を1項目ずつ README・コード・実測と突き合わせた結果、
      **9/6 以降に残るのは次の4つだけ**である。

      1. **テスターの感想3件**（9/13。これは予定どおり）
      2. **GitHub Bio / Pin**（Jun の5分作業）
      3. **`ACCESS_CODE` がデプロイ側に設定されていない** ← **新しく見つかった。下記**
      4. **携帯実機での音声確認**（A に残っている4項目）

      **3 は PLAN.md との食い違いである。** DoD は「ライブデモ URL（**誰でも触れる状態にはしない**）」、
      §2-5 は「**アクセスは共有コード1本のまま**」と決めている。
      **いまの本番はコードを訊かない**＝この行を満たしていない。**コードは実装済みなので、
      デプロイの secrets に `ACCESS_CODE` を1行入れるだけで満たせる。**

      **あわせて注意**：§2-5 は「1セッションのターン上限と1日の利用上限」を**費用の歯止め**として残した。
      その歯止めは効いているが、**数え方はプロセスのメモリ**なので、
      **アプリが寝て起きるたびに 0 に戻る**（`app/limits.py:_used`）。
      「1日 200,000 トークンで頭打ち」ではなく「**1プロセスあたり1日 200,000**」である。
      **無料枠のまま置くなら、この差は知っておく必要がある。**

      **なお PLAN.md の DoD のチェックボックスは古い。**比較表・RAG・当たり率・FastAPI・
      LangGraph・docker・押さずに話す、はいずれも実装済みだが `[ ]` のままである。
      **PLAN.md は Jun の正本なので、こちらでは触っていない。**
- [ ] 実績工数の記入 ← **Jun 本人が記入。推測で書かない**

## 緩衝（**使わずに済めば使わない**）

- [ ] **実機確認のやり直し**（3.0h）——A の①〜④のどれかが通らなかった場合
      — 直らなければ **`CONTINUOUS_VOICE=0` で押して話す形に戻す。**
        **9/13 は「押さずに」より「音声で練習できる」が上位である**

---

## 完了条件（要求書の受け入れ条件に対応）

1. [x] 公開 URL が `main` と同じコードで開始画面まで描画される（9/2 確認）
2. [x] 公開 URL で**1往復**でき、振り返りまで通る（9/2 確認）
3. [x] `docs/{ja,en}/architecture.md` にデプロイ後の確認手順がある（**コミット待ち**）
4. [x] README に**英語注釈つきスクショ1枚**がある（**コミット待ち**）
5. [~] README の Progress のチェックが**実態に合っている**
   — デプロイの1行は入れた。**`Reference:` が出ない件が README の RAG の記述に当たるかは調査中**
6. [ ] GitHub の Bio と Pin が更新されている
7. [ ] 英語2分台本と想定質問集があり、**3回読んである**
8. [ ] `ruff` / `mypy` / `pytest` 通過。**ドキュメントは ja と en が同じコミットに入っている**
9. [ ] **9/6 時点で、9/20 DoD の残りが「テスターの感想3件」だけ**になっている

---

## 対象外（今回やらない）

- 訂正の品質・プロンプト・閾値・モデルの変更（**README の公開数字が実装を説明しなくなる**）
- 応答時間の再測定（8/22 の数字を使う）
- 音声段の精度測定・Docker の作り直し（PLAN.md で降ろし済み／検証済み）
- 発音採点・ローカルモデル・React 画面・DSPy / scikit-learn / AWS（PLAN.md「9/20 までにやらないこと」）

---

## 記録

| 項目 | 値 |
|---|---|
| 予定工数 | **11.5h**（緩衝 3.0h を除く。枠 22h に対して 52%）／**9/2 以降の残り枠は 10h** |
| 実績工数 | ← **Jun 本人が記入** |
