# 第2週 タスクリスト

対応する設計: [design.md](design.md) ／ 期間: **2026-08-10（月）〜 08-16（日）**

**週は月曜始まり。今週は Day 0 に当たる日が無く、Day 1 が 8/10（月）。**

### 稼働の前提（第1週から変えない）

**平日は2時間、休日は8時間。金曜も平日なので2時間。この枠を超える計画を書かない。**
**枠を広げて辻褄を合わせると、遅れが「見積り違い」ではなく「無理な計画」として積み上がる。**

| 日 | 見積 | 行ごとの合計 |
|---|---|---|
| Day 1（8/10 月） | 2h | 1.5 ＋ 0.25 ＋ 0.25 |
| Day 2（8/11 火） | 2h | 0.25 ＋ 0.25 ＋ 0.25 ＋ 1.0 ＋ 0.25 |
| Day 3（8/12 水） | 2h | 1.0 ＋ 0.25 ＋ 0.25 ＋ 0.25 ＋ 0.25 |
| Day 4（8/13 木） | 2h | 0.75 ＋ 0.25 ＋ 0.5 ＋ 0.5 |
| Day 5（8/14 金） | 2h | 0.75 ＋ 0.5 ＋ 0.25 ＋ 0.5 |
| Day 6（8/15 土） | 8h | 3.0 ＋ 0.5 ＋ 0.25 ＋ 0.25 ＋ 1.5 ＋ 0.5 ＋ 0.25 ＋ 0.25 ＋ 0.5 ＋ 予備 1.0 |
| Day 7（8/16 日） | 8h | 2.0 ＋ 0.5 ＋ 2.0 ＋ 0.5 ＋ 条件つき 1.5 ＋ 予備 1.5 |
| **合計** | **26h** | |

進捗記号: `[ ]` 未着手 ／ `[~]` 着手中 ／ `[x]` 完了 ／ `[-]` 降ろした

> **第1週の教訓を3つ持ち込む。**
> ①**予備枠を作業で埋めない**（余ったら余ったまま週を閉じる）。
> ②**当初のタスクに無い作業が出たら、行を足して「当初のタスクには無い」と書く。**
> ③**規律ではなく仕組みで閉じる。** 「読んでも使わない」で守るものは、次に必ず破られる。

> **見積りの無い作業を置かない。** 第1週の監査で「未見積りのタスクが枠を超えている」という
> 指摘を受け、直したところ**いちばん枠の細い日に同じ破綻が移った。**
> **対処が新しい誤りを作る**の典型なので、**全部の行に時間を付けてある。**

---

## Day 1（8/10 月）— ステアリングと、測定の前提を整える（2h）

**初日に実装を積まない。** 枠の 1.5h がステアリング3点で消える。
**残りは「測定より前でなければ意味が無い」2件だけ。測定そのものは Day 2 の先頭へ送る。**

> **第1週 tasklist は同じ件を2箇所で「朝2h」と「1.5h」と書いている。1.5h を採る。**
> **正式測定を Day 2 に送っても順序の条件は崩れない**——守るべきは「日付が 8/10」ではなく
> 「**検証ノードのコードを1行でも書く前**」であり、実装が始まるのは Day 4 である。

- [x] **eval-090 を裁定する**（0.25h）← **今日の先頭。** 第1週から保留されていた件
      — **結論：現状のまま残す。`items.json` は1文字も変えていない**（`items_digest` は `6444e2bec265`）
      — **指摘は成立しなかった。** `thanks` は**段階A ＝ 丁寧形を要求しない**ので、
        丁寧さの混在それ自体を理由に直すと **glossary §2 の下限より厳しい基準をこの1件にだけ当てる**ことになる。
        代案2つ（`ございます` に揃える／普通形に揃える）はそれぞれ
        **「直しだけ敬語に上げる」型**と**eval-091 との重複**を作るので捨てた。詳細は [design.md](design.md)
      — **以後 `test` の項目を1件も変更しない**
- [x] **`test` の実行で per-item を一切出さないようにする**（0.25h）← **測定より前**
      — 現状の `_print_manual_check` は**分割を見ずに**正解ラベル・システムの判定・理由・自分の正解を印字する。
        そのまま走らせると **`test` 40件のうち5件（12.5%）の当たり外れが確定的に分かる**
      — **「学習者文と直しだけ出す」では足りない。** 手照合の目的は採点器の検査で、
        それに要るのは `expected` と `predicted` の突き合わせ＝まさに伏せたいものである。
        **目的のほうを Day 2 の `dev` 実行へ移す**
      — pytest で固定する（**綴りの検査ではなく、`test` の実行で per-item の行が1つも出ないこと**）
- [x] ステアリング3点（1.5h）— `requirements.md` / `design.md` / `tasklist.md`
      （**1ファイルごとに承認**／CLAUDE.md）— 独立監査2周を当てて書き直した

### 当初のタスクには無い（8/11 に必要になった）

- [x] **`--scorer-checked-on` を `test` で必須にした** — 指し示す `dev` 実行が無ければ `test` は測れない。
      **「読んでも使わない」という規律ではなく経路を閉じる**
- [x] **`--limit` を足した（`dev` 専用）** — 「`dev` を数件回す」が設計に書いてあるのに、
      **スクリプトは分割の全件しか回せなかった**。`test` に渡すと拒否する
      ——部分的な `test` 記録は `evals/runs/` の中で完全な記録と見分けがつかない
- [x] **変異テストで、塞いだ2箇所を実際に壊して落ちることを確かめた**
      — **伏せ字化は落ちた。ガードのほうは落ちず、300秒でタイムアウトした**
      — 原因は**自分のテストの欠陥**で、ガードを外すと `main()` が本物の呼び出しに進み
        `test` 40件を叩き始めた。**失敗の出方が「赤」ではなく「課金を伴う停止」**だった。
        存在しない `--items` を渡す形に直し、**ガードがデータを開くより前に効くことも同時に縛った**
      — **対処が新しい誤りを作る、が今回は「塞げたことを確かめる側」に出た**（[design.md](design.md)）
- [x] **既存2指標の回帰 pytest を Day 3 から前倒しした** — `run_record` に手を入れる前に置いた。
      **触る前に置かなければ「変えていない」は確かめずに主張することになる**
- [x] **`items_digest` と `scorer_checked_on` の実装を Day 4 から前倒しした** — 8/11 の `test` 記録に要る。
      tasklist は「digest は手で書く」としていたが、**実装しても3行**で、手で書くほうが間違えうる

### 第二採点者（枠外・数分）

- [ ] **返信が来ているか確認し、来ていなければ督促する** ← **Jun 本人**
      — 本文は [20260808-2041-rater-kit-whatsapp.txt](../../evals/rater/20260808-2041-rater-kit-whatsapp.txt)、期限は `Sunday 16 August`
      — **8/13（水）までに返信が無ければ2人目へ出す**（日本語ネイティブ3名の連絡先がある）

---

## Day 2（8/11 火）— 正式測定と、ベースラインの誤り分析（2h）

- [ ] **`dev` を数件、同一 `scorer_version` で回して手照合する**（0.25h）← **`test` の測定より前**
      — **採点スクリプトの不具合とモデルの不出来を切り分ける工程は、ここで完結させる。**
        `dev` なら何を見てもよい
      — この実行の `run_id` を、次の `test` 実行の `scorer_checked_on` に書く
- [ ] **`test` 40件でベースラインを測る**（0.25h）← **検証ノードのコードを1行でも書く前に**
      — 実行記録に `n=40` / `split=test` / `scorer_version` / `model` / `prompt_version` / `scorer_checked_on`
      — **`items_digest` は `shasum -a 256 data/evaluation/items.json` を1回叩いて手で書く。**
        自動化は Day 4（**測定を待たせない**）
      — **per-item の行が記録にもコンソールにも出ていないことを確認する**
      — **この数字が README の表Aに載る**
- [ ] **`dev` 80件でベースラインを走らせる**（0.25h）
      — 8/8 の記録（[20260808-2041-baseline-dev.json](../../evals/runs/20260808-2041-baseline-dev.json)）は
        **当時の `dev` 全件（40件）**であって、いまの80件ではない。**流用しない**
        （「採点キットを選ぶための40件」と読むのは誤り。当時の `dev` がちょうど40件だった）
      — **`dev` なので per-item の行を残してよい**
      — **per-item に `japanese_ratio`（割合そのもの）を出す。** 真偽値だけでは次の行が判断できない
- [ ] **外した件を全部読む**（1.0h）— **「眺める」ではなく「数える」**
      — 誤って直しに行った件／見逃した件を、それぞれ**型で数える**
      — **型はここで数えるだけで、ラベルには足さない**（glossary §2）
      — **「検証ノードで拾えるもの」と「プロンプトで直すもの」を分ける。**
        検証は確定処理なので、**確定的に判定できる型にしか効かない**
      — 分かったことを [design.md](design.md) に書く
- [ ] **`reason_language` 閾値 0.25 を `dev` で再確認する**（0.25h）
      — **glossary §7 が第2週の宿題として名指ししている**（`thresholds.toml` に TODO が無く追いにくい）
      — 据え置くなら「再確認した。据え置き」と §7 に書く。**確認した事実を残さないと次も宿題のまま**

---

## Day 3（8/12 水）— 形態素解析と、素の遵守率（2h）

- [ ] `nlp/` を作る（1.0h）— SudachiPy ＋ SudachiDict
      — **内容語だけを数える**（`thresholds.toml` の `content_pos`）。助詞・助動詞は必ず高頻度なので、
        混ぜると比率がゼロに寄り、**難しすぎる返答が検査を通る**
      — **固有名詞は除外**（`exempt_pos`）
- [ ] `nlp.level_check(reply, level)` の pytest（0.25h）
- [ ] **固定台本を書く**（0.25h）— 6場面 × 5ターン ＝ 30往復
      — **評価データを一切使わず新規に書く。** `items.json` から採ると
        **`test` の文を会話生成に通す**ことになる
      — `SCENE_BRIEFS` の開始文から自然に続く形にする
- [ ] **「1発目の遵守率」を測る**（0.25h）— **閾値を触る前に測る**
      — 台本 × 3レベル ＝ **90返答**。**再生成はまだ入っていない**（②は Day 4）ので、
        ここで測れるのは**ゲートを通す前の素の品質**であり、**これが README の主値になる**
      — `temperature = 0.7` なので返答は固定されない。**今週は1試行**とし、
        README に「n=90、1試行、temperature 0.7」と書く
      — **記録欄は「1発目」と「再生成後」を2行に分ける**
      — **不合格率が 30% を超えたら、閾値ではなく会話プロンプトを直す**（`thresholds.toml` の判断）
- [ ] **既存2指標の回帰 pytest**（0.25h）← **指標を足す前に置く**
      — 固定の `Outcome` 一式を JSON で保存し、`run_record()` に通して
        `detection_accuracy` / `over_correction_rate` / `format_compliance_rate` が一致することを確かめる
      — **「既存の計算は変えない」と規則を4回書いても、確かめる手段が無ければ守られない**

---

## Day 4（8/13 木）— 検証①②と、実行記録の項目（2h）

- [ ] `correction/validation.py` — **検証①（書き換えすぎ）**（0.75h）
      — 正規化編集距離が閾値超え → **訂正を捨て `needs_correction: false` で返す**
      — **`needs_correction: true` のまま返すと、学習者に何も表示されないのに検出率だけ高くなる**。
        `corrected_sentence` が空の `true` は `parse_correction` が禁じている形でもある
      — **`delivered_detection_accuracy` は足さない。** `false` で返すと決めた時点で
        `detection_accuracy` と**恒等的に一致する**（README に同一の2列が並ぶだけ）。
        同じ情報は**段を分けた測定（①→②）が持っている**
      — **後処理として書く**（LLM の出力に当てるだけ）。これで Day 6 に
        「検証を当てた場合と当てなかった場合」を**追加の API ゼロ**で両方作れる
- [ ] **検証②（レベル超過）** — `dialogue.reply()` に再生成を1回入れる（0.25h）
- [ ] **実行記録の項目を足す**（0.5h）
      — `items_digest` / `thresholds_digest` / `validation_fired` / `discarded` / `latency_ms` / `scorer_checked_on`
      — **`validation_fired` は①③を `true` / `false` 別に数える。**
        **②はここに載らない**——`dialogue.reply()` の中で起き、評価スクリプトは
        `correction.check()` しか呼ばないので、**訂正の記録に置くと②は構造上つねに 0 になる**。
        ②は90返答の測定記録に入れる
      — **既存2件の記録への後付けは、今日のハッシュを書かない。**
        両方とも当時は**全60件（dev 40 / test 20）**で、正しい値は **`55581252c8c9`**
        （コミット `7857c94`。120件版が入ったのは `4c56dac` で両記録より後）。
        現在の `6444e2bec265` を書けば**再現性を担保する欄が再現性の虚偽記載になる**。
        `items_note` に「現在の120件とは別物」と1行残す
      — **既存2指標の回帰 pytest（Day 3）が緑のままであることを確認する**
- [ ] **`rewrite_too_far` の閾値を `dev` で暫定確定する**（0.5h）
      — **`dev` の `true` 件に訂正を流し、捨てられる正しい訂正を数える。1件でも捨てられたら閾値を上げる**
      — 捨てた件は id・元文・訂正・距離・**自分の可否判定**を JSON に残す。
        **目視の判定を残さないと、閾値の根拠が「そのとき見た印象」になる**
      — **これは暫定。** 今日流すのはベースラインの訂正で、閾値が掛かるのは本実装の訂正。
        **8/15 に本実装①の出力で再確認する**（後処理なので追加の API は要らない）
      — `thresholds.toml` の `TODO(week2)` を消し、glossary §7 に書き戻す（**ja → en を同じコミットに**）

---

## Day 5（8/14 金）— 引用を閉じる（2h）

**Chroma に載せる前日。** 載せてから見つけると `retrieval_hit_rate` を測り直しになる。

- [ ] **`data/grammar/` × 評価データ120件全体の引用検査 pytest**（0.75h）
      — **走査対象は `data/grammar/` 全体 × 120件全体。** grammar-008 だけを想定すると
        **残り2本を通したまま緑になる**（grammar-004 が eval-040 の直した文、
        grammar-007 が eval-060 の学習者文を引用している）
      — 見るのは `learner_sentence` / `corrected_sentence` / `reason_en` の3つ
      — **`test` の一致はハード失敗。`dev` の一致は「整数の件数」ではなく `id` の許可リストで固定する**
        ——整数だと、リファレンスを増やして新しい一致が出たとき**数字を1つ増やせば緑に戻り、
        diff は1文字しか出ない**。id なら追加が1行として見え、そこに理由を書ける。
        **`test` 由来の id は許可リストに入れられないことをテスト側で強制する**
      — **照合は `。、？！「」` を落としてから**（第1週に `はじめまして` と `はじめまして。` が素通りした）
      — **件数は文書の固定値を信じず、機械で数え直す。** 第1週の「完全一致17件」は
        **内訳を足すと 17 にならず、8/9 の書き換え後の再計算もしていない**。
        **2026-08-10 の実測は `dev` 16件（`true` 12 / `false` 4）／ `test` 8件**
- [ ] **`test` 側の引用を直す**（0.5h）— **リファレンス側を書き換える。`items.json` は触らない**
      — **差し替え先は `dev` の現象から採るか、評価データを見ずに書く。**
        「同じ現象をなお説明できるように」`test` を見ながら選ぶと、
        **検索コーパスを `test` に合わせて整形した**ことになり、第4週の再測定に効く
- [ ] **「このままでよい」の根拠を守る**（0.25h）
      — 第1週の監査2周目が足した**6箇所**（grammar-002・003・005・006・007・008）。
        そのうち **grammar-007 の「少し考えます」は eval-060（`test`）の学習者文＝必ず消える**
      — **本数を数えるだけでは足りない。** 別の文に替えて本数を保っても、扱う現象が変われば
        検証③の根拠にならない。**前後で「どの現象を扱っているか」を1語ずつ書き添えて比較する**
- [ ] **`retrieval_hit_rate` の30件を選ぶ**（0.5h）
      — **`dev` の一致件（8/10 時点で16件）を機械で除外してから選ぶ。**
        `dev` の `true` は63件・`false` は17件あるので、除いても足りる
      — **内訳を先に固定する：`true` 20 / `false` 10。**
        **`false` を入れないと、検証③が依存する「このままでよい側の根拠が引けるか」が一度も測られない**
      — **30件を 10件（閾値決定用）と 20件（測定用）に分ける**
      — **除外の向きは保守側**（一致件は当てやすいので、除くと当たり率は下がる）。
        ただし**除外した16件は定型句に集中している**ので、
        **当たり率はその話題について何も言わない**と README に1行書く
- [ ] **文法リファレンスを 8本 → 10本は、今日はやらない。** Day 7 の予備枠へ（降ろす順番の2番）

---

## Day 6（8/15 土）— RAG・段ごとの測定・比較表（8h）

### RAG（3.0h）

- [ ] リファレンスを**節ごとに割って**取り込む（1本まるごと1チャンクにしない）
- [ ] sentence-transformers の多言語モデルで埋め込み → Chroma に登録（**索引は起動時に作る**）
- [ ] `retrieval.search(sentence)` → 上位3件と id と**類似度**を返す
- [ ] **`grounding_ids` を検索結果由来にする**（現状はモデルの JSON 出力を拾っている）

### `[retrieval] score_min` を決める（0.5h）

- [ ] **比較表の指標（`detection_accuracy` / `over_correction_rate`）を見て決めない**
      — この閾値は検証③の発火を通じて**両指標を直接動かすつまみ**である
      — **「決めた回の `retrieval_hit_rate` は公表しない」では守れない。**
        `retrieval_hit_rate` は**順位の指標**で、採否の下限である `score_min` に対して不変
- [ ] **決定基準は検索単体の性質**：Day 5 の**閾値決定用10件**について、
      手で付けた正解 id が `score_min` 以上で残る割合（再現率）で決める
- [ ] 値と、それを決めたデータと量を `thresholds.toml` にコメントで残す（既存3つと同じ書式）

### 依存の疎通確認（0.25h）← **判定を今日のうちに済ませる**

- [ ] **依存だけ先に Community Cloud へ上げ、起動するか確かめる**
      — 多言語埋め込みモデル（数百MB）＋ SudachiDict（約70MB）が無料枠に載るか**未確認**
      — **8/16 に初めて試して落ちると、切り替える時間が残らない**
      — 落ちたら**公開版だけ RAG 無し**にすると決めて、下の予備 1h で切り替える。
        **測定は手元で回すので数字は1つも失われない**

### `retrieval_hit_rate`（0.25h）

- [ ] **Day 5 の測定用20件で測る**（`n=20`）— **正解 id は先に付けてある。検索は今日はじめて回す**
- [ ] 測り方を glossary §5 に書き戻す（件の選び方・除外・内訳・誤差の目安。**ja → en**）

### 段ごとの測定（1.5h ＋ 後処理 0.5h）

- [ ] **ベースラインを `dev` 80件で2回続けて回す**（表Bの左列＋再現幅）
      — **同日2回の差 ＝ 測定の再現幅**（README に書ける）
      — **8/11 と 8/15 の差 ＝ 4日間のドリフト**（プロバイダ側の変化を含む）。**別の量として書く**
      — **この2つを1語で「再現幅」と呼ばない**
- [ ] **⓪ 本実装プロンプト・RAG なし・検証なし**（80件）← **プロンプトの差**
- [ ] **① ＋RAG・検証なし**（80件）← **RAG の効果。これが無いと今週の3本柱の2つ目について表が何も言わない**
- [ ] **② ＋検証① ／ ③ ＋検証③** — **①の出力に後処理を当てるだけ。追加の API 呼び出しはゼロ**
      — **段の差にサンプリングの揺れが1ミリも混ざらない**
- [ ] **`rewrite_too_far` を本実装①の出力で再確認する**（0.25h・Day 4 は暫定）
- [ ] **`dev` から5件を手で照合**（毎回の工程）

### 検証③の採否を判定する（0.25h）

- [ ] **事前登録した基準で判定する**：`dev` で **`false` 側の発火率が `true` 側より 20pt 以上高いこと**
      — **基準は先に書いてある。結果を見てから基準を決めない**
      — 述語は**段階A の場面は無条件に通し、段階B・C だけ文末を見る**形
        （段階は `POLITENESS_FLOORS` のタプルの**第1要素だけ**。**評価項目を1つも引用していない**）
      — **`politeness_rule()` と `POLITENESS_ADJUDICATIONS` を `correction/` から import しない**
        （eval-081 は `test`、eval-017・eval-022 の正解を実例として引用している）。**pytest で縛る**
      — **満たさなければ採用しない。測定結果を README に書く**
        ——「実装できなかった」ではなく「**当てたら判別力が無かった**」と書ける
      — 参考（2026-08-10 の実測）：素朴な文末判定は **`false` 30.0% 対 `true` 37.8%＝向きが逆**。
        場面の段階を見る版で **93.3% 対 81.1%（+12.2pt）**。**+12.2pt は主力になる数字ではない**ので、
        判別の主力は「根拠が空」であることを README でもそう説明する

### 比較表（0.5h）

- [ ] **表B** — ベースライン ⓪ ① ② ③ を並べる。**すべて `dev` 80件・同じ日**
      — **指標ごとの分母と誤差を表の中に**：`detection` 63 ／ `over_correction` **17（1件 5.9pt）**
      — **「本実装は `dev` で調整した側の数字であり、差は上振れする」と表の中に明記する**
      — **表A（`test` 40・ベースラインのみ）と足し合わせない**
- [ ] 実行記録を全部 `evals/runs/` に残す（`run_id` を README から参照できるように）

### 予備（1.0h）

- [ ] **RAG 無し版への切り替え**（疎通確認で落ちた場合）
- [ ] **測定が枠に入らない場合、落とす順は ③ → ⓪ → 同日2回目のベースライン。**
      **表Bそのものは落とさない**（今週の最低ライン）
- [ ] 消化しなければそのまま残す。**予備枠を作業で埋めない**

---

## Day 7（8/16 日）— 改善サイクル・公開・README・週の締め（8h）

### 改善サイクル1周目（2.0h）

- [ ] **一番低い指標を1つ選ぶ** — 8/15 の測定結果で決める。**予想で先に決めない**
- [ ] **変えるのは1つだけ**
- [ ] **プロンプトを変えたら `PROMPT_VERSION` を上げる。閾値を変えたら `thresholds_digest` が動く**
      — どちらも動かないと、**実行記録を2つ並べても何を変えたのかが復元できない**
- [ ] **前後を同じ `scorer_version`・同じ分割（`dev`）・同じモデルで測る**
- [ ] **悪化しても記録する**

### 公開 URL（0.5h）

- [ ] https://nihongo-coach.streamlit.app/ を更新し、**他人が触れることを実機で確認する**
      — 依存の可否は 8/15 に判定済み。**今日やるのは反映と確認だけ**
      — **起動ファイルは Settings から変更できない**（第1週 Day 1 の実測）。作り直すと URL を取り直しになる

### README（2.0h）

- [ ] **表A**（`test` 40・ベースラインのみ・分母 27/13・誤差 ±13pt / ±19〜27pt）
- [ ] **表B**（`dev` 80・段5つ・**分母 63/17・1件 5.9pt**・調整に使った側である旨）
      — **差が2件以内なら「この差は分母17件で2件ぶんであり、誤差の範囲を出ない」と本文に書く**
- [ ] **測定の再現幅（同日2回）と、4日間のドリフト（8/11 vs 8/15）を別の量として書く**
- [ ] **改善サイクル1周**（何を変えたらどの数字がどう動いたか・`run_id` 2つ）
- [ ] **`retrieval_hit_rate` の但し書き**（`n=20`／除外した16件は定型句が中心で、
      **この当たり率は定型句については何も言っていない**／近似の別集計をやったかやめたか）
- [ ] **遵守率は2行**：**1発目（主値・目標を掛けない）**と**再生成後（目標90%はこちら）**。
      **「再生成後の値は同じ関数でゲートした結果なので、独立の証拠ではない」と併記する**
- [ ] **検証③を見送った場合は、その測定結果を書く**（発火率の `true` / `false` 別）
- [ ] **発火0件のノードがあれば、その旨を書く。** それも測定結果である
- [ ] **測っていない型の但し書き**（`test` 27件は助詞3件・直訳5件で、
      **助数詞・条件・複文の接続・数量と時間の語法・受身・使役の6つ**は1件（3.7pt）でしか見ていない）
- [ ] Progress の checkbox を実態に合わせる（120件・ベースラインは済んでいる）
- [ ] **README は3面目のドキュメントである**。glossary を直したら README も見る
- [ ] `docs/{ja,en}/repository-structure.md` の `nlp/` `retrieval/` の *(planned)* を外す（**必達・5分**）

### 週の締め（0.5h）

- [ ] **「降ろす順番」の点検**（PLAN.md §3 は 8/16 を点検日に指定している）
      — **番号順に機械適用しない。** 2・3・4・5 で足りるかを先に見る
      — **5番に1行入れる**——第1週側の表には「第2週が最後の分岐」が残るので、
        そこだけ読むと分岐が閉じたように見える
      — 現在地を [design.md](design.md) に書く
- [ ] **記録欄を埋める（実績工数を含む）** ← **Jun 本人が記入**
- [ ] 第3週へ引き継ぐ確定事項を書く

### 条件つき：`rater_agreement`（返信が届いていれば 1.5h・予備枠より優先）

- [ ] 採点結果を JSON にして `evals/rater/` に保存し、**`rater_agreement` を計算する**（0.5h）
      — 手順は [day7-minna-shuugou.md](../20260802-week1-baseline/day7-minna-shuugou.md) §5 のまま
      — **返信が複数来ていたら、最初に返ってきた1名を正とし、残りは参考値として別に書く。**
        **採点者を選べる状態にすると、一致率は「一番近い人を選んだ数字」になる**
      — **どのキットの一致率かを `kit_id` で残す**（2人が別のキットを採点しうる）
- [ ] **一致しなかった件を読み、尺度のどこで解釈がずれたかを glossary §6 に書き戻す**（1.0h・**ja → en**）
      — **尺度を先に直したこと（8/9 の規約2件）を併記する。**
        書かないと、あとで高い一致率を「尺度がよくできていた」と読んでしまう

### 予備（1.5h。返信が来なければ 3.0h になる）

**返信が来なければ Day 7 は 5.0h の作業で閉じる。** その 3.0h を望ましい枠に使ってよいが、
**使い切る必要は無い。余ったら余ったまま週を閉じる**（第1週と同じ扱い）。

- [ ] 文法リファレンスを **8本 → 10本**（降ろす順番の2番。**4番より先に発動する**）
      — **1話題に複数本が当たるように書く。** 複数本が当たっているのは
        「を／が」「ています」「丁寧さ」「定型句」の**4系統だけ**で、残りは実質1話題1本。
        **足す2本は、1話題1本の系統に2本目を当てる方向にする**
      — **例文を評価データから採らない。** 追加後に Day 5 の引用検査 pytest をもう一度回す
        ——**対処が新しい誤りを作る**（第1週の教訓）
- [ ] `docs/{ja,en}/architecture.md` を**増補**（**1ファイルごとに承認**／**ja → en を同じコミットに**）
      — **レイテンシとコストは書かない。** 今週あるのは生の経過時間であって、
        公開できる形に集計した値ではない。集計と前後比較は第4週
- [ ] `docs/{ja,en}/functional-design.md` に検証ノード・`grounding_ids`・`discarded` を追記
- [ ] 1ターンあたりのトークン数を実行記録に足す

---

## 完了条件

- [ ] **eval-090 の裁定が済み、以後 `test` の項目を1件も変更していない**
- [ ] **`test` 40件のベースライン正式値が実行記録つきで出ている**
      — per-item が記録にもコンソールにも出ておらず、`items_digest` と `scorer_checked_on` がある
- [ ] **検証①②が動き、①は訂正の記録に、②は90返答の記録に発火件数がある**
- [ ] **検証③の採否が、事前登録した基準で判定されている**（見送った場合は測定結果が README にある）
- [ ] **閾値の宿題が4件とも片付いている**（`rewrite_too_far` ／ `vocabulary_level` ／
      `reason_language` の再確認 ／ `[retrieval] score_min`）。glossary §7 が更新されている
- [ ] **`data/grammar/` × 120件の引用検査 pytest が緑**（`test` はハード失敗、`dev` は id の許可リスト）
      — **Chroma に載せる前に通したこと**
- [ ] **`retrieval_hit_rate`（`n=20`）と遵守率2値が測れている**（測り方が glossary §5 にある）
- [ ] **比較表が README にある** — 同じ日・同じ分割・段5つ・**分母と誤差が表の中にある**
- [ ] **改善サイクル1周が README にある**（`run_id` 2つ・変えたものが記録から復元できる）
- [ ] **公開 URL で他人が触れる**
- [ ] **`rater_agreement` が出ている、または2人目以降への依頼が済んでいる**
- [ ] **`repository-structure.md` の *(planned)* が外れている**（ja / en）
- [ ] `ruff` / `mypy` / `pytest` 通過

> **PLAN.md の 8/16 最低ライン**：「ベースラインと本実装の比較表がある」。
> **ここが出ていれば、以降どこで遅れても価値はゼロにならない。**
> 逆に、これ以外の完了条件を全部満たして比較表だけ無い週は、**失敗である。**

---

## 第3週へ引き継ぐ確定事項

**第3週は 8/17（月）〜 8/23（日）**。ディレクトリは `.steering/20260817-week3-voice/`。

- **ステアリング3点は 8/17（月）の朝に作る。初日に実装を積まない**
- **`test` 40件は第4週まで触らない。項目の変更も禁止**（`items_digest` が動く）
- **LangGraph を載せるかの分岐はここ**（第1週 design.md の「第2週が最後の分岐」は今週撤回した）
- **検証③を見送った場合、第3週に持ち越すか本当に降ろすかをここで決める**
- **音声を載せる前に、第1週 Day 1 の罠に対処する**——**同じ返事＝同じ音声は2回目が鳴らない**。
  末尾に長さの違う無音を数十ミリ秒足す
- **テスターへの説明文に「マイクの許可を求められたら Allow を押す」を必ず入れる**
- **画面に「発音は見ていない」と1行書く**（第1週 design.md に文案あり）
- **RAG が公開版に載らなかった場合、FastAPI を Hugging Face Spaces へ出すときに載せ直す**
- **第2週の予備枠に入らなかった望ましい枠は、入らなかったものだけをここに転記する**

---

## 記録欄（週の終わりに埋める）

| 項目 | 結果 |
|---|---|
| eval-090 の裁定 | 　 |
| ベースライン `detection_accuracy`（`test` 40・正式） | 　 |
| ベースライン `over_correction_rate`（`test` 40・正式） | 　 |
| ベースライン `format_compliance_rate`（`test` 40・正式） | 　 |
| 測定の再現幅（8/15 の同日2回の差） | 　 |
| 4日間のドリフト（8/11 vs 8/15） | 　 |
| 段ごとの `detection_accuracy`（ベース／⓪／①／②／③） | 　 |
| 段ごとの `over_correction_rate`（同上・分母17） | 　 |
| 検証①の発火件数（`true` ／ `false`） | 　 |
| 検証②の発火件数（90返答） | 　 |
| 検証③の発火率（`true` ／ `false`）と**採否** | 　 |
| `retrieval_hit_rate`（`n=20`・一致件を除外） | 　 |
| 遵守率 — **1発目**（n=90・1試行） | 　 |
| 遵守率 — 再生成後（目標90%はこちら） | 　 |
| `rewrite_too_far` の確定値／捨てられた正しい訂正 | 　 |
| `[retrieval] score_min` の確定値／決定に使った再現率 | 　 |
| `data/grammar/` の一致件（掃除前 → 掃除後・`dev`／`test`） | 　 |
| 「このままでよい」が扱う現象（掃除前 → 掃除後） | 　 |
| 改善サイクル：変えたもの／動いた指標 | 　 |
| `rater_agreement`（`dev` 20件・`kit_id`） | 　 |
| 公開 URL で触れるか／RAG は載ったか | 　 |
| 「降ろす順番」の発動 | 　 |
| 実績工数 | 　時間（見積 26h）← **Jun 本人が記入** |

> **`実績工数` を機械が埋めない。** この欄は「見積りとどれだけずれたか」を残すためにあり、
> 見積りから推測して書いた瞬間に、見積り精度を上げるための欄が**見積りの写し**になる。
