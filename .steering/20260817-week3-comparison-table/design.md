# 第3週 設計 — Day 6・Day 7 の引き取りと、比較表（表B）の RAG 抜き版

期間: **2026-08-17（月）〜 08-23（日）**（実枠は 8/19〜8/23 の5日＝10h）
／ 要求: [requirements.md](requirements.md) ／ タスク: [tasklist.md](tasklist.md)
／ 前週: [第2週 design.md](../20260810-week2-validation/design.md)

---

## 1. この週が引き取るもの

**第2週（8/10〜8/16）の Day 6・Day 7 が丸ごと未着手のまま週が閉じた。** その16h枠を、この週が受け取る。

| 元 | 枠 | 実作業 | 内訳 |
|---|---|---|---|
| Day 6（8/15 土） | 8h | **6.75h** | RAG 3.0 ／ `score_min` 0.5 ／ 疎通確認 0.25 ／ `retrieval_hit_rate` 0.25 ／ 段ごとの測定 2.0 ／ 検証③の採否 0.25 ／ 比較表 0.5 |
| Day 7（8/16 日） | 8h | **5.0h** | 改善サイクル 2.0 ／ 公開 URL 0.5 ／ README 2.0 ／ 週の締め 0.5 |
| **合計** | **16h** | **11.75h** | 予備（Day 6 の 1.0h・Day 7 の 1.5h・条件つき 1.5h）は消滅 |

**予備枠を持ち越さない。** 予備は週ごとに取り直すものであって、繰り越すと「使わなかった枠」が
**やっていない作業の言い訳として残り続ける**。この週の予備は、この週の枠から取る。

## 2. 11.75h を今週に積まない理由

**実枠は 8/19〜8/23 の5日 ＝ 10h。** 11.75h を積めば 1.75h 超過する。

**枠を超えて積むのが、8/16 に計画が壊れた形そのものである。** 第2週は週26h の枠に26h を積み、
結果として Day 6・Day 7（16h）が丸ごと落ちた。**落ちた量は、超過した量ではなく枠の6割**だった——
**枠を100%埋めた計画は、超過分だけでなく、その手前から崩れる。**

したがってこの週は **5.5h だけを積み、4.5h を空けたまま閉じる**（PLAN.md §3 の稼働前提と同じ扱い）。
残りは第4週以降へ配る。**配り先を行ごとに書く**ので、引き取りは1箇所で見切れる。

## 3. 順序を反転させた（RAG → 測定 ではなく 測定 → RAG）

**Day 6 の設計は「RAG を作ってから、①（＋RAG）を含む5段をまとめて測る」だった。** これを反転し、
**①を欠いた4段（ベース／⓪／②／③）を先に出し、RAG は後半に回す。**

理由は3つある。

1. **表Bが今週の最低ラインだから。** RAG を先に置くと、RAG が延びた分だけ表そのものが存在しない期間が延びる。
   **先に骨格を出して①列だけ後から足す**ほうが、途中で崩れたときに残るものが多い。
2. **RAG は依存の追加を伴い、見積りが最も外れやすい。** `chroma` と `sentence-transformers` は
   `pyproject.toml` にまだ無く、多言語埋め込みモデルは数百MBある。**外れたときに巻き込む対象を減らす。**
3. **測定は依存を1つも増やさない。** `correction/baseline.py` と `correction/engine.py` は両方動き、
   検証①③は `correction/validation.py` に、採点は `evals/score.py` にある。**すでにあるものを測って並べるだけ。**

**代償は、②③の当て先が①ではなく⓪になること。** Day 6 の設計は②③を「①の出力への後処理」と書いていた。
①が無いので⓪の出力に当てる。**後から①を足したとき②③の値は動く**ので、その旨を表に明記する。

**もう1つの代償は、検証③の採否を今週決められないこと**（requirements.md §7 に詳しく書いた）。
検証③の規則は「根拠が引けておらず、かつ元の文が十分自然なら『直す必要なし』に倒す」であり、
**RAG が無い今週は「根拠が引けていない」が全件で真になる。** 判別を担うのが丁寧さの述語だけになるので、
**事前登録した採用基準（`false` 側の発火率が `true` 側より 20pt 以上高い）を当てる場になっていない。**
採否の判定は **第4週（RAG が入ってから）** へ送る。

## 4. ベースラインを測り直す

**Day 6 の設計は、ベースラインを `dev` 80件で「2回続けて回す」ことで
表Bの左列と測定の再現幅を同時に取るつもりだった。** その前提が崩れている。

| | `20260811-0752-baseline-dev.json` | 現在 |
|---|---|---|
| `items_digest` | `6444e2bec265` | **`758e01ba6d39`** |
| `detection` の分母 | 63 | **64** |
| `over_correction` の分母 | 17 | **16** |

**8/13 の eval-052 の再ラベル（`false` → `true`）を挟んでいる。** したがって 8/11 の記録は
**いまの120件で測った数字ではない**。「同じ `dev` 80件・同じモデル・同じ日で測る」という
受け入れ条件は、**ベースを回し直さないと満たせない。**

**追加の API 呼び出しは、⓪の80件だけでなくベースの80件も要る（計160件）。**
Day 6 は「ベースは測定済みなので呼び出し不要」と読める書き方をしていたが、それは成立しない。

**同日2回のベースラインは今週は取らない。** 再現幅（同日2回の差）とドリフト（日をまたいだ差）を
分けて書くのは README の話であり、**表Bの骨格を出すことより優先度が低い**。
第4週で①列を足すときに、必要なら同じ日に2回回して取る。

## 5. `scorer_checked_on` を今回から必ず埋める

**既存の実行記録7本のうち6本で `scorer_checked_on` が `null` である。**
開発ガイドラインは「`dev` の実行のたびに `dev` から5件を手で照合する」と定めているのに、
**記録の側がそれを裏づけていない。**

これは記録漏れであって測定の誤りではないが、**「毎回やっている」と README に書ける状態ではない。**
今週の実行からは、**照合したうえで `scorer_checked_on` を埋める**ところまでを1つのタスクとして扱う。

## 6. 変更するコンポーネント

| 対象 | この週で触るか | 内容 |
|---|---|---|
| `correction/baseline.py` | **触らない** | 測り直すだけ。コードは変えない |
| `correction/engine.py` | **触らない** | ⓪はこのプロンプトをそのまま使う |
| `correction/validation.py` | **触らない** | 検証①③は実装済み。②③は既存の関数を後処理として当てる |
| `evals/score.py` | **触らない** | 採点器を変えると前後比較が壊れる |
| `evals/runs/` | **増える** | ベース／⓪／②／③の実行記録。`items_digest` は現行の `758e01ba6d39` |
| `pyproject.toml` | **増える**（8/21） | `chroma` / `sentence-transformers` を追加 ← **RAG 前半。本書の §3 の後半** |
| `retrieval/` | **新設**（8/21〜8/22） | 節ごとの分割・埋め込み・Chroma 登録まで。`search()` は第4週 |
| `config/thresholds.toml` | **触らない** | `[retrieval] score_min` は第4週。**未記入のままだと `threshold()` が `KeyError` で落ちる**（第2週の申し送り(c)） |
| `README.md` | **表Bを貼るのは第4週** | 今週は数字を出すところまで。README 完成は 9/14〜9/20 |

## 7. 影響範囲

- **`test` 40件には触らない。** 項目の変更も禁止（`items_digest` が動く）。`test` の最後の1回は **9/14〜9/20**
- **プロンプトを変えない。** 今週は測るだけ。直すのは第4週の改善サイクル
- **閾値を変えない。** `rewrite_too_far` は 0.85 で確定済み。本実装の出力での再確認は第4週
- **`data/grammar/` の8本を増やさない**（PLAN.md §2-3 で固定）
- ~~**公開 URL はまだ存在しない。** 「更新」ではなく新規作成であり、第5週（8/31〜9/6）の2h枠~~
  → **誤り（2026-08-19 に判明）。URL は既に存在する**：https://nihongo-coach.streamlit.app/
  **8/3 に作成し本番で会話まで確認済み**（[第1週 tasklist](../20260802-week1-baseline/tasklist.md) Day 1）。
  **新規作成ではなく再デプロイ**なので第5週の枠は 2h → 1h。**今週の作業内容は変わらない**

## 8. 落とす順番（この週の中で）

**枠に入らなくなったときは、上から順に落とす。その場で悩まないために先に決める。**

1. **RAG 前半（8/21〜8/22 の 2h）** — 第4週へ丸ごと送る。**表Bは影響を受けない**
2. **②または③のどちらか** — ②（書き換えすぎの検出）を残す。③は今週どのみち採否を決められない
3. **⓪** — ベースだけの表になるが、それは表Bではない。**ここまで落ちたら週の目的が消える**

**表Bそのものは落とさない。** PLAN.md §3 の最低ライン（8/30）が「比較表が README にある」であり、
**ここが出ていれば以降どこで遅れても価値はゼロにならない。**

## 9. 測る前に書いた予想

**結果を見てから「そう思っていた」と言わないために、先に書く。**

| 段 | `detection_accuracy` の予想 | `over_correction_rate` の予想 |
|---|---|---|
| ベース | 98%前後（8/11 の 98.4% から分母が1件動くだけ） | 65%前後（同 64.7%。**分母が17→16なので1件で 6.25pt 動く**） |
| ⓪ | **ベースと同等かやや下**。プロンプトは判定を厳しくする方向ではない | **ベースより大きく下がる**。余計な直しを抑える指示が入っている |
| ② | ⓪と同じ | ⓪よりわずかに下がる（書き換えすぎを捨てるので、直し自体が減る） |
| ③ | ⓪と同じ | **⓪とほとんど変わらない**。根拠が全件で空なので判別が効かない |

**③が⓪と変わらないことを予想している**のが要点である。変わらなければ §3 の説明どおりで、
**変わったとしたら丁寧さの述語が単独で効いたということ**になり、それはそれで第4週の材料になる。
**どちらに転んでも書く。**

### 予想と実測（2026-08-19 に突き合わせた）

**4行のうち当たったのは1行だけである。**

| 段 | 指標 | 予想 | 実測 | 判定 |
|---|---|---|---|---|
| ベース | `detection` | 98%前後 | **100.0%** | **外れ**（+2pt。1〜2件ぶん上振れした） |
| ベース | `over_correction` | 65%前後 | **56.2%** | **外れ**（−8.8pt ＝ 1.4件ぶん） |
| ⓪ | `detection` | ベースと同等かやや下 | 100.0% → **93.8%** | **当たり**（−6.2pt ＝ 4件） |
| ⓪ | `over_correction` | ベースより大きく下がる | 56.2% → **31.2%** | **当たり**（−25.0pt ＝ 4件） |
| ② | 両方 | ⓪と同じ／わずかに下がる | **⓪と完全に同一** | **半分外れ。`over_correction` は動かなかった** |
| ③ | 両方 | **⓪とほとんど変わらない** | ③a 18.8%/0.0%・③b 92.2%/18.8% | **外れ。両方とも大きく動いた** |

**一番外したのは③である。** 「根拠が全件で空なので判別が効かない＝⓪とほとんど変わらない」と書いたが、
**実際には残った述語が単独で強く効いてしまった。** ③a は `detection` を 93.8% → 18.8% に破壊し、
③b も `over_correction` を 31.2% → 18.8% に動かしている。

**予想が外れた理由は、論理積の「効かない」を「発火しない」と読み違えたことである。**
根拠が空という条件は**全件で真**になるので、論理積から**条件が1つ消える**。
消えれば残りの述語は**抑制されるどころか自由になる**。「判別が効かない」と「何も起きない」は別物だった。

**②の外れ方も同じ形をしている。** 「わずかに下がる」と書いたのは発火が数件ある前提だったが、
**閾値 0.85 に対して実測の最大が 0.818 で、発火は0件**だった（§10）。

**ベースの2つを外したことは、`over_correction_rate` の読み方に効く。**
8/11 の 64.7% と今回の 56.2% の差 8.5pt は、**分母の変更（17→16）と1件の再ラベルで説明できる範囲**である。
**同じ実装・同じプロンプトでも、この指標は1件で 6.25pt 動く。**
**したがって表Bの `over_correction_rate` の差を、6.25pt 未満の粒度で語らない。**

---

## 10. 表B（2026-08-19 実測）

**同じ `dev` 80件・同じモデル（`gemini/gemini-2.5-flash`）・同じ日・同じ `items_digest`
（`758e01ba6d39`）・同じ `thresholds_digest`（`0f2620b2a901`）・同じ `scorer_version`（`score-v1`）。**

| 段 | 中身 | `detection_accuracy`（**n=64**） | `over_correction_rate`（**n=16**） | API 呼び出し | 実行記録 |
|---|---|---|---|---|---|
| **ベース** | 素朴な1回呼び出し（`baseline-v1`） | **100.0%**（64/64） | **56.2%**（9/16） | 80 | `20260819-0813-baseline-raw-dev.json` |
| **⓪** | 本実装プロンプト（`correction-v1`）・RAG なし・検証なし | **93.8%**（60/64） | **31.2%**（5/16） | 80 | `20260819-1741-engine-raw-dev.json` |
| **②** | ⓪ ＋ 検証①（書き換えすぎの検出） | **93.8%**（60/64） | **31.2%**（5/16） | **0** | `20260819-1756-engine-validated-dev.json` |
| **③a** | ⓪ ＋ 検証③（**丁寧さの述語**版） | **18.8%**（12/64） | **0.0%**（0/16） | **0** | `20260819-1756-engine-check3-politeness-dev.json` |
| **③b** | ⓪ ＋ 検証③（**admission** 版） | **92.2%**（59/64） | **18.8%**（3/16） | **0** | `20260819-1756-engine-check3-admission-dev.json` |
| ~~①~~ | ~~＋RAG・検証なし~~ | **空** | **空** | — | **RAG 未着手のため今週は測れない**（第4週） |

**表を読むときの但し書き。表から切り離さないこと。**

- **`dev` の数字である。`dev` は調整に使ってよい側なので、ベースとの差は上振れする。**
  `test` 40件での測定は 8/31〜9/6 の1回だけ
- **両方向のラベルを 2026-08-19 に本人が再確認している**——等間隔サンプルの `true` 側5件
  （eval-001・025・048・073・097）と、⓪が余計に直した `false` 側5件（eval-009・034・047・082・083）。
  **変更は1件も無かったので `items_digest` は動いていない**
- **分母は4段すべて同じ**：`detection` 64件（1件 1.56pt）／`over_correction` 16件（**1件 6.25pt**）。
  **`over_correction_rate` の差は最小単位が 6.25pt なので、1〜2件の違いを「傾向」と読まない**
- **①列が空なのは RAG が未着手だからである。** ①を後から足すと**②③の値は動く**——
  ②③は⓪の出力への後処理であり、①の出力は別物になる
- **表A（`test` 40件・ベースラインのみ）と足し合わせない**
- **検証②（レベル超過による再生成）はこの表に載らない。** `dialogue.reply()` の中で起きるもので、
  `level_compliance` の記録が持っている（1発目 94.4% / 再生成後 98.9%）
- **③は採否の判定ではない。** RAG が無いので「根拠が引けていない」が全件で真になり、
  判別を担うのが残りの述語だけになる（[requirements.md](requirements.md) §7）。**採否は第4週**

### ②が1件も発火しなかったことについて

**検証①（`rewrite_too_far`、正規化編集距離 0.85）は⓪の出力に対して発火0件である。**
したがって**②はデータ上⓪と完全に同一**であり、表の2行は同じ数字を指している。

訂正を出した65件の正規化距離の**最大は 0.818**（`eval-028`「ご苦労さまです。」→「ありがとうございます。」）、
次が 0.800（`eval-004`「良い一日。」→「おはようございます。」）。**いずれも閾値 0.85 を下回る。**

- **閾値はベースラインの出力で決めた**（第2週 Day 4。「正しい訂正を1件も捨てない」を優先して 0.85 に置いた）。
  **本実装の出力では、その保守的な置き方が「何も捨てない」に振り切れている**
- **0.818 の `eval-028` は、検証①が本来捕まえるべき「別の文への置き換え」に見える。**
  「ご苦労さま」→「ありがとうございます」は語彙がほぼ入れ替わっている
- **ただし今週は閾値を動かさない**（[requirements.md](requirements.md) §6 の制約）。
  **第4週の「`rewrite_too_far` を本実装の出力で再確認する」に、この2件を持ち込む**

### ③の発火率（採否の判定ではない。第4週の材料）

**事前登録した基準：`false` 側の発火率が `true` 側より 20pt 以上高いこと。**

| 版 | `false` 16件（守るべき側） | `true` 64件（止めてはいけない側） | 差 | 基準 |
|---|---|---|---|---|
| **③a 丁寧さの述語** | 31.2%（5/16） | **75.0%（48/64）** | **−43.8pt** | **満たさない。向きが逆** |
| **③b admission** | 12.5%（2/16） | 1.6%（1/64） | **+10.9pt** | **満たさない**（20pt に届かない） |

- **③a は `detection_accuracy` を 93.8% → 18.8% に破壊する。** 段階A（`greeting` / `thanks`）を
  無条件に通す述語が、そのまま「段階Aの訂正を全部捨てる」に化けている。
  2026-08-10 の測定（`false` 93.3% 対 `true` 81.1% ＝ +12.2pt）は**述語を単体で学習者文に当てた数字**であり、
  **検証③の論理積として当てた今回とは測っているものが違う**。**この差自体を第4週に持ち込む**
- **③b は第2週 design が予告したとおり縮んだ。** ベースラインの理由で測ったときは +35.2pt だったが、
  **本実装の理由では +10.9pt**。プロンプトが違えば同じ言い回しは出ない、という予告が当たった形である。
  4語句は**測る前に凍結してある**（`ADMISSION_PHRASES`）
- **どちらも今週の数字では採用しない。** RAG が入ってから、同じ基準でもう一度測る

### 実装上の申し送り

- **②③は `evals/restage.py`（新規）で作った。** `evals/score.py --stage validated` はモデルを呼び直すため、
  「段の差＝検証の差」が保てない。保存済みの `raw` 記録に後処理を当てる経路を分けた。**API 呼び出しは0**
- **検証③の論理積そのものは `correction/validation.py` に入れていない。** 採否が未決のうちに入れると
  アプリが「採用済み」の振る舞いになる。**採用が決まった時点で `validate()` へ移す**
- **実行記録は `grounding_ids` を項目ごとに保存していない。** 今週は全件が空なので影響はないが、
  **RAG が入った後は保存しないと検証③を後から再現できない**（`evals/score.py` の `_result_row`）
