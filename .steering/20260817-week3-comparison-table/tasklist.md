# 第3週 タスクリスト — 第2週 Day 6・Day 7 の引き取り

対応する設計: [design.md](design.md) ／ 要求: [requirements.md](requirements.md)
／ 期間: **2026-08-17（月）〜 08-23（日）**（実枠は **8/19〜8/23 の5日 ＝ 10h**。8/17・8/18 は着手なし）

**この文書は、[第2週 tasklist](../20260810-week2-validation/tasklist.md) の Day 6（8/15 土・8h）と
Day 7（8/16 日・8h）を丸ごと移してきたものである。** 移動元からは削除済みで、実行の管理はここに一本化する。

### 稼働の前提

**毎日2時間 ＝ 週14時間**（PLAN.md §3）。**休日8hという枠はもう存在しない。**
**この週の実枠は 8/19〜8/23 の5日 ＝ 10h**（8/17・8/18 は着手なし）。

| 日 | 枠 | 積む |
|---|---|---|
| 8/19（水） | 2h | **2.0h** |
| 8/20（木） | 2h | **2.0h** |
| 8/21（金） | 2h | **2.0h** |
| 8/22（土） | 2h | **2.0h** |
| 8/23（日） | 2h | **2.0h** |
| **合計** | **10h** | **10.0h** |

> **予備枠は置かない（2026-08-19 の指示）。毎日2hを均等に埋める。**
>
> **これは枠を100%埋める形である。** 埋めるために **RAG を第4週から丸ごと前倒し**した
> （旧配分は 5.5h ＝ 比較表3.5 ＋ RAG 前半2）。**緩衝は週の中ではなく計画全体に置く**——
> PLAN.md §3 の残り枠は 8/19〜9/27 で 80h に対し54h なので、**26h が緩衝として残っている。**
>
> **リスクは正直に書いておく。** 第2週が壊れたのは枠を100%埋めたからであり、この週は同じ形をしている。
> **違うのは、落ちたときに吸収する先が週の外（26h）にあること**である。
> **落ちた日を翌日に足して取り返そうとしない**——それをやると 8/16 と同じになる。

進捗記号: `[ ]` 未着手 ／ `[~]` 着手中 ／ `[x]` 完了 ／ `[-]` 降ろした ／ `[→]` 先の週へ送る

---

## 8/19（水）— 段ごとの測定：ベースと⓪（2.0h）

**追加の API 呼び出しは 160件**（ベース80件 ＋ ⓪80件）。**⓪だけでは足りない。**

- [x] **ベースラインを `dev` 80件で測り直す**（1.0h）
      — **完了（2026-08-19 08:13）。`evals/runs/20260819-0813-baseline-raw-dev.json`**
      — **`detection_accuracy` 100.0%（n=64）／`over_correction_rate` 56.2%（n=16）**
        ／`format_compliance_rate` 100.0%／不能な判定 0／「」で括られていない日本語 37件
      — **8/11 の記録は流用できない。** `20260811-0752-baseline-dev.json` の `items_digest` は
        `6444e2bec265` だが、**8/13 の eval-052 再ラベル（`false` → `true`）で現在は `758e01ba6d39`**。
        分母も **`detection` 63 → 64 ／ `over_correction` 17 → 16** に動いている
      — **eval-052 は `dev` なので、`test` を1件も変更していないという第2週の完了条件は保たれている**
      — **同日2回は回さない**（再現幅の測定より表Bの骨格が優先。[design.md](design.md) §4）
- [x] **⓪ 本実装プロンプト・RAG なし・検証なし**（80件・1.0h）← **プロンプトの差**
      — `correction/engine.py`（`correction-v1`）をそのまま使う。**プロンプトを変えない**
      — 実行記録に `items_digest` / `thresholds_digest` / `prompt_version` / `scorer_version` が入ること
      — **完了（2026-08-19 17:41）。`evals/runs/20260819-1741-engine-raw-dev.json`**
      — **`detection_accuracy` 93.8%（n=64）／`over_correction_rate` 31.2%（n=16）**
        ／`format_compliance_rate` 97.5%／不能な判定 0／「」で括られていない日本語 9件
      — **4項目はいずれも入っている**：`items_digest` `758e01ba6d39`（ベースと一致）／
        `thresholds_digest` `0f2620b2a901`（同）／`prompt_version` `correction-v1`（ベースは `baseline-v1`）／
        `scorer_version` `score-v1`（同）。モデルも `gemini/gemini-2.5-flash` で一致
- [x] **【計画外・2026-08-19】`llm.py` の Gemini 分岐にタイムアウトを入れた**
      — **1回目の⓪は7時間50分ハングした。** CPU 時間 2.88 秒に対し接続は張られたままで、
        ソケット読み取りで停止していた。`ChatGoogleGenerativeAI` の `timeout` の既定は `None` ＝
        **無制限**であり、`REQUEST_TIMEOUT_SECONDS = 30.0` は Anthropic 分岐にしか渡っていなかった
      — 直したのは `llm.py` の1行のみ。**プロンプト・採点器・閾値・`correction/` は触っていない**ので、
        ベース（タイムアウト無しで実行）と⓪（有りで実行）の比較は成立する。
        **タイムアウトは失敗の出方を変えるだけで、返ってきた答えの中身を変えない**
      — 再実行した⓪は約20分・80件すべて完了。**タイムアウトによる欠損はゼロ**（不能な判定 0）

## 8/20（木）— ②③の後処理・手照合・表B（2.0h）

- [x] **② ＋検証①（書き換えすぎの検出）／ ③ ＋検証③（余計な直しの抑制）**（0.5h）
      — **⓪の出力への後処理。追加の API 呼び出しはゼロ**なので、段の差にサンプリングの揺れが混ざらない
      — **旧計画は②③を「①の出力への後処理」と書いていた。** ①が無いので⓪に当てる。
        **後から①を足すと②③の値は動く**——この差を表に明記する
      — **完了（2026-08-19）。`evals/restage.py` を新設した**——`evals/score.py --stage validated` は
        **モデルを呼び直す**ため「段の差＝検証の差」が保てない。保存済みの `raw` 記録に後処理を当てる経路を分けた。
        **`evals/score.py` は触っていない**（採点ロジックは import して再利用）
      — **② は発火0件で⓪と完全に同一。** 訂正65件の正規化距離の最大が **0.818**、閾値は 0.85（[design.md](design.md) §10）
      — **③は2つの候補を両方測った**（第2週 design の指示どおり）：
        **③a 丁寧さの述語** → `detection` 93.8% → **18.8%**・発火差 **−43.8pt（向きが逆）**／
        **③b admission** → `detection` **92.2%**・`over_correction` **18.8%**・発火差 **+10.9pt**。
        **どちらも事前登録の 20pt を満たさない。採否の判定は第4週**
      — **検証③の論理積は `correction/validation.py` に入れていない。** 採否が未決のうちに入れると
        アプリが「採用済み」の振る舞いになるため、`evals/restage.py` 側に置いた
- [x] **`dev` から5件を手で照合し、`scorer_checked_on` を埋める**（0.5h）
      — **完了（2026-08-19）。Jun 本人が5件を確認し「分類は問題ない」と判定。**
        **8/19 の実行記録5本すべてに `scorer_checked_on` を書き込んだ**（値は各記録自身の `run_id`
        ＝照合が行われた `dev` の実行はその実行そのもの）。**`null` の記録はもう無い**
      — **`false` 側5件（eval-009・034・047・082・083）も同日に確認済み。**
        規定の工程は等間隔サンプル5件だが、**`over_correction_rate` の分母は1件 6.25pt 動く**ので
        ⓪が余計に直した5件を追加で当てた。**ラベルの変更なし＝`items_digest` は動かない＝測り直し不要**
      — **したがって表Bの4段は、両方向のラベルを本人が確認したうえで出た数字である**
      — **既存の実行記録7本のうち6本が `null` のまま**である。「毎回やっている」と README に
        書ける状態ではないので、**今回から必ず埋める**（[design.md](design.md) §5）
      — **照合用の一覧は作成済み**（eval-001・eval-025・eval-048・eval-073・eval-097。3本とも同一）。
        **判定は Jun 本人が行う。確認が済んだら各記録の `scorer_checked_on` に自身の `run_id` を書き込む**
      — **【発見】等間隔サンプルの5件が全部 `true` ラベルだった。**
        **`over_correction` 側の採点バグはこの5件では捕まらない。**
        補助として⓪が余計に直した `false` 側5件（eval-009・034・047・082・083）も並べた。
        **`manual_check_sample` をラベルで層化するかどうかは第4週で決める**（今週は採点器を変えない）
- [x] **表B を書く**（0.5h）— ベース／⓪／②／③ の4段
      — **完了（2026-08-19）。[design.md](design.md) §10 に置いた。** README へ貼るのは 8/31〜9/6
      — **すべて同じ `dev` 80件・同じモデル・同じ日**
      — **分母と誤差を表の中に**：`detection` **64** ／ `over_correction` **16（1件 6.25pt）**。
        **4段すべてがこの分母**（＝ベースも現在の `items_digest` で測り直した、ということ）
      — **「本実装は `dev` で調整した側の数字であり、差は上振れする」と表の中に明記**
      — **①列が空であることと、その理由（RAG 未着手）**を書く
      — **表A（`test` 40・ベースラインのみ）と足し合わせない**
      — **README に貼るのは 8/31〜9/6**（2026-08-19 変更。**応募開始が 9/13〜9/14 に前倒しされ、
        README の中核がその前に要る**ため。旧「9/14〜9/20」）。**今週は数字を出すところまで**
- [x] **測る前に書いた予想と実測を突き合わせ、[design.md](design.md) §9 に結果を書く**（0.5h）
      — **外れたら外れたと書く。** 特に「③は⓪とほとんど変わらない」という予想
      — **完了（2026-08-19）。当たったのは⓪の2行だけで、ベース2行・②・③は外した。**
        **③の外し方が一番大きい**——「根拠が全件で空だから判別が効かない」を
        「発火しない」と読み違えていた。**条件が全件で真になると論理積から条件が1つ消え、
        残った述語はむしろ自由になる**

## 8/21（金）— RAG：取り込みと引用検査（2.0h）

- [x] `pyproject.toml` に `chroma` / `sentence-transformers` を追加する（0.5h）
      — **完了（2026-08-19 に前倒し実施）。`chromadb` 1.5.9 / `sentence-transformers` 6.0.0。**
        `uv sync` 済みで `uv.lock` も更新
- [x] リファレンスを**節ごとに割って**取り込む（1.0h）— **1本まるごと1チャンクにしない**
      — **完了。`retrieval/chunks.py` を新設。8本 → 36チャンク**
        （記事あたり3〜7節。本文の長さは 最小124 / 中央339 / 最大1331 文字）
      — **見出し（`##`）で割る。文字数で割らない**——固定窓は「✗ X → ✓ Y」の対比を途中で切り、
        片側だけが引かれると役に立たなくなる
      — **最初の見出しより前の段落も1チャンクとして残す**（記事が「なぜこの区別があるか」を
        書いている唯一の場所）
      — **埋め込むテキストと本文は別**：`embedding_text` は記事タイトルと見出しを前置きする
        （「Negatives」のような見出しは単独では意味を持たない）。**`body` はファイルのまま**なので
        引用検査がこのモジュールの合成物ではなく実物と突き合わせられる
- [x] **`data/grammar/` × 120件の引用検査 pytest を、Chroma に載せる前に通す**（0.5h）
      （`test` はハード失敗、`dev` は id の許可リスト）
      — **完了。既存の `tests/test_grammar_reference.py` に加えて、`tests/test_chunks.py` に
        「実際に索引へ載る単位」に対する同じ検査を足した**（13件）。`test` 40件の混入ゼロ、
        `dev` の重なりは許可リスト12件と完全一致
      — **構造側も縛った**：チャンク本文が記事に逐語で存在すること・記事の行が1つも失われないこと・
        id が一意であること・frontmatter が壊れていたら例外
- [ ] **【8/22 への申し送り】最大チャンク（`grammar-008#1`・1331文字）が埋め込みで切り詰められないか確かめる**
      — `intfloat/multilingual-e5-small` の `max_seq_length` を実測し、超えるなら節の中でさらに割る。
        **切り詰めは無言で起きるので、低い `retrieval_hit_rate` としてしか現れない**

## 8/20（木・2026-08-19 に前倒しで実施）— RAG 完了と検索の2指標

- [x] **埋め込みと Chroma 登録**（`retrieval/index.py`）
      — `intfloat/multilingual-e5-small`（384次元）。**索引は起動時にメモリ上へ作る**——
        永続化すると記事を直したのに索引が古いまま、という失敗が起こりうる。36チャンクで約1秒
      — **e5 の接頭辞（`query: ` / `passage: `）を入れた。** 入れ忘れても動くが当たり率だけが静かに下がる
      — 初回19.5秒（モデル読み込み込み）、以降は1件 14〜210ms
- [x] **【8/22 への申し送りの回答】切り詰めは起きない**
      — `max_seq_length` は **512トークン**、最大チャンク（`grammar-008#1`）は **473トークン**。**超過ゼロ**
      — 文字数での上限（1400字）を `tests/test_chunks.py` に置いた（テストでモデルを落とさないため）
- [x] **`retrieval.search(sentence)` が上位3件と id と類似度を返す**
      — **`score_min` で絞らない。** 絞ると当たり率が閾値で動き、閾値を通じて当たり率を調整できてしまう
- [x] **`[retrieval] score_min` を決めた → `0.0`**（`evals/retrieval_measure.py --choose`）
      — **事前登録の規則は床を選べなかった。** 閾値用10件のうち根拠のある8件で、
        注釈記事が上位3件に入ったのは **5件だけ**（要求は7件）。**順位の問題なので、どんな床でも救えない**
      — 規則が予告していたとおり **「この参照規模では検証③を運用できない」と記録した。**
        `recall_floor_items` を下げて辻褄を合わせることはしない
      — **`score_min = 0` の帰結：全結果が根拠として通るので「根拠が引けていない」が常に偽 ＝ 検証③は一度も発火しない**
- [x] **`retrieval_hit_rate` を測った → 75.0%（12/16、順位のみ、上位3件）**（`--measure`）
      — **目標80%に届いていない。** 閾値用10件でも 5/8 ＝ 62.5% で、両方が同じことを言っている
      — **測定用20件は閾値決定に一切使っていない**（`--choose` と `--measure` が別の集合しか読まない）
- [x] **`grounding_ids` を検索結果由来にする**
      — **後貼りにはしなかった。** モデルが読んでいない記事を根拠として見せることになるため
        （当たり率75%・`score_min = 0` なら4件に1件が無関係）。
        **検索結果をプロンプトに入れて読ませ、モデルが「使った」と答えた id だけを残す**
      — **実在しない id は捨てる**（`grammar-009` のような幻覚が学習者にも検証③にも届かないように）
      — **プロンプトが変わるので版を分けた**：`correction-rag-v1`。⓪の `correction-v1` はそのまま再現できる
      — `retrieval` は**関数内 import**。公開版が torch を載せられない場合に、
        訂正エンジンごと import できなくなるのを避けるため。**検索が落ちても訂正は返る（根拠なしで）**
- [x] **表B に ① 列を足して比較表を完成させた**（第4週から前倒し）
      — **`detection` 92.2%（59/64）／`over_correction` 25.0%（4/16）**。
        **⓪との差はどちらも1件**なので「RAG が効いた」と読める大きさではない
      — 実行記録 `20260819-2147-engine-rag-raw-dev.json`
- [x] **実行記録に `grounding_ids` と `grounded_items` を残すようにした**（今日見つけた欠落）
      — **今日の①の記録には入っていない**（追加より前に回したため）。次の①から入る

## 8/22（土）— RAG：埋め込みと索引（2.0h）※上へ前倒し済み

- [ ] sentence-transformers の多言語モデルで埋め込み → **Chroma に登録**（1.5h）
      — **索引は起動時に作る**
- [ ] `retrieval.search(sentence)` の骨格を置く（0.5h）— **上位3件と id と類似度を返す**

## 8/23（日）— RAG：検索を繋ぐ・週の締め（2.0h）

- [ ] `retrieval.search(sentence)` を仕上げる（1.0h）
- [ ] **`grounding_ids` を検索結果由来にする**（0.5h）— **現状はモデルの JSON 出力を拾っている**
- [ ] **週の締め**（0.5h）
      — `ruff` / `mypy` / `pytest` 通過
      — **「降ろす順番」の点検**（PLAN.md §3。点検日は 8/30・9/6・9/12・9/20）。
        **番号順に機械適用しない。** 2・3・4・5 で足りるかを先に見る
      — 現在地を [design.md](design.md) に書く
      — **記録欄の実績工数を埋める** ← **Jun 本人が記入。推測で書かない**

---

## 第4週（8/24〜8/30）へ送る

**RAG が繋がってから初めて成立するものと、比較表の完成。**
**RAG そのもの（取り込み・埋め込み・索引・`search`・`grounding_ids`）は第3週へ前倒しした**ので、ここには残っていない。

- [→] **`[retrieval] score_min` を決める**
      — **比較表の指標（`detection_accuracy` / `over_correction_rate`）を見て決めない。**
        この閾値は検証③の発火を通じて**両指標を直接動かすつまみ**である
      — **「決めた回の `retrieval_hit_rate` は公表しない」では守れない。**
        `retrieval_hit_rate` は**順位の指標**で、採否の下限である `score_min` に対して不変
      — **決定基準は検索単体の性質**：第2週 Day 5 の**閾値決定用10件**について、
        手で付けた正解 id が `score_min` 以上で残る割合（再現率）で決める
      — 値と、それを決めたデータと量を `thresholds.toml` にコメントで残す（既存3つと同じ書式）
      — **未記入のままだと `threshold()` が `KeyError` で落ちる**（第2週の申し送り(c)）
- [→] **`retrieval_hit_rate`**（`n=20`）— 第2週 Day 5 の測定用20件。**正解 id は先に付けてある**
      — 測り方を glossary §5 に書き戻す（件の選び方・除外・内訳・誤差の目安。**ja → en**）
- [→] **① ＋RAG・検証なし**（80件）→ **表B に ① 列を足して比較表を完成させる**
- [→] **`rewrite_too_far` を本実装①の出力で再確認する**（第2週 Day 4 は暫定）
- [→] **検証③の採否を、事前登録した基準で判定する**
      — 基準：`dev` で **`false` 側の発火率が `true` 側より 20pt 以上高いこと**。**結果を見てから基準を決めない**
      — **今週は判定できない。** RAG が無いと「根拠が引けていない」が全件で真になり、
        判別を担うのが丁寧さの述語だけになるため（[requirements.md](requirements.md) §7）
      — 述語は**段階A の場面は無条件に通し、段階B・C だけ文末を見る**形
        （段階は `POLITENESS_FLOORS` のタプルの**第1要素だけ**。**評価項目を1つも引用していない**）
      — **`politeness_rule()` と `POLITENESS_ADJUDICATIONS` を `correction/` から import しない**
        （eval-081 は `test`、eval-017・eval-022 の正解を実例として引用している）。**pytest で縛る**
      — **満たさなければ採用しない。測定結果を README に書く**
        ——「実装できなかった」ではなく「**当てたら判別力が無かった**」と書ける
      — 参考（2026-08-10 の実測）：素朴な文末判定は **`false` 30.0% 対 `true` 37.8%＝向きが逆**。
        場面の段階を見る版で **93.3% 対 81.1%（+12.2pt）**。**20pt に届いていない**
- [→] **改善サイクル1周目**
      — **一番低い指標を1つ選ぶ** — 測定結果で決める。**予想で先に決めない**
      — **変えるのは1つだけ**
      — **プロンプトを変えたら `PROMPT_VERSION` を上げる。閾値を変えたら `thresholds_digest` が動く**
        （どちらも動かないと、実行記録を2つ並べても何を変えたのかが復元できない）
      — **前後を同じ `scorer_version`・同じ分割（`dev`）・同じモデルで測る**
      — **悪化しても記録する**

## 第5週（8/31〜9/6）へ送る

- [→] **依存の疎通確認 → 公開 URL**（PLAN.md の「デプロイ 1h」に吸収。**2h から下方修正**）
      — 多言語埋め込みモデル（数百MB）＋ SudachiDict（約70MB）が無料枠に載るか**未確認**
      — 落ちたら**公開版だけ RAG 無し**にする。**測定は手元で回すので数字は1つも失われない**
      — ~~**URL はまだ存在しない。「更新」ではなく新規作成である**~~
        → **誤り（2026-08-19 に判明）。URL は既に存在する**：https://nihongo-coach.streamlit.app/
        **8/3 に Community Cloud へデプロイ済みで、本番で会話まで確認している**
        （[第1週 tasklist](../20260802-week1-baseline/tasklist.md) Day 1）。**新規作成ではなく再デプロイである**
      — **起動ファイルは Settings から変更できない**（第1週 Day 1 の実測）。作り直すと URL を取り直しになる。
        **したがって `app/main.py` を恒久的なエントリとして固定する**——FastAPI 切り出しでここを動かすと URL を取り直しになる
- [→] **【2026-08-19 に追加】Streamlit Community Cloud 側の確認2件**（本体作業とは別に対応する）
      — **いま URL がログインを要求している。** `curl` で `/-/login` へ 303 リダイレクトされるので、
        **アプリが private 設定になっている可能性が高い。** この状態だと **9/13 に会場でインドの参加者が
        自分のスマホから開けない**（§2-5 の「共有コード1本」はアプリ内の仕組みで、Streamlit 側の認証とは別物）。
        **public に戻す。数分の作業だが、当日気づくと詰む**
      — **Cloud 側の Python が 3.14、手元の `.venv` は 3.12**（第1週 Day 1 の記録）。
        **sentence-transformers（＝torch）の wheel が 3.14 に無いと、公開版のビルドが落ちる。**
        **手元で通っても本番で落ちる**組み合わせなので、RAG を書き終えた直後に一度上げて確かめる

## 8/31〜9/6 へ送る（**2026-08-19 に 9/14〜9/20 から前倒し**）

**応募開始が 9/13〜9/14 になったので、README に載る数字はその前に確定していなければならない。**

- [→] **`test` 40件での最終測定**（§2-4 の「最初と最後にしか触らない」の「最後」）
- [→] **README の中核** — 顧客課題1文／既存サービスとの差分1段落／**表A・表B**／3指標と**信頼区間の併記**／
      改善サイクル／`retrieval_hit_rate` の但し書き／遵守率2行／
      検証③を見送った場合はその測定結果／発火0件のノードがあればその旨／測っていない型の但し書き

## 9/15〜9/20 へ送る

**9/14 の応募には要らないと判断したもの**（PLAN.md §0 の注記）。**出してから足す。**

- [→] **README の仕上げ** — アーキ図／**英語注釈つきスクショ**／`docker compose` 手順／データの出典とライセンス／
      Progress の checkbox を実態に合わせる／**9/13 の感想3件**
- [→] **Docker**（3h）
- [→] `docs/{ja,en}/repository-structure.md` の `nlp/` `retrieval/` の *(planned)* を外す
- [→] `docs/{ja,en}/functional-design.md` に検証ノード・`grounding_ids`・`discarded` を追記

## 降ろした（この週では復活させない）

- [-] **文法リファレンスを 8本 → 10本** — **恒久的に降ろした（2026-08-16）。8本で固定**（PLAN.md §2-3）
- [-] **1ターンあたりのトークン数・レイテンシの前後比較** — **応答時間とコストは公開指標から外した**
- [-] **同日2回のベースライン（再現幅の測定）** — 今週は回さない。第4週で①列を足すときに必要なら取る
- [-] **`rater_agreement` をこの週で計算する** — ~~9/13 の Minna Shuugou の会場でその場採点まで持ち込む~~
      → **2026-08-19 に Minna Shuugou から切り離し、第4週（8/24〜8/30）へ移した**（PLAN.md §2-4・§3）。
      **紙の20件であって URL も音声も要らず、日本語ネイティブなら誰でもよい**ため、会場を待つ理由が無かった
      （INDIGITAL の同僚・前田さん）。**README の中核を書く 8/31〜9/6 より前に数字が要る**のが前倒しの理由。
      **空の採点キットは消さずに残す**

---

## 週の完了条件

- [ ] **表B（ベース／⓪／②／③ の4段）が、同じ `dev` 80件・同じモデル・同じ日で出ている**
- [ ] **ベースが現在の `items_digest`（`758e01ba6d39`）で測り直されている**
- [ ] **分母と誤差が表の中にある**：`detection` 64 ／ `over_correction` 16（1件 6.25pt）
- [ ] **①列が空であることと、その理由が表に書いてある**
- [ ] **`dev` から5件を手で照合し、`scorer_checked_on` が埋まっている**
- [ ] **測る前に書いた予想**（[design.md](design.md) §9）と実測を突き合わせ、**外れた場合は外れたと書いてある**
- [ ] `pyproject.toml` に `chroma` / `sentence-transformers` が入り、Chroma への登録まで動いている
- [ ] **`retrieval.search(sentence)` が上位3件と id と類似度を返す**
- [ ] **`grounding_ids` が検索結果由来になっている**（モデルの JSON 出力を拾うのをやめた）
- [ ] **`data/grammar/` × 120件の引用検査 pytest を、Chroma に載せる前に通した**
- [x] **第2週 tasklist が閉じている**（実績工数は「計測できず」で記入済み。2026-08-19 に閉じた）
- [ ] `ruff` / `mypy` / `pytest` 通過

> **PLAN.md の最低ライン（8/30）**：「ベースラインと本実装の比較表がある」。
> **今週はその骨格（①抜き4段）まで。** ここが出ていれば、以降どこで遅れても価値はゼロにならない。
>
> **落ちたときに真っ先に降ろすのは 8/21〜8/23 の RAG である**（第4週へ戻す）。
> **8/19〜8/20 の表Bは降ろさない。** RAG は元々第4週の作業であり、前倒ししたぶんが戻るだけなので、
> **降ろしても計画は遅れない。** 逆に表Bを落とすと最低ラインに直接効く。

---

## 記録

| 項目 | 値 |
|---|---|
| 予定工数 | **10.0h（枠10h ＝ 100%）**。予備枠は置かない（2026-08-19 の指示） |
| 実績工数 | **未記入** ← Jun 本人が記入。**推測で書かない** |
| 引き取り元 | 第2週 Day 6（8h枠・実作業6.75h）＋ Day 7（8h枠・実作業5.0h） |
