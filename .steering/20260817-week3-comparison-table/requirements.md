# 第3週 要求内容 — 第2週 Day 6・Day 7 の引き取りと、比較表（表B）の RAG 抜き版

期間: **2026-08-19（水）〜 08-23（日）**（**5日＝枠10h**。8/17・8/18 は着手が無かった） ／ 設計: [design.md](design.md) ／ タスク: [tasklist.md](tasklist.md)

> **【2026-08-19】第2週（8/10〜8/16）の残り16h枠を、この第3週がまとめて引き取った。**
> Day 6（8/15 土・8h）と Day 7（8/16 日・8h）は**丸ごと未着手**で、実作業は約 **11.75h**。
> **休日8hという枠がもう存在しない**ので、この週が帳簿上いったん全部を受け取り、**そこから先の週へ配る**。
> **この週で消化するのは 5.5h だけ**（下の日ごとの割り当て）。残りの行き先は：
> `score_min`・`retrieval_hit_rate`・①列・比較表完成・検証③の採否・改善サイクル1周＝**第4週（8/24〜8/30）**／公開 URL と疎通確認＝**第5週（8/31〜9/6）**／
> README と `test` 40件の最後の1回＝**9/14〜9/20**。**第2週の tasklist は 8/23 に閉じる。**
>
> **11.75h を今週に全部積まない。** 実枠は10hで、積めば 1.75h 超過する。
> **枠を超えて積むのが 8/16 に計画が壊れた形そのもの**なので、繰り返さない。

> **【2026-08-18 改訂】稼働が毎日2時間 ＝ 週14時間に戻った**（副業を断ったため。PLAN.md §3）。
> **この週の実枠は 8/19〜8/23 の5日 ＝ 10h**（2026-08-19 に、着手が無かった2日を落として更新）。
> **予備枠は置かず、毎日2hを均等に埋める**（2026-08-19 の指示）。積む見積りは **10.0h**。
> 埋めるために **RAG を第4週から丸ごと前倒し**した。
>
> **日ごとの割り当て（2h/日）**
>
> | 日 | やること |
> |---|---|
> | **8/19（水）2h** | **ベースラインを `dev` 80件で測り直す**（`items_digest` が動いたため）＋ **⓪ を80件**（API 計160件） |
> | **8/20（木）2h** | ②③の後処理 ＋ **`dev` 5件の手照合と `scorer_checked_on` 記入** ＋ **表B を書く** ＋ 予想と実測の突き合わせ |
> | **8/21（金）2h** | RAG：依存追加（`chroma` / `sentence-transformers`）・節ごとの分割・引用検査 pytest ← **本書の範囲外** |
> | **8/22（土）2h** | RAG：埋め込み・Chroma 登録・`search` の骨格 ← **本書の範囲外** |
> | **8/23（日）2h** | RAG：`search` 仕上げ・`grounding_ids` の差し替え ＋ **週の締め** ← **本書の範囲外** |
>
> **検証③の採否判定は 8/20 から外した**（§7 のとおり、RAG が無いと判定の場にならないため。第4週へ）。
> **RAG 前半（依存追加・節ごとの分割・埋め込み・Chroma 登録）は本書の範囲外**である。
> 本書は比較表だけを扱い、RAG は次の作業単位で扱う。
>
> ~~稼働は週6.5時間（平日0.5h・休日2h）。実装に使えるのは土日の4hだけで、平日0.5hには実装を置かない。~~
> **平日にも実装を置いてよくなった**ので、比較表を土日まで待たない。
> **ただし枠を全部埋めない**（12h の枠に 5h。理由は PLAN.md §3 の稼働前提）。

---

## 1. 今回やること

**素朴なベースラインと本実装を並べた表を、`dev` 80件で出す。** ただし RAG はまだ無いので、
**RAG を含む段（①）だけを欠いた形**で出す。

これは新しい機能の追加ではない。**すでにあるものを測って並べるだけ**である。
`correction/baseline.py`（`baseline-v1`）と `correction/engine.py`（`correction-v1`）は両方とも動き、
検証①③は `correction/validation.py` に実装済みで、採点は `evals/score.py` が持っている。

## 2. なぜ今週これなのか

**8/30 が全体の最低ラインで、その中身が「比較表がある／改善サイクルが1周回っている」だからである**
（2026-08-18 に 9/20 から前倒し。PLAN.md §3）。
ここが出ていれば以降どこで遅れても価値はゼロにならないし、逆にここを落とすと
9月に何を作っても「作っただけ」になる。**残り6週のうちの1週目を、表の骨格に充てる。**

**RAG を待たない理由**：RAG には 4h かかり、待つと表そのものが 8/30 まで存在しない。
**先に骨格を出し、①列だけ後から足す**ほうが、途中で計画が崩れたときに残るものが多い。
**稼働が週14h に戻って RAG が同じ週に入るようになっても、この順番は変えない**——
表が先にある状態を作ることが目的であり、同時に着手すると両方が中途半端になる。

## 3. 測る段

**「段」の番号と「検証」の番号は別物である。** 混同すると表が読めなくなるので先に固定する。

| 段 | 中身 | 追加の API 呼び出し |
|---|---|---|
| **ベース** | 素朴な1回呼び出し（`baseline-v1`） | **必要（80件）。2026-08-18 に「不要」から訂正。**<br>`evals/runs/20260811-0752-baseline-dev.json` の 98.4% / 64.7% は **古い評価データで測った数字**である：`items_digest` が `6444e2bec265`（現在は `758e01ba6d39`）で、8/13 の eval-052 再ラベルを挟んでいる。**分母も `detection` 63・`over_correction` 17** であり、下の受け入れ条件が要求する 64 / 16 と合わない。**測り直す。** |
| **⓪** | **本実装プロンプト**（`correction-v1`）・RAG なし・検証なし | **必要**（80件） |
| **②** | ⓪ ＋ **検証①（書き換えすぎの検出）** | **ゼロ**（⓪の出力への後処理） |
| **③** | ⓪ ＋ **検証③（余計な直しの抑制）** | **ゼロ**（同上） |
| ~~①~~ | ~~＋RAG・検証なし~~ | **今週は出さない**（RAG 未着手） |

**検証②（レベル超過による再生成）はこの表に載らない。** `dialogue.reply()` の中で起きるもので、
評価スクリプトは `correction.check()` しか呼ばないため、訂正側の記録に置くと構造上つねに 0 になる。
**検証②は 90返答の測定記録（`level_compliance`）が持っている**（測定済み：1発目 94.4% / 再生成後 98.9%）。

**旧計画は②③を「①の出力への後処理」と書いていた。** ①が無いので**⓪の出力に当てる**。
この差は表に明記する——後から①を足したとき、②③の値は動く。

## 4. ユーザーストーリー

1. **面接官として**、素朴な実装とこの実装が同じデータで並んでいるのを見られるので、
   **「大規模言語モデルに投げただけではないか」という疑いに、数字1つで答えてもらえる。**
2. **面接官として**、各段が「プロンプトの差」と「検証ノードの差」に分かれているのを見られるので、
   **どの工夫がどれだけ効いたのかが分かる**（合計の差だけでは、何が効いたのか言えない）。
3. **開発者として**、一番低い指標がどれか分かるので、**翌週以降の改善サイクルで何を1つ選ぶかを決められる。**
4. **開発者として**、`dev` で測ったと明記されるので、**調整に使った側の数字であることを自分で誤解しない。**

## 5. 受け入れ条件

- [ ] **ベース／⓪／②／③ の4段が、同じ `dev` 80件・同じモデル・同じ日で測られている**
- [ ] 各段について `detection_accuracy` と `over_correction_rate` が出ている
- [ ] **分母と誤差が表の中にある**：`detection` **64** ／ `over_correction` **16（1件 6.25pt）**。**4段すべてがこの分母である**（＝ベースも現在の `items_digest` で測り直した、ということ）
- [ ] **`dev` の数字であり、本実装は調整に使った側なので差は上振れする**旨が表に書いてある
- [ ] **①列が空であることと、その理由（RAG 未着手）**が表に書いてある
- [ ] 実行記録が `evals/runs/` に残り、`items_digest` / `thresholds_digest` / `prompt_version` / `scorer_version` が入っている
- [ ] **`dev` から5件を手で照合し、実行記録の `scorer_checked_on` を埋めた**（採点器の検査。毎回の工程。**既存の実行記録は7本中6本が `null` のままなので、今回は必ず埋める**）
- [ ] **測る前に書いた予想**が design.md にあり、**外れた場合は外れたと書いてある**

## 6. 制約事項

- **`test` 40件に触らない。** 項目の変更も禁止（`items_digest` が動く）。`test` の最後の1回は **9/14〜9/20**（2026-08-18 に 11/16〜11/22 から前倒し）
- **追加の API 呼び出しはベースの80件と⓪の80件＝計160件。** ②③は後処理なのでゼロ（2026-08-18 に「⓪の80件だけ」から訂正。理由は §3 のベース行）
- **プロンプトを変えない。** 今週は測るだけで、直すのは翌週以降の改善サイクル
- **閾値を変えない。** `rewrite_too_far` は 0.85 で確定済み
- ~~**平日0.5hに実装を置かない。** ステアリング3点は平日枠（火・水・木）に1日1ファイル~~ → **2026-08-18 に撤回。** 平日も2h枠なので実装を置いてよい
- **予備枠を作業で埋めない。** 余ったら余ったまま週を閉じる

## 7. 今週わかっていること — 検証③は公平に測れない

**先に書いておく。結果を見てから言い訳にしないため。**

検証③（余計な直しの抑制）の規則は「**根拠が引けておらず、かつ元の文が十分自然なら『直す必要なし』に倒す**」である。
**RAG が無い今週、「根拠が引けていない」は全件で真になる。** つまり判別を担うのは丁寧さの述語だけになる。

そして**採用基準は事前登録されている**：「`dev` で `false` 側の発火率が `true` 側より **20pt 以上**高いこと」。
2026-08-10 の実測では、場面の段階を見る版で **93.3% 対 81.1% ＝ +12.2pt** だった。**20pt に届いていない。**

したがって**今週の③は、検証③の採否を決める測定にはならない。**

- **今週の③は「根拠が常に空のときにどう振る舞うか」を見る数字**であって、それ以上ではない
- **採否の判定は、RAG が入って「根拠が空」が本当に判別として働くようになってから**（8/24〜8/30 以降）
- **この但し書きを表に書く。** 書かずに③を載せると、読む側は採否の根拠だと受け取る

**これは失敗ではなく順序の問題である。** ただし「測ったが判定には使えない」と
**自分から書けるかどうか**が、この表の信頼性を決める。

## 8. 今週やらないこと

- **RAG 一式**（依存追加・節割り・埋め込み・Chroma 登録・`search`・`grounding_ids` 差し替え） — **今週 8/21〜8/23 の枠に入るが、本書の範囲外**（冒頭の但し書き参照）。**当たり率測定と `score_min` は第4週**
- **①列の測定** — RAG が繋がってから（第4週 8/24〜8/30）
- **改善サイクル** — 第4週 8/24〜8/30。**今週は一番低い指標を特定するところまで**
- **`test` での再測定** — 9/14〜9/20 の1回だけ
- **応答時間・コストの集計** — 恒久的に降ろした（公開指標にしない）
- **FastAPI・LangGraph・公開 URL** — 8/31〜9/6／**音声** — 9/7〜9/12／**Docker** — 9/14〜9/20
